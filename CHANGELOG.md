# Changelog

KINO has no published release yet. Changes intended for the first release collect under **Unreleased**. Git remains the record for work completed before this file existed.

## Unreleased

### Changed

- **Firmware 0.4.58: the on-device UI laid out for the hand that holds the shutter (#177).**
  The shutter is at X 115 on the body's top wall, so the right hand wraps the
  +X end, the index finger is on the button and the right thumb is the only
  digit free for the glass. The panel is 93.6 x 56.2 mm for 800 x 480 px
  (8.5 px/mm): the 44 px control this file called "the floor a thumb needs"
  was 5.2 mm, and every screen's Back sat at x 16, the far corner from that
  thumb. What moved: the header's system buttons - Back, and the gallery's
  PREV / NEXT beside it - are a cluster at the hand's edge, 64 x 52 px, with
  the caption plate taking the far side; the finder's MENU is in the same
  corner and its status bar puts the readings (cameras, power, card) at the
  far end and the three decisions (mode, flash, look) at the hand's end as one
  control that opens LOOK, which returns to the finder; the menu is two
  columns, the SHOOT card at the panel's full height on the hand's side and
  the five rows an 84 px column on the other; the photograph screen puts the
  picture on the far side and its column on the hand's, DELETE at the top,
  FAVOURITE at the foot nearest the thumb's rest, and PREV / NEXT photograph
  above the stack (within the page in place, past its end by turning the
  page and returning to the grid); LOOK's trio runs COLOUR, MODE, FLASH so
  the flash is under the picker, and the QUAD target row puts ALL at the
  hand's end; a confirm dialog sits on the hand's side with the action on top
  and Cancel on the bottom row, which is the one a thumb coming up from its
  rest reaches first. Segment bands and picker buttons go to 52-56 px. The
  open and back moves follow the two columns: opening the card, the rows
  column leaves toward its edge; opening a row, the card leaves toward its
  own edge while the rows part around the touched one. Two palm rules: a
  contact that begins on the hand's edge is not a finger, and one that
  drifts more than 40 px is released with nothing fired. All of it is one
  flag, `body.hand` (`right` by default, `left` mirrors), read once per frame
  and written as an inset from the hand's edge (`from_hand()`), so a
  left-handed shooter is a config write. `kino-ui.wasm` re-baked;
  `host_preview` renders every screen and the moves.
  Second pass, from filming the moves in the Twin: the other menu column
  leaves in the first half of an open move and returns in the second half of
  the back move, so the growing screen never runs edge to edge against the
  card it is replacing - on one clock the two edges moved together and read
  as two cards racing. Every list hangs from the header (`LIST_TOP_DEFAULT`)
  instead of centring when short: POWER's and STATUS's first rows sat a third
  of the way down while SETTINGS' sat at the top, the same control in a
  different place on every screen. POWER is RESTART, the shut-down
  instruction, and a RUNNING ON group (USB or BATTERY, no gauge on this body);
  the CANCEL row went, because Back is on every header. SOUND's volume band
  goes to 52 px on the list's own margins. STORAGE's list-to-gauge gap goes
  from 20 to 14 so its readings stay inside the safe area now that the list
  starts at the top.
  Third pass, on the camera: the moves were choppy on the panel and smooth
  in the Twin, which steps virtual time and cannot be choppy, so every move
  now reports frames, wall time, presenting time and drawing time per frame
  to the log ring (`anim_report`, `gfx_present_us_total`), and the boot
  measures one rotate. The numbers: the PPA rotate of a frame took 30 ms
  with the PSRAM at 80 MHz and the CPU drew at 45 MB/s, so the splash ran
  at 19 fps and the menu cascade at 15. Rotating in strips gained 4 percent
  and is not kept. `CONFIG_SPIRAM_SPEED_200M` (with the experimental flag
  IDF puts it behind on the P4) takes the rotate to 16 ms and halves the
  drawing: splash 35 fps, cascade 27, memory test passing; the reasoning is
  in `sdkconfig.defaults`. Also: opening the P4's USB serial port from
  Windows resets the chip (`CHIP_USB_UART_RESET`), so the bench's first
  request landed in the bootloader and every later one failed for want of
  the lost HELLO; `kino-bench.mjs --settle <ms>` waits out the boot. A real
  open move then measured 25 fps: 15 ms presenting and 23-29 ms drawing per
  frame, most of it a whole-canvas clear under a blit that covered it, so
  `open_frame()` clears only the ground round the card. A pipelined present
  - a second canvas, the PPA rotating one frame while the CPU drew the next -
  was built and measured too: 35 to 36 fps, because the drawing slowed by
  what the presenting hid. The PPA and the CPU share the PSRAM bus and at its
  practical 90 MB/s a full frame is 27 ms in any order, so the pipeline is
  withdrawn and the finding is written at the top of gfx.c. The boot cascade
  stops recopying the whole canvas every frame and writes only the rows still
  moving. Moves on the camera now run at 26-30 fps, the splash at 35 and the
  cascade at 45. And the boot field fills the panel by its own clock: its
  reach was scaled by how many cameras had answered, so a body with four
  silent nodes booted with a field stopping at 42 percent of the screen and
  read as a failed boot; a quadrant whose camera is silent now stays in the
  cold greys instead, which says the same thing without looking broken.
  Fourth pass: the second half of an open move no longer copies the arriving
  screen into the canvas - `gfx_present_with_stash()` rotates the card's
  rectangle straight out of the stash and the canvas round it in four blocks,
  checked byte for byte against the whole-frame rotate at boot and falling
  back to the copy if they differ. (Presenting only the ring where the card
  grew was built and found impossible here: the card is hung from the
  arriving screen's top edge, so every pixel inside it scrolls as it rises.)
  The splash draws each glyph row as one span instead of up to five squares.
  Measured on the camera: open moves 41-43 fps and back moves 37-39, from
  28-31 and 26-29, with the drawing down to 7-10 ms a frame; the 16 ms rotate
  was then the frame.
  Fifth pass: the rotate is gone. A frame is no longer drawn into a landscape
  canvas in PSRAM and turned by the PPA; `gfx_render()` draws it in 128 x 48
  tiles into two buffers in internal SRAM, and the PPA rotates each finished
  tile straight into the portrait framebuffer, non-blocking, while the CPU
  draws the next. PSRAM carries the 768 KB write and nothing else. Every
  primitive in ui.c draws through a clipped window (`gfx_target_t`, `cv_ptr`)
  so the same screen code draws a whole frame or one band of it; every frame
  the shell shows is a `gfx_draw_fn` the compositor calls once per tile
  (`render_screen`, `render_field`, `render_open`, the marks), and a move's
  two ends are drawn into the canvas on purpose (`gfx_render_canvas`) for the
  stash, the layer and the snapshot the push and dissolve still use. The 189
  preview renders are byte-identical before and after. On the camera: splash
  76 fps (8 ms drawing, 4 ms waiting on the engine, per frame), from 32.
  Toasts expire between frames, never inside a draw, because a draw now runs
  once per tile. The direct-from-stash present and its block self-check are
  gone with the rotate they skipped. Open moves 48-64 fps and back moves
  53-61 (10-14 ms drawing a frame, most of it stash and layer reads from
  PSRAM), against 41-43 and 37-39 before; the panel's 60 Hz is the ceiling
  now. The cost to watch: the two tile buffers are 24 KB of internal SRAM,
  and the boot's minimum free internal RAM went from 81 KB to 35 KB with the
  radio's recovery reserve still held (2/2).
  Sixth pass: frames are paced to the panel. Nothing waited for the refresh,
  so a frame could be written into the framebuffer the panel was still
  scanning (a tear), and frames landing whenever they were ready against a
  fixed 60 Hz scan were shown for one refresh or two at random (the judder a
  60 fps move still had). The DPI driver's refresh-end callback now gives a
  semaphore, and whoever is about to write the back buffer waits for the first
  refresh end after the last hand-over (`wait_back_free`). A frame that draws
  in under 16.7 ms is shown exactly once: splash 76 -> 59 fps and steady,
  3 ms of each frame waiting for the panel. The cascade, still on the PPA
  composite path, goes 45 -> 40 for the same wait. And sleep shows the camera's mark
  before the backlight goes: power_task asks the UI (`power_sleep_pending`),
  the UI puts the mark up and answers (`power_sleep_shown`), the mark holds
  600 ms, then the light goes; a touch in that window calls the sleep off
  and the screen comes back. Sleep used to cut the light from whatever was
  on screen, which is what a crash looks like.
  A seventh pass takes the menu moves off the compositor altogether. A row
  opening or a card closing used to be assembled from a PSRAM stash of the
  destination and a retained layer of the menu, blitted into place each
  frame; the reads through PSRAM were most of the 10 ms of drawing left in a
  frame, and preparing them was a visible hitch at the start of every move.
  A move is now drawn from state: `gfx_target_view` lets the compositor
  offset a drawing window, so the menu columns draw themselves shifted and
  the destination screen draws itself into the growing card, tiled through
  internal SRAM like any other frame. Nothing is prepared before the first
  frame, so the move starts on the tap. The snap at either end goes with it:
  the card face keeps the row's tint until a third of the way in and the
  row's words fade out over that same third as the screen fades in under them;
  on the way back the face carries the resting tint and the words fade in.
  The host preview renders byte-identical except in that wash phase.
- **Firmware 0.4.57: the on-device UI measured, and the numbers fixed (#176).**
  An audit of `firmware/p4/main/ui.c` as it runs in the Twin's SCREEN VIEW,
  with WCAG contrast computed from the palette defines and every screen
  driven and captured through Playwright. What it found and what changed:
  the second line of a list row was the title pulled a fixed 43% into the
  row face, which measured 2.6:1 at the top of the ramp and 5.1:1 at the
  foot; `dim_ink()` now measures the face and finds the quietest mix that
  clears 4.5:1, and a disabled row (SHUT DOWN's "Hold the power slide" was
  1.5:1) is set in that voice instead of a grey chosen for the dark face.
  `W_GRAYTEXT` moves from 6E7D74 (4.4:1 on the face, 4.1:1 on a window) to
  7A8A80 (5.2 / 4.8); red as 18 px type (the DELETE label, a destructive
  dialog's title) gets `C_RED_INK` at 5.9:1 where `C_RED` measured 3.5.
  Controls get an edge a thumb can find: `W_KEYLINE` at 3.4:1 on the face
  replaces the 1.3:1 hairline on buttons and is added to segment tracks, the
  off toggle and the picker buttons (WCAG 1.4.11). The header's condition
  chip, the menu card's state line and the STATUS rows carry the severity as
  a word - FAULT, WARN, NOTE - where a coloured dot carried it alone, and the
  yellow dot on the terracotta plate measured 1.3:1. SOUND's picker buttons
  go from 36 to 44 px and DISPLAY's bands from 40 to 44, the last controls
  under the file's own floor. The list rows' position numbers are gated on
  `UI_FOCUS_VISIBLE` like the segments' already were. Notes on SOUND,
  CONNECTION and STORAGE move from a literal x of 24 to `PAGE_M`. Four
  colours that were in no palette - two blue-greys for NO CAMERA, a
  blue-tinted gallery strip, and the 1998 desktop's 0000A8 in the ROLL
  progress fill - become `D_DIM`, `D_PANE` and `W_SEL`. And a touch or a key
  during the 3.8 s splash now ends it on that pass and hands off to the menu;
  a shutter press used to wait in its queue until the field had left.
  `kino-ui.wasm` re-baked; `host_preview` renders every screen.
- **Twin: a viewfinder pane that stops receiving frames stops being live.**
  The firmware shim marked a pane live for ever once its buffer pointer had
  been fetched, so SCREEN VIEW showed four black panes under a lit 4/4 when
  the preview feed stalled - the rig's timer stops with a hidden tab, and
  nothing noticed. The page now reports each landed frame (`kui_vf_landed`)
  and the shim ages them on its virtual clock with the driver's own two-second
  rule: NO RECENT FRAME, and out of the strip's count, exactly as
  `viewfinder.c` reports it on the camera.
- **Twin: the stage starts as the party scene, not as a void.** The stage
  store began with no subjects and no room, so the virtual sensors
  photographed nothing: four pure-black panes on SHOOT under a lit 4/4, and a
  black JPEG from every capture, until someone found PARTY TEST SCENE on the
  STAGE tab. The scene the brief asks for as the one useful default is now
  the start state; CLEAR STAGE still empties it. Found by sampling every 2D
  canvas the rig writes - the 320x240 preview was all zeros in both views.
- **Firmware 0.4.57, second pass.** A control standing on a warm row draws
  its edge in that row's quiet ink (`edge_ink()`): the grey keyline tuned
  for the dark face measured 1.1:1 on the top terracotta row, under the SOUND
  pickers and the off toggles. The STATUS rows drop the severity dot - the
  word carries it - and start on the same left rule as every other list.
- **Firmware 0.4.57: opening SHOOT no longer flashes the finished screen.**
  The pass that opened the viewfinder ran on to the viewfinder's own repaint
  tail - `s_screen` was already SHOOT and nothing was held - and presented
  the destination at full size before the open move's first frame; the next
  pass played the move from the card and landed on the same screen again.
  Recorded at 80 ms intervals in the Twin: full screen, card, growth, full
  screen. A pending move now owns the panel from the pass that starts it.
  And a pane that has never had a frame holds its ground for 1.5 s
  (`SH_SETTLE_US`) before it says NO CAMERA: the first frame is a request,
  a JPEG and a decode away, and for that long every pane called four
  answering cameras absent and then cut to pictures at once. STALLED and
  ERROR are not waited out.
- **A frame is drawn once, and a move is a state rather than a loop.** Four
  things were wrong with the interface's motion and only one of them was the
  easing curve. They were found by counting, not by looking: the renderer now
  reports what each frame costs, in units of full 800x480 screens, and the
  camera's own budget makes that number a time - the canvas is in PSRAM at
  160 MB/s, the DPI panel eats 46 MB/s of that continuously whatever else is
  happening, and 114 MB/s is 1.9 MB per frame at 60 Hz.

  **A blocking move cannot survive the Twin.** Every animation was a `for(;;)`
  that read the clock, drew, and presented until its time was up. The Twin
  runs this same code as WebAssembly and a pass is ONE call: whatever it
  presents lands in a ring of 40 frames and is played back afterwards. The
  boot sequence presented about 119. Seventy-nine of them went into the same
  slot, so the splash played its first third and snapped to its end - the
  frames were computed and thrown away, and no easing curve was ever going to
  fix that. Moves are state now, and `anim_tick()` advances one frame per UI
  pass: measured on the shipping wasm, a menu open is 35 passes of exactly one
  frame each, and the splash never puts more than two in a pass. The camera
  gets the same fix for a different reason - a 2.1 s splash no longer owns the
  UI task while a shutter press waits behind it.

  **A frame that only moves should not be drawn again.** `gfx_layer_keep()` /
  `gfx_layer_blit()` keep a second retained surface, and the menu's open move
  carries its six cards as two blits instead of redrawing them. That took the
  move from 1.38 screens a frame to 1.09, and - the part that matters - its
  spread from 2.1x to 1.5x. Even frames are what the eye reads as smooth, more
  than fast ones, and the old move was slowest at its start, which is exactly
  where the eye is following it.

  **And it ended with a hitch.** Every transition finished by calling
  `draw_screen()` on the destination - up to 3.12 screens on GALLERY against
  about 1.0 for a composited frame - to produce a picture the card was already
  showing. Opening, the last frame is composited now; going back it is still a
  real draw, because going back the stash holds the screen being left.

- **The boot screen is the reference's field, traced frame by frame, and it
  is the only thing on the screen.** The first attempt copied the wrong clip -
  the round device's concentric bloom, which the reference does do and is not
  what its startup is. The startup is a **square lattice of small glyphs**
  that grows out of a seed of about four cells, fills the panel, holds, and
  draws back in. Four things make it read the way it does and only the last is
  a colour choice. Cells sit on a regular pitch in rows and columns, so the
  diagonals you see are the lattice and not something drawn diagonally. Each
  cell holds a mark from an alphabet rather than a dot, which is what stops
  900 cells reading as noise. Weight falls with distance - hashes, discs and
  solid squares in the middle, single dots and short runs at the rim - and
  that gradient is why it looks like something radiating instead of a texture
  somebody tiled. And **every cell changes what it is while you watch**: a
  cell holds a mark for about 190 ms and then is a different one. Not off a
  global counter, which makes the whole panel blink at once and reads as a
  glitch, but off a clock offset per cell, so the changes scatter. Whether a
  cell is lit at all is fixed by position and never re-rolled - a field whose
  cells appear and vanish twinkles, and the reference's does not.

  **Nine glyphs, and they are the reference's nine.** The first read of the
  alphabet had a plus and a pair of HORIZONTAL bars where the reference draws
  vertical ones - a mark turned ninety degrees is a different mark - and folded
  the asterisk into the hash, which are the two commonest things on that screen
  and are not the same shape: the hash has a dark centre and dark corners, the
  asterisk is solid through the middle with its four diagonal points detached.
  Most of the rim turns out to be pairs of short uprights. So: hash, asterisk,
  a square ring broken at one corner with a dot inside, a stepped circle, a
  solid square, two uprights, one upright, a short run of dots and a dot -
  every one of them a 5x5 bitmap at three pixels to the bit.

  **Nothing in the field is a drawn shape.** The circle and the square were
  going through `disc()` and `round_rect()`, which put the one antialiased
  curve in the whole picture - a smooth circle among nine pixel forms, which
  is as wrong as a smooth letter in a bitmap font. Blown up twenty times the
  reference's own circles step: a flat top a few pixels wide, out one, out
  again. It is drawing a pixel circle, so this draws a pixel circle, and the
  two forms differ only by whether the corner bits are set - which at three
  pixels to the bit is the whole difference between a round thing and a square
  one. The arrival is carried entirely in the ink now, which is where a bitmap
  can carry it.

  **And it is drawing this camera, not somebody else's lamp.** The reference
  is a light remote, and both quantities in its display are facts about a
  lamp: the field's size is how bright the light is, and its colour is the
  colour the light is being set to under your finger. That is why it holds
  attention - it is not decoration, it is the instrument's read-out. Copying
  the picture without the meaning would have given us a screensaver.

  A D4 is the opposite instrument: it does not emit light, it gathers it,
  through four lenses. So the two quantities carry the two things that are
  actually true about this machine while it wakes up.

  **How far the field reaches is how many of the four cameras have answered.**
  One quadrant each - cam 1 top left, running across then down, which is how
  the four sit on the viewfinder, in the gallery's mark and on the body -
  eased rather than switched, so a node answering part-way through the boot
  opens its quarter out instead of making it appear. A camera that has not
  answered holds its quarter at 42% rather than blacking it out, so the shape
  stays a field instead of a pie chart with a slice missing, and the
  quadrants blend across their axes because a hard quarter boundary draws a
  cross through the middle and the reference's field has no cross in it. What
  this buys: **a body with a dead node says so on the first screen it shows**,
  without a word of type, in the product's own mark, which is four.

  **What colour it is, is the look it will shoot with.** The lamp's colour is
  the light; a camera's colour is its look, which is the recipe every
  photograph gets graded through - the same role in the same grammar. The
  field comes up cold and travels to the look's own colours as the cameras
  answer: the camera finding what it is shooting with. The look is hashed to
  one of four palettes rather than tabulated, because looks are the operator's
  and are made in Studio, so there is no list here to colour by hand; what
  matters is that it is stable, so a given look brings the same colour up
  every time and the way your camera starts becomes something you know about
  it. The palettes are the camera's own - the reference's hues where they fit
  the product and dropped where they did not, because a camera that boots
  violet is a camera pretending to be somebody else's object.

  **The two snaps were the re-roll clock, not the motion.** The field's
  geometry was provably continuous across its phases - the reach curve, and
  the lit-pixel count measured off the rendered frames, show no step at either
  boundary - and it still visibly hitched twice. Each cell picks its mark from
  a generation counter derived from elapsed milliseconds, staggered by a
  per-cell offset so the changes scatter; but that number was measured from
  the start of the current PHASE, and starting a phase resets it. So at each
  of the two internal boundaries the counter went back to zero and every cell
  in the field re-rolled on the same frame. Three phases, two boundaries, two
  whole-field flashes, in exactly the two places the motion was smoothest. It
  runs off the start of the whole sequence now. Counting the interior pixels
  that change per frame is what found it and is what shows it gone: flat at
  about 1,500 through the hold, where it had been spiking threefold.

  **And the motion itself is one motion.** The growth had been two eased
  beats glued end to end - a seed easing out to a stop at 132 px and a bloom
  easing back in from rest - and smootherstep has zero velocity at both ends,
  so the field visibly stalled a third of the way out and set off again. It is
  one curve now, 0 to 590 px. The retreat used `1 - smootherstep`, which
  flattens as it finishes: the last of the field crept away over a third of a
  second and then the panel sat empty waiting for the phase to end. Something
  leaving should gather speed, so it is `1 - t^2` and hits nothing exactly as
  the menu arrives. The field also starts at a 52 px seed rather than zero,
  because smootherstep has moved six per cent a fifth of the way through and
  the first four hundred milliseconds were a black screen; the reference's
  first frame already has cells in it. The frontier is soft and ragged rather
  than a hard expanding circle - each cell arrives at its own radius and comes
  up out of the ground over 66 px of travel - and the jitter grows with
  distance, because a thing spreading out is tight at its origin and ragged at
  its edge. The whole sequence is 3.76 s, and it is centred on the panel: it
  had been sitting 32 px high, holding a gap for a wordmark that is no longer
  on this screen.

  **Nothing else is on the boot screen.** It had carried the studio's disc at
  its centre and the camera's wordmark under it with a four-cell mark under
  that; the reference's startup carries none of it, and it is one thing
  happening, which is why it holds attention for two seconds. Both marks keep
  the place that is theirs: the studio signs the ABOUT screen, and the
  wordmark is what the camera shows when it is **going away**. Shutting down
  and restarting now put `kino D4` in the middle of the screen and then let go
  of it, fading through the ground rather than cutting to black - the backlight
  is still on for a moment after that returns, and a hard cut leaves an empty
  lit rectangle. The old shutdown took the four cells out one at a time, which
  is a sentence about the four cameras: the right thing to say while a capture
  is happening and the wrong thing to say while the camera is switching off.

- **The renderer fails a run that sets type outside the safe area, and it
  found four screens doing it.** `text()` reports any string drawn outside the
  page margin, deduped and named by the screen it happened on. Clipping is the
  obvious fault, but the one the interface actually had is a sentence ending
  2 px from the bezel: it does not look broken, it looks cheap, and it is much
  harder to catch by eye. The viewfinder's status bar is exempt by saying so
  at the bar, not by a magic range in the checker. What it caught: the About
  screen's camera note running off the right of the panel and over the bezel
  (the break was chosen by hand for the old bitmap face and never revisited
  when the type changed - notes wrap on a measurement now, `text_block()`);
  the ROLL screen's "Wi-Fi is up. They go when KINO answers." doing the same,
  386 px in a 344 px column, on the one screen a guest reads at a party; the
  Storage screen's two readings sitting 1 px past the bottom of the panel with
  their descenders in the bezel, because 406 px of content was being asked to
  fit a 401 px body; and the ABOUT name box overrunning by a pixel while the
  maker's plate opposite it was already on the margin - the two columns of a
  two-column screen were ending on rules 9 px apart. ABOUT lays its left
  column from the margin back up now, and ROLL anchors the block whose height
  depends on the camera's situation to the margin so its slack comes out of
  the middle of the column, where nobody can see it move.

- **The photograph screen's column had two grammars.** Bare type on bare
  ground at the top, three rounded controls at the foot, 114 px of nothing
  between them. The facts go in a well on the same rail as the buttons, and
  CAMERAS sits above the mark it names rather than under it - every other
  label on this interface is over the thing it labels. `PH_LINE` was also
  still 18, the old bitmap face's line height, so the one assert standing
  between the caption and the buttons had been measuring a column a third
  shorter than the one being drawn.

- **The guest half is gone from the records too.** `D4_INTERACTION.md` argued
  for a guest/owner split behind a physical switch and was still telling
  readers the shell is a Windows-era placeholder, the register is undecided,
  and to build a specimen next - none of which is true. It records what
  replaced it, and why the argument lost, and keeps the production-state list,
  which was the most useful page in it and is not about guests. `ECN-0006`
  asked hardware for a pin to switch a UI that no longer exists and is
  withdrawn, with its reasoning kept: a lock that is a config key is not a
  lock, and that should not have to be worked out twice. `hardware/WIRING.md`
  says `SLIDE_MODE` is unassigned again. The renderer's `KINO_GUEST_PREVIEW`
  flag was dead and is removed.

- **The studio is named.** Odd Jobs makes this camera, and the firmware
  carries its mark - baked from the studio's own artwork by `tools/mklogo.swift`
  into a 160x160 coverage map that `ui.c` samples, at the centre of the boot
  bloom and at the foot of ABOUT. It is a trade mark and not source:
  `firmware/**` is MIT and that grant does not reach it, so REUSE.toml, LICENSE
  and THIRD_PARTY_NOTICES all say whose it is, and `license:check` fails if any
  of the three stops saying so.

- **The camera stops being a Windows 98 desktop.** The interface was the 1998 shell - four-tone bevels, a navy title bar, six icon tiles, 1-bit Tahoma baked through a headless browser, and a composite-video filter that band-limited the chroma of anything with colour in it so the artwork would resolve the way it did on a CRT. All of it is gone, and what replaced it is one register with a short vocabulary: a near-black ground, one warm card, one warm accent, one mint, a single 10 px corner on every surface, and no edges at all. `W_FACE`, `W_WINDOW`, `W_SEL` and the rest keep their names because the *structure* those tokens describe - a face, a window, a selection - was never the problem; only what they are worth changed.

  **Nothing is baked any more.** `icons_w98.h` (Microsoft's shell artwork, and the licence question that blocked every firmware release - #134), `ui_labels.h` (eleven screen titles stamped to bitmaps by a tool needing Playwright and a Windows path that only existed on one machine), `icons.c`, `tools/mktext.mjs`, `scripts/bake-w98-icons.mjs` and the Twin's second private wasm variant all go together. A screen title is one word in the interface's own voice; there was never a reason for it to be a different kind of object, and the reason it was is that the type could not draw it well enough.

  **The type is Inter and Oxanium, at coverage rather than one bit.** Inter carries everything a person reads as language - the rows, the values, the sentences - and Oxanium, a chamfered techno face, carries everything the camera says as a machine: the screen titles, the technical caps, the word on the card. Both OFL, rasterised to 8-bit coverage by `tools/mkfont.swift` (CoreText, greyscale, never subpixel), so every glyph blends into whatever is behind it. The old header was thresholded to one bit and every diagonal on the panel was a staircase; at 19 px the difference between a stem 1.4 px wide and one rounded to 1 or 2 is most of what makes a face legible. Rounded rectangles, discs and the chevron are sampled the same way, so nothing on the interface has a hard edge on a curve.

  **And it moves.** `gfx.c` gained three motions that ride the PPA the dissolve already used. `gfx_open()` is a row becoming a screen: the list parts (each row accelerating out of frame, the nearest first), the row lifts and grows in both directions, keeping its own face, its own word and its own corners the whole way, and the destination pours into it from the top edge once the card has been itself long enough to be recognised. `gfx_slide()` pushes one screen off another with the outgoing frame travelling three-eighths as far as the arriving one, because two sheets locked together read as one sheet. `gfx_cascade()` lands a list one row at a time. All three ease on smootherstep - no speed and no acceleration at either end - and run 340-700 ms, which is long enough to read; the first cut ran at 160-220 ms and was over before the eye found what had moved.

  Two bugs in the Twin's own playback were fixed on the way, and they were most of what "choppy" meant. Its frame queue timed a pass's frames from the moment the pass *started*, so every frame computed while the CPU was busy arrived late and was dropped - the first half of every transition was computed and thrown away. And it allocated a 1.5 MB `ImageData` per frame, putting 20 MB through the collector during the animation it was allocating for; there is a pool now.

  **The move is drawn, not composited.** The first cut of `gfx_open()` slid two
  finished frames around each other, which is all a compositor can do, and it
  is why the word on the card stopped following it: a rectangle of pixels cut
  out of a 50 px row cannot become a 40 px title, because by the time it is a
  picture the letters are the size they are. So the renderer keeps the one
  thing it is good at - holding a frame (`gfx_stash`/`gfx_stash_blit`) - and
  `open_anim()` in `ui.c` draws every frame of the move with the real fonts.
  The word travels from where it sat in the row to where the screen's title
  will be, changing face on the way, and arrives a moment before the title is
  drawn there, so the eye follows one continuous object from the row it touched
  to the screen it is looking at. The card's target is carried as an offset
  INSIDE the card rather than as a screen position, which is the bug that had
  the word reach the top of the panel long before the card did and land on the
  menu row above it.

  **And the register reaches the whole product, not the half of it that was
  easy.** `draw_row_at()` is now one row grammar - a position, a name, and what
  the name is set to - and every list on the camera is made of it: SETTINGS,
  DISPLAY, SOUND, CONNECTION, STORAGE, ABOUT and POWER, at each one's own pitch,
  with a one-line fallback for the narrow second column ABOUT keeps. The
  confirmation dialogs and the toast were the last two objects still built out
  of the 1998 grammar - a raised window with a caption bar, and a square
  INFOBK plate - which is the worst place for an inconsistency, because
  "delete this photograph" is the moment a person most needs to know what they
  are looking at. Both are cards now. The last five bevel primitives have no
  callers and are gone; there is no `bevel4` left in the file.

  Three collisions the typeface change caused and the first pass shipped
  unlooked-at are fixed, each at its cause rather than by nudging a number:
  the camera column on ABOUT measured its two columns against the face it used
  to be set in and now truncates against the space it actually has; the sound
  picker was drawn through its own row's title and is now the row's value with
  the buttons at the end; and the photograph's readout ran off the left edge of
  the panel, so `chrome_state()` takes a left bound and drops a reading rather
  than drawing past it - the condition chip is laid last and survives longest,
  because it is the only part of that strip a person has to act on.

  **The open, third time, and the sentence it finally makes: the row IS the
  screen's header.** The destination is revealed inside the growing card,
  cropped to it and hung from its top edge, so the first frame fills the row's
  own rectangle with the top 50 px of the screen it opens - which is that
  screen's header, with its title already in it. Nothing has to travel and
  nothing has to be re-typeset, because the title was always in the right
  place: the card's top edge is where a header lives. The row's colour washes
  off over the first third, so the card visibly starts as the thing that was
  touched and becomes the thing that was opened. The version before this flew
  the word to where the title would be and left a large empty rectangle of the
  pressed row's orange behind it for ten frames, which is the loudest thing an
  interface can put on a panel. The per-row stagger went too: a near row
  starting before a far one overtook it, so the card clipped the menu's own
  card and the gaps went uneven; the list parts as two rigid blocks on the
  card's curve now, because this is one gesture.

  Going back was drawing the wrong frame entirely - the stash was taken after
  the redraw, so the card shrinking into the GALLERY row contained the top of
  the menu. The caller stashes whichever end of the move the card holds.

  **And the animation is a sequence of stills you can hold up against each
  other.** `prev_vclock_us` lets the host renderer own the clock and step it
  once per presented frame, so `anim_open_00..27.ppm` and `anim_back_*` come
  out of `npm run preview` like every other screen. The open could only be
  judged by watching it in a browser before, which meant it could only be
  judged carelessly.

  **One page margin.** Measured on LOOK, a single screen had four left edges
  and four right ones - 7, 16, 19, 24 and 775, 780, 783, 792. Nothing lined up
  with anything, and that wobble down both sides of every detail screen is the
  loudest thing on an interface that says nobody drew it. `PAGE_M` is 16 and
  the header plate, the back button, the list cards, the group fields, the
  segment tracks, the LOOK detail strip, the gallery grid, the viewfinder's
  status panels and the photograph's column and well all sit on it. The
  gallery's gutters gave way instead of its tiles (6 across, 8 down) because
  the tile size is the decoder's; a static assert now fails if that grid stops
  ending on the margin.

  **The menu had no floor.** The row ramp ran to 0B1A15 against an 0B0D0C
  ground - a summed channel distance of 20, where the top row has 129 - so the
  bottom two rows had no container and the list appeared to fade out through
  the foot of the panel. The ramp is shallower and stops well clear of the
  page (68 at the darkest). With it: one left rule and one right rule for the
  card and the rows, `text_ink()` so a 74 px headline and a 17 px caps line
  share a left edge rather than differing by their side bearings, a chevron on
  every row because every row opens a screen and that is what a chevron means
  everywhere else in the product, the gallery count in the face every other
  reading uses, and the severity dot on the card's state line so a fault and
  an instruction are not in the same voice.

  Four more, each measured rather than noticed: the viewfinder's status
  readings were the last Inter on a strip of technical caps; the gallery's
  `quad` badge was set in the near-black an accent plate is written in, on a
  near-black strip, so the one word that tells two kinds of photograph apart
  was the least legible thing on the screen; the photograph's capture id did
  not fit its column at any size worth reading, and cutting it produced
  "CAP_0000..." - the prefix every capture shares - so the number takes the
  billing and the prefix drops to the line below; and `menu_word_at`,
  `bevel4`, `bevel_raised`, `bevel_sunken`, `bevel_sunken_dark` and
  `bevel_etched` have no callers left.

  **The last four Windows 98 constructions, found by looking rather than by
  grepping for the word.** The caption plate on every screen was a `fill()` -
  a square-cornered rectangle, the one surface in the product without the
  product's corner - and the gallery then painted a second square rectangle of
  the page colour back over its right end to make room for the page buttons,
  which left that plate with a rounded left edge and a hard-cut right one.
  Patching a shape to the size you wanted is a habit from an interface made of
  rectangles; `head_plate_right()` draws it at the width it needs instead. The
  keyboard focus mark was a rectangle on alternating pixels - Win98's dotted
  focus - which around a rounded card is two shapes disagreeing about what the
  control is; it is a two-pixel rounded ring now. And the capture banner was a
  full-bleed strip with a square corner, a hairline across the top and a
  five-pixel tab down its left edge, sitting on the one screen whose other
  chrome had just moved onto the page margin; it is a card on that margin, the
  accent kept as a bar at its left end because a colour with no shape to hold
  it is a stripe.

  Two comment blocks went with them: the chrome section still opened with a
  paragraph explaining the two-pixel 3D edge and why it reads as a physical
  control, and the flash glyph still said every other icon came from the
  Windows 98 archive. Both were true and neither is; a comment describing a
  grammar the file no longer speaks is worse than no comment at all.

  **The header bar had no rhythm, and the readout is now one layout with a
  stated priority.** Measured on the gallery: six readings separated by gaps of
  14, 34, 39 and 22 px, where the gap between two WORDS of one reading is 10.
  A 14 px separator is four pixels wider than a word space, so "GALLERY" and
  "2 OF 3" read as a single phrase and the whole bar collapsed into a run of
  type nobody could parse. Two gaps now - `BAR_READ_GAP` between readings and
  `BAR_GROUP_GAP` between the title and the group - and `chrome_state()` lays
  every reading on the bar rather than two functions each spacing by eye.
  It also measures everything first and keeps by importance rather than
  position: the first version dropped whatever it reached when it ran out of
  room, which cost the gallery its photograph count - the one reading that
  screen exists to show - by two pixels. The page position moved to the bar's
  right end, beside the buttons that change it.

  **And three contradictions that rounding the corners had papered over.**
  Making everything the same shape is not a design system; a system is roles
  with consistent treatment, and the product was breaking its own:

  - **Three radii calling themselves one.** 10 everywhere, 14 on the menu, 14
    again on the dialog, under a comment that said "the product's one radius".
    One now, with a stated rule: anything inset by n uses UI_R - n so the
    curves stay concentric, which is geometry rather than a second radius.
  - **`button()` and `well()` were byte-identical** - same fill, same corner -
    so a control and a readout were the same object until you touched one. A
    button carries a keyline one step off its ground; a well stays flat.
  - **The confirm dialog was a rounded outsider.** Two chips side by side in a
    card, which is what a dialog looks like everywhere else and is exactly the
    problem: nothing else on this camera asks you to choose between two things
    laid out that way. Its answers are `draw_row_at()` rows now - the same
    function the settings list is made of - which also replaces a pair of
    148 px targets with full-width ones.

  Building it out of rows exposed a fourth, and it was the serious one:
  **focus and selection rendered identically** (`focused || pressed` filled a
  row with the accent), so the delete confirmation opened with CANCEL painted
  as the brightest object on the panel. A press changes the face; focus only
  adds the ring.

  **And the screens compose.** Measured across all 64 renders: POWER was 54%
  bare ground, the roll's empty state 71%, and DISPLAY, SOUND and CONNECTION
  between 45 and 46% - three rows hanging from the top of a panel with two
  hundred pixels of black under them. A screen that stops halfway down reads
  as unfinished whatever is on it, because the eye takes the void as something
  that failed to load. `body_top()` centres a body that does not fill the page
  and leaves one that does at the top, and each list screen states the height
  of its own content - rows, the block under them, and the line under that -
  rather than being centred by eye.

  It is one variable set by the draw and read by the hit test, the way
  `s_head_state_left` already works, because a layout the touch map derives
  separately is the bug this file keeps finding. `draw_screen()` resets it
  before every screen: ABOUT, DISPLAY and ROLL read the value and never set
  it, so without the reset they would have drawn at the previous screen's
  offset and the hit test with them.

  **The conditions got a screen, because they were information nobody could
  reach.** They lived in a panel at the foot of SETTINGS that had room for two
  of them however many there were, so a body with four things wrong showed two
  and the words "and 2 more" - and there was nowhere in the product to read
  the rest. `D4_INTERACTION.md` called that out before it was built: the
  conditions are a list, and a list belongs on a screen. SETTINGS grows a
  sixth row when there is something to say, carrying the worst one by name
  ("Cameras are not answering, and 2 more") rather than a count somebody has
  to open the screen to understand, and SCR_STATUS lists them all with their
  severity and what to do about each.

  **The focus ring is compiled out until something can move it.** Nothing on
  this body can: the shutter fires, touch acts on what it lands on, and BTN_FN
  has no pin until the expander ECN-0003 deferred arrives. A ring marking a
  position no input can move is not a focus indicator, it is a decoration that
  looks like a selection - and it was appearing on the last row anyone
  touched. The ring, the focus array and all eighteen call sites stay; one
  `#if` turns it back on the day that pin exists.

  **The menu has its six marks, drawn rather than sourced.** The reference
  carries a pictogram beside each row; this menu had none, because the icons
  it used to have were tile artwork drawn for a 250 x 205 tile and at a 50 px
  row they were twice the height of it. They are built here out of the same
  primitives every other shape uses - a filled rectangle, a disc, a stroke -
  so there is no file, no licence and nothing to bake, and they antialias with
  everything else. One construction rule is what makes six drawings read as a
  set: LOOK is the four frames with one picked, GALLERY the four as a grid,
  ROLL two rings, SETTINGS three tracks with their knobs apart, POWER the
  power mark.

  **And the roll's empty state composes.** It was 71% bare ground - the most
  of any screen - with its heading, its two lines and the card fact spaced by
  three separate calculations, leaving 90 px of nothing above the heading and
  95 more below the lines. One measured block, centred like every other body.

  What is NOT here, and why: **undo on delete.** `storage_capture_delete()`
  unlinks the files, so a grace period is a trash in storage.c rather than a
  UI timer - and a UI that delays the call would show a photograph as deleted
  while it is still on the card, with no persistence across a power cut. The
  confirmation says it cannot be undone because it cannot.

  **The unity audit, and the one it found that mattered.** Extracting every
  typeface and colour token used by every draw function turned up five
  all-caps labels set in the reading face, four different sizes for the one
  role "this screen has nothing on it", and four colours outside the palette.
  The last of those was the serious one: **the four-mark was blue.** It is the
  product's signature - on the gallery tiles, the viewfinder's status bar, the
  photograph's column, the capture banner and the boot screen - and it was
  drawn in `2F70C9` with a yellow cell for the frame just taken, in a camera
  whose palette is a near-black ground, one terracotta and one mint. Blue
  appeared nowhere else, which made the one shape that says "this is a KINO"
  the one shape that did not look like it. Mint for a frame in hand, the
  accent for the one that just arrived, red when a lens lost it.

  With it: the favourite star was yellow and is the accent, because a
  favourite is not a warning; the capture banner's accent was blue and the
  "saved" state green, and both are mint; `C_WELL` was a second name for
  `W_WINDOW` and `W_LIGHT` a bevel token doing duty as a tile fill, and both
  are gone with `C_BLUE` and `C_GREEN`. The sound picker's pressed state was
  `MZ_CARD` where every other pressed state in the product is `W_SEL`.

  And the type roles hold now: caps belong to the display face (SCAN TO JOIN,
  CODE, NO IMAGE, RESTARTING were set in Inter), sentences to the reading
  face, and an empty screen states itself at one size everywhere rather than
  at 40, 26, 25 and 19 px on four different screens.

  Still a documented divergence, not an oversight: the guest half
  (`guest.h`) keeps the palette `D4_GUEST.md` specifies - `FFD22A` on near
  black - which was written before this register existed. It is unreachable
  until `SLIDE_MODE` gets a pin, and changing a written spec silently is worse
  than carrying the difference.

  P4 image 0x12a5f0, 61% of the app partition free, no warnings. `license:check`
  and `twin:ui:check` green; all 65 host renders and both 28-frame transitions
  inspected.

- **The reference device answers like firmware 0.4.55 on D4-V1 hardware.** An audit of `packages/test-fixtures/src/MockKinoDevice.ts` against `firmware/p4/main/kdp_server.c` found the mock still describing the demo body in a dozen places; each is now the firmware's answer on every profile that pins a real build, with `d4-sim-full` the only place a simulated future remains (and only where labelled). `GET_POWER_STATUS` sends `batteryV: null`, `batteryPct: null`, `batteryMeasured: false`, `charging: false`, `state` from USB presence, plus `displayStage`, `idleSeconds`, `displayOn`, `cameraBankPowered` — the pack figures go on the wire only when the effective `powerTelemetry` capability is true, which no profile (the firmware hardcodes it false) but a Twin override can make it; the internal pack model stays for the Twin's own POWER tab. `SELF_TEST` runs the firmware's six checks by name and order (`P4 heap`, `PSRAM`, `SD card`, `SD write`, `CAM1 link`, `CAM1 sensor`) with its detail strings, and no gauge/flash/speaker rows. The base capability object states `flashHardware: false` (no emitter since ECN-0003), `powerTelemetry: false`, `configStore`, `mediaIndex`, `powerManagement`, `radioFitted`, `radioRouted`; the top-level `firmwareMismatch` field, which nothing consumed and no firmware sends, is gone. `SYNC_BENCH` on a real profile is one blocking `SyncBenchResponse` of per-camera edge counts (`pulses` 1–200, `gapMs` 20–1000, `poll`; `INVALID_ARGUMENT` / `BUSY`; an unwired camera is `watched: false`, not a refusal) and leaves the network/roll gate; the async-job form survives on `d4-sim-full` only. `CAMERA_TEST` answers `CAMERA_OFFLINE` / `SENSOR_NOT_DETECTED`; `DELETE_RECIPE` of an unknown id is `NOT_FOUND`; `SOUND_END` checks the WAV header (16 kHz mono 16-bit PCM, `BAD_FORMAT` with `wav_probe`'s reason); `GET_RUNTIME_STATS` adds `ui{}` and `protocol.droppedTxFrames` and nulls the die temperature of a node that is not answering; `GET_CAMERA_INFO` / `CAMERA_STATUS` carry `viewfinder{frames,fpsX10,drops}` and `CAMERA_LINK_STATS` `viewfinderFpsX10`; `NETWORK_LIST {scan:true}` and `NETWORK_STATUS {probe:true}` answer the D3 blocks; `ROLL_STATUS` carries `serverState`, `ROLL_CREATE` answers the full `RollView` beside the flat five fields Studio reads; `UPLOAD_QUEUE_STATUS` returns all nine fields; `STORAGE_BENCH` clamps instead of refusing, accepts `sizeKB`, takes the capture lock and returns `ok`/`failedPhase`/`passes`/`totalMs`/`cleanupOk`/`sustained{}`/`small{}`; `GET_HW_VALIDATION` mirrors all 56 rows of `hwv_item_t` (wire ids without the enum's `HWV_` prefix, `FLASH_EN_GPIO28` permanently unvalidated); `SET_CONFIG` validates `network.apiBase` per README D3; `MEDIA_INFO` omits digests and `meta` by default (`mediaInfoAsShipped` now starts ON; switching it off opts a suite into digests). A latent `hex8()` sign bug that could emit a negative "CRC" is fixed on the way.
- `packages/test-fixtures/src/firmwareProfiles.ts`: `d4-settings-0-4-9` (0.4.9..0.4.56) uses a new `SETTINGS_0_4_9_COMMANDS` whitelist — 0.4.8's surface plus `SYNC_BENCH` (0x46, dispatched since 0.4.31) and `STORAGE_BENCH` (0x4c, whose handler has been in the tree since firmware/VERSION 0.3.0) — so the profile stops refusing two commands today's dispatcher answers. `PROFILE_FOR_VERSION` gains `0.4.56`. Real profiles no longer state a `syncBench: false` flag the firmware never emits.
- `@kino/kdp` types: `PowerStatus` gains optional `idleSeconds`/`displayOn`/`cameraBankPowered`; `StorageBenchResult` gains the firmware's additive fields and a `StorageBenchPass` type; `KinoConfig` gains `network?: { apiBase?: string }`.
- `@kino/schemas` `kino.device-config` mirrors the wire envelope — `config.mode` plus passthrough `wiggle`/`quad`/`shoot`/`body` and optional `roll`/`network` — instead of a top-level `resolution` and string `flash` that matched nothing a camera sends.
- `firmware-contract/commands.md`: `SET_RECIPE` row carries `cam` and `{ok,id,cam}`; `DELETE_RECIPE`, `SOUND_END`, `NETWORK_LIST`, `NETWORK_STATUS`, `ROLL_CREATE` rows and the `RuntimeStats` example match the firmware; the `CAMERA_TEST` "known drift" note and the `GET_CAPABILITIES` `firmwareMismatch`/`syncBench` notes are updated. README D15's "five targets" clause is retired (the reference device reports six). `docs/studio-spec-audit.md` D-2 records that 02 §30 time sync is met through `HELLO` `hostEpochMs`/`hostUtcOffsetMin` + `clockSource` (D14), no `SET_TIME` needed.
- **The Twin's cameras sit on the body's lens cells, and the external light is on its stud.** The XIAO envelope was drawn landscape with its lens at the board centre; in the field body the board stands portrait (17.8 across, 21 tall) with the camera 6.95 mm below its centre, so every barrel missed its cell by 7 mm and the virtual sensors photographed from the board centre. The proxy now draws the lens at the instance's optical offset, `sensorPoses()` puts each eye at the lens, and the profile's envelope is the portrait one. The 1/4-20 light stud (measured: 25 mm, 18 mm base) is placed from the CAD datums and carries an external constant light whose envelope is PROVISIONAL until the unit is measured; it is now the flash emitter the virtual sensors see, with a TOP LIGHT mode in STAGE - OFF, FLASH (fires for a capture the device asks a flash for), CONSTANT (on for every preview and capture, the video light that is fitted).
- `kino.hardware-profile` instances gain an optional `opticalCenterConfidence` (a source kind: OFFICIAL_CAD, MEASURED, ...). Schema version unchanged: absent means what it meant before, PROVISIONAL. Studio's bench worksheet keys "measure the optical centres" on it instead of on the offsets being zero, because the CAD-derived offsets are non-zero and still not a measurement.
- **KINO Twin is the field body.** The 3D twin used to be a provisional 126 x 80 x 36 acrylic-and-skeleton box with the boards guessed inside it; the released camera body (ECN-0004) is 131 x 90 x 65.5 with two chassis halves, a face shell, a sliding lens cover and a rear door. The CAD generator now writes those six shells assembled (`hardware/cad/KINO_FIELD_BODY/twin/`) and a datum manifest, the Twin loads the shells as Tier A meshes straight from the release folder, and `npm run twin:body:bake` places the P4 module, the four camera stations, the hub board and the shutter switch from the datums into `d4-v1.json` - the profile is derived from the CAD, not typed. Two consequences worth reading twice: camera 1 is now at +X (the CAD puts the shutter at high body-X, the photographer's right, so camera 1 is on the photographer's left), and the battery, fuse, BMS, SW6106, speaker and bulk capacitor are no longer placed - the field body is a USB-C powered fixture with no bay for them; they stay in the BOM and the power model. The LENS view reads camera 2's lens from the profile instead of a constant.
- **KINO Twin's screen is the camera's own firmware.** The rear display used to be a TypeScript sketch of the 0.4.0 viewfinder; the camera had since grown a launcher, SHOOT, LOOK, GALLERY, PHOTO, ROLL, SETTINGS and POWER, and a Twin next to a camera showed a different product. `firmware/p4/twin_ui` now compiles the P4's `ui.c` to WebAssembly the way `host_preview` compiles it natively - the file is #included, nothing in it knows - and the Twin steps its real loop, feeds it the simulated device's state, hands it the touch point and plays back every frame it presents at the virtual time it was presented. Tap the glass in the 3D view or the SCREEN panel; a setting written on the body reaches the mock's config store, the shutter tile runs the capture pipeline, DELETE deletes, FAVOURITE stars, RESTART reboots. The committed `kino-ui.wasm` carries placeholder menu glyphs, because the menu's Windows 98 artwork is Microsoft's (#134, `THIRD_PARTY_NOTICES.md`); `npm run twin:ui:bake -- --w98` builds the private variant with the real artwork into `apps/twin/public/` (gitignored), which the Twin prefers when present. One firmware refactor made this possible and is behaviour-preserving: `ui_task()`'s loop body is `ui_pass()`, returning the sleep it used to take, and the task is `for (;;) vTaskDelay(ui_pass())`. All 59 `host_preview` renders are byte-identical before and after.
- **The Twin's current-firmware profiles report the current firmware.** `d4-settings-0-4-9` covers every release from 0.4.9 to today's `firmware/VERSION` and was pinned to report `0.4.9`; it and the SIMULATED FUTURE profile now report `0.4.56`, which is what a body flashed with today's build answers. Bump alongside `firmware/VERSION`.
- **The nine-track audit, and what it fixed.** A read-only sweep of the whole platform - API, worker, both web apps, the P4 and camera-node firmware, the KDP core, the contracts and the CI gates - found 84 items; this is the determinate half of them. The one that had been losing data since the first photograph: the camera wrote its active look into `kino.capture` as `recipeIds` while the schema and the API read `look`, and because that document is a passthrough schema it parsed clean every time, so `captures.look` was NULL for every capture ever taken and the guest app printed "KINO standard" over a photograph shot with `party-neg`. `firmware-contract/README.md` (D1) predicted it in those words - "a data-loss bug that validation cannot catch for you" - and the rule was crossed anyway; the document now carries both, because `recipeIds` is what the body's own MEDIA_LIST reads. Two more that could lose a photograph: a capture could be marked `ready` while an asset was still pending, because the recompute read assets and jobs on two connections and wrote unguarded, and `ready` is settled so convergence never revisited it - both reads and the write are one transaction now and the write carries the status it decided on in its WHERE clause; and a job's `queued` row was never retired on success, so the partial unique index that makes a second capture-complete a no-op also blocked every re-render, which meant a frame arriving after `complete` was baked out of the photograph permanently and both documented recovery paths were silent no-ops. Two security fixes: the host-log buffer clamped its copy length against the destination and never against the 192-byte source, so any ESP-Hosted line over 191 characters copied up to 5.9 kB of adjacent stack into a buffer `GET_LOGS` serves; and the production TLS switch the source told you to set did not exist in any build file, so every image accepted an `http://` API base out of NVS and put the device token on the air - it is plumbed now and fails secure, an image has to ask for cleartext with `-DKINO_ALLOW_HTTP_API_BASE=1` and CMake says so while it builds. The host dashboard's thumbnails could not authenticate at all: tile URLs were built as bare strings with no bearer, so every hidden or trashed tile 404'd and every tile on a PIN roll 401'd - the exact photographs the moderation grid exists to show. Upload completion no longer holds a row lock across two S3 round trips and a full sha256 re-stream on a ten-connection pool; the API has an error handler, so record ids stop reaching clients in 500 bodies and every error answers one envelope; Redis roll streams expire; the render endpoint and four unmetered host routes have their own budgets. Firmware: a camera abandoned at its capture timeout is released rather than left holding a frame mid-encode, which is the state that made a camera read offline on the next capture; the KDP decoder stops memmoving its whole buffer onto itself on every partial read, which was 8.5x amplification and ~5 MB of PSRAM traffic per four-camera set generated inside the window the validation record blames for tail loss; it counts one resync per discarded frame the way the normative decoder does, so every link-health figure in the bench record stops reading 2x pessimistic; the CRC table is `static const` rather than lazily built behind an unsynchronised flag; and the card-yield mismatch that charged a correct yield to the shutter as a failed upload attempt is gone, replaced by an out-parameter rather than two string literals that had to agree. Records: `infra/.env.staging.example` was missing the provisioning token that `docker-compose.prod.yml` refuses to start without, so the documented staging bring-up ended in a hard refusal; the device upload limit was documented as 60/minute in four places against a real 120/minute per route - 24 captures a minute, not four - and two reference load runs were sized off the wrong figure; the contract called two closed enums free strings, so a document its own prose endorsed would throw at parse time; and the host guide marked six shipped features "unverified", including restore, which told a host a recoverable photograph was gone. `version:check` now refuses a version that goes backwards, checks the migration journal for a file per entry and contiguous indices, and derives its workspace set from the root `package.json` so a new workspace cannot be silently unchecked - the monotonicity gate caught a real regression on its first run. 1,182 tests green across six workspaces (api 348, studio 301, roll-web 224, worker 132, schemas 95, kdp 82) and 6,246 firmware host checks across fifteen suites. Held for the bench, because they change measured-good behaviour on evidence the audit itself called insufficient: the L2 cache split, the per-channel UART receive ring size, the UHCI DMA migration, execute-in-place from PSRAM, the chunk-size raise, task stacks in PSRAM, and the UI redraw rework.

- **Firmware 0.4.56: three replies stop describing a one-camera body.** (1) `GET_DEVICE_INFO` filled `cameraFirmware[1..3]` and `sensors[1..3]` with `""` no matter what was wired - a constant from before CAM2-4 had links, the same defect `GET_CAMERA_INFO` shed earlier - so a body with four nodes answered one here and four there. All four channels now report the way `build_camera_info()` does: the node's firmware string and sensor name when the sweep has it online, `""` when it is offline or reports no sensor. (2) `GET_RUNTIME_STATS` `tempC.cams` was cam1 followed by three hardcoded nulls, and cam1's figure was whatever the last bench capture happened to leave behind, because a node's die temperature travels in its STATUS reply and nothing asked for one. The probe sweep in `main.c` now follows each answered HELLO with one STATUS (`camlink_ping_ch`) in its own scheduler window, so every online channel carries a reading refreshed every sweep; null for a channel that is offline or whose node reported none, never a number that was not measured. (3) `SOUND_BEGIN` snprintf-truncated `name` into its 33-byte field, so a 40-character name came back from the next `GET_SOUNDS` as a clip the host never uploaded - the truncation `firmware-contract/README.md` D18 forbids. A name the host sends must be 1..32 characters or the reply is `BAD_NAME` "name must be 1..32 characters"; a name the host omits is still derived from the id. Compile-verified on `espressif/idf:v5.5.1` (P4: 0xe69f0 bytes, 70% of the app partition free), host suites and the renderer green, `kino-ui.wasm` re-baked so `kui_version` reports 0.4.56. `firmware/FIRMWARE_ROADMAP.md` §13 no longer says 31 of 71 commands are implemented: it is 56 of 72, and D17 is the live count.

- **Firmware 0.4.53: the camera can see its own lens cover, and says so without acting on it yet.** The field body has a Hall switch and a magnet in the cover (hardware 0.1.4), but fitting the switch is a permanent step before the face shell is glued on, and no firmware read it. The cameras can answer the same question: closed, all four lenses face an opaque plate 0.2 mm away; open, all four see the room. The viewfinder already decodes a frame per camera for its tiles, so the watcher reads a mean off those - a strided pass over memory, no capture, no UART, no second JPEG decoder - and folds it into a state machine in `pure.c` that is tested on the host (`pure_lid_update`, 17 new checks). Three rules, each because the naive version is wrong: every camera that answered must agree, so one dark lens is a thumb and not the cover; there is a band between dark and lit where nobody has an opinion, so the state cannot chatter at dusk; and a candidate must hold before it wins, 1500 ms to close against 300 ms to open, because sleeping by mistake costs the shot. **It observes and logs and changes nothing.** The two luminance thresholds cannot be derived - the node runs auto-exposure, so a covered sensor reports amplified noise rather than zero, and how much is a property of the part - so they are `MEASURE_REQUIRED`, the watcher prints the four readings every four seconds for somebody to read at the bench, and `lid_set_acting(true)` is the one line that arms it afterwards. What it will never do is wake the camera: `camIdleTimeoutS` cuts the camera rail, a node needs one to two seconds to boot its sensor, and keeping one alive to watch for light would cost about 100 mA, roughly 30 h of standby off the 3000 mAh cell. Waking stays with touch, the shutter and the Hall switch. It also cannot tell a closed cover from a dark room, which is benign - if all four lenses see nothing there is nothing to photograph - and is why this drives idle behaviour and not a shutter interlock. `power_sleep_now()` ages the idle clock rather than switching the panel off, so the transition still goes through the one path that knows about the backlight and about a finger landing in the same pass. Issue #169.

- **Firmware 0.4.52: the camera tells the host it is there, and stops hammering an upload the server has forgotten.** Two findings from the pre-deployment audit. The dashboard could not tell a camera that was offline from a room where nobody was shooting: the camera knows, and showed it on its own ROLL screen, but nothing reached the server. It now posts `{pending, uploading, failed, serverState, firmware}` to `POST /api/device/rolls/:rollId/heartbeat` every 45 s while a roll is joined and the link is up, plus once within a second or two of the queue emptying or the server state changing, never more often than every 10 s. `pending` is the same sum the body's own screen calls waiting, so the two can never disagree. It runs on the upload worker at a step boundary rather than in its own task, because a second TLS session needs its own stack out of the internal SRAM this build is short of (#162); the cost, stated in the code, is that a heartbeat can sit between two upload steps for one request timeout. It never blocks a shutter, never takes the card, never halts the queue, and a 401 or 403 is dropped rather than acted on. Second: `409 UPLOAD_NOT_OPEN` was classified as transient with every other 409, so a session the server considered gone burned all twelve attempts on a dead upload id at 1 s doubling to a 30 s cap and then parked, when going back to `init` would have succeeded at once. The classifier now reads the error code out of the body and gives that one case an immediate re-init, bounded by the same `RQ_MAX_REREADS` (2) the re-read disposition uses, so a pathological server still cannot loop the camera. `409 UPLOAD_IN_PROGRESS` and `409 ROLL_CLOSED` keep their old behaviour. The server half of the same fault - a multipart swept after 24 hours - now re-opens instead of answering 500, so the two changes meet in the middle. Host suites green, roll-queue 1425 to 1478 checks.

- **Firmware 0.4.50: the gallery and the photograph screen give the panel to the pictures.** Operator on the body: "the image showing is horrid, it's so small and there is so much wasted space". Measured on the host renders: the gallery drew 3x2 tiles of 208x156 under a 62 px header, over a 40 px footer, each with a 32 px caption strip; the photograph well was 464x348, and because the PPA scales in sixteenths a 1600x1200 frame actually showed at 400x300 inside it, matted. Now: gallery tiles 252x189 (a 288x224 THUMB.JPG lands at 13/16 = 234x182), the frame mark and the mode word on a 20 px dark strip along the tile's foot, the star where it was, PREV/NEXT as two header buttons at the right end of the bar with the count beside them, no footer; the photograph well is 600x450 at the right - an exact 6/16 of the frame, no mat, a quad's quadrants exactly 300x225 - with BACK, the caption lines and DELETE / FAVOURITE / SEND TO ROLL stacked in a column on the left. Frame buffers 4 x 540 KB of PSRAM (was 4 x 323 KB; 27 MB free). Touch targets follow the drawing: tiles are the tile, paging is the full header band split between the two buttons. Rendered on the host before the flash (`firmware/p4/host_preview`: gallery, gallery_focus_pressed, photo, photo_quad, photo_partial, photo_delete_confirm, photo_wiggle_playing).

- **KINO Roll web + backend: the phone test, 2026-09-06.** Four defects from the first real phone session on a 1,900-capture roll and one from the desktop. (1) The capture page on a phone drew a quad as four ~150 px thumbnails in the top-left corner with the look floating beside them: the hero was a row flexbox and the grid, the look and the chip were three of its items. One child now, a column, the quad edge to edge at each frame's own ratio from the asset row, the look a caption row under it; landscape and desktop cap the block by height. (2) "Image quality is horrid": the hero could fall back to the 480 px feed tile and the feed tile itself was the camera's 288 px THUMB.JPG, because a device-uploaded `thumb` suppressed the worker's `generate-thumbnail`. The hero now uses `enhanced-still` → `kino-still` → the middle original and never a thumb; the worker always makes the thumb (720 px, q82, was 480 px q70) and upserts it over the device's placeholder row; the feed tile carries `srcset` so a 3× phone takes the 1280 px still. (3) Order: the feed and its cursor page on `(captured_at, id)` instead of upload time, so a camera that spent an evening offline no longer lands two hundred old shots on top of the party; live arrivals are filed by shutter time too. New index `captures_roll_captured`, migration 0012. (4) A way to clear the roll: `POST /api/host/rolls/:rollId/clear` trashes every live capture in one statement (same `deleted_at` the single delete sets, same 7-day purge), writes one audit row per capture plus `roll.cleared`, emits ONE `roll.cleared` event; the dashboard's Danger panel requires the roll code typed back. (5) On a desktop the whole feed sat in the left third of the page at 159 px a tile: a stale `.k-stream` grid rule from before the virtualised rows made each row one cell of a three-column grid. Removed; a fourth column at 1600 px. Tests: roll-web 145, API 292, worker 102.

- **Firmware 0.4.49: the ROLL column spaced over its height.** Operator note on the panel: too tight at the top, empty at the bottom. Name, code, lamp word, count and the state lines now sit with 26 to 30 px between blocks instead of 4 to 18, rendered on the host first. Nothing else changes.

- **Firmware 0.4.48: the ROLL screen speaks to a guest.** Same composition - QR left, the roll on the right - with the address a guest can type under the symbol (`kino.acronym.sk/r/CODE`, the scheme dropped), the name, the code, one lamp word for the connection (ONLINE, OFFLINE, KINO NOT ANSWERING for Wi-Fi up and the server silent, UPLOAD PAUSED for a credential fault), one big truthful count (`127 PHOTOS`, from the index in RAM), and then only what matters: caught up reads "All uploaded / Last upload 8s ago"; work in flight reads "3 waiting to upload", a bar that exists only while there is work, "Uploading now"; work that cannot leave reads "3 waiting to upload / Saved safely on camera" and one line saying when it will go; a card not yet counted reads COUNTING THE CARD and nothing else. No queue internals, no HTTP codes, no "0 waiting", no empty bar. The upload queue now reports when the last upload landed and how many landed in the current burst, which is the bar's numerator. Six states rendered on the host before the flash (`firmware/p4/host_preview`: roll_active, roll_uploading, roll_offline, roll_counting, roll_server_quiet, roll_paused). Bench image a75ca108 built from HEAD; an earlier a069d590 labelled 0.4.48 carried the 0.4.47 sources because the build overlay list had CR line endings, caught by the operator on the panel and fixed in the build script.

- **Firmware 0.4.47: the gallery verifies its index against the card at boot.** Both panicked captures of 2026-09-05 were on the card and uploaded by the queue, yet missing from the Photos count until the gallery screen happened to open. The edit meant to fix that was written for 0.4.45 and never reached the file (the record said it had; corrected). 0.4.47 sets the verify flag before the gallery task starts: one index read and one directory count per boot, and a `klog` line either way. `firmware/HARDWARE_VALIDATION.md`.

- **Firmware 0.4.46: the gallery says in the KDP log what it does to its index.** Rebuild reasons (no header, count mismatch, an indexed capture missing from the card), a rename that failed, a write given up after its retries, and one "order index verified: N captures" line per boot were `ESP_LOGW` on a console that has been UART0 since 0.4.43; they are `klog` lines now, readable over GET_LOGS. Bench the same night, on 0.4.45: three shutter presses without a panic (capture task 4,048 B minimum free on the new 10 KB stack, so the path needs about 6.2 KB), one photograph deleted through the gallery with the index, MEDIA_LIST total and the queue all agreeing and the deletion surviving a reboot - SINGLE DELETE PASS. `firmware/HARDWARE_VALIDATION.md`.

- **Firmware 0.4.45: the shutter panicked the body, and the bench had never pressed it.** The first physical shutter press after the Delete All test panicked the P4, and so did the second; the two coredumps are identical to the byte: capture task, `sp` 48 bytes below its stack base, inside newlib `sprintf` under `cJSON_PrintUnformatted` under `upload_store_save` under `upload_queue_enqueue_slots` in `on_capture_done()`. The trap cause, read from the stored dump by the new boot line, is `mcause 0x1b`, the RISC-V stack protection fault. The capture task had 6 KB; the shutter runs the whole capture and the upload-record encode on that task, while KDP's `CAMERA_CAPTURE` runs `capture_fire()` on the 12 KB kdp_server task - so a hundred host-driven captures a day never touched the product path, and the task monitor truthfully read 5.7 KB free for a task that never ran the deep chain. Two changes: the capture task stack is 10 KB (4 KB of internal RAM, budget 93 KB free at boot), and `main.c` logs the last panic from the coredump partition at every boot (`last panic: task capture pc 0x400edeaa mcause 0x1b mtval 0 ...`) so a crash on a body whose console is UART0 explains itself over GET_LOGS. No photograph was lost: both crashes hit after META.JSON was committed, the queue's reconciliation found the folders and uploaded them (four originals each). The 0.4.44 dialog rewording ships in the same image. Lesson written into the validation log: a soak that fires the shutter over KDP does not exercise the shutter.

- **Firmware 0.4.44: the restart and shut-down dialogs speak to the owner.** "Back in a moment? KINO restarts and comes back to SHOOT." and "Calling it a night? Hold the power slide to wake KINO up again." replace the two contract-flavoured questions. Nothing else changes. Delete All proven on the 1,540-capture card the same evening: 1540 removed in 16 min, total 0 before and after reboot, no dangling upload job, SD errors 0. `firmware/HARDWARE_VALIDATION.md`.

- **Release closure, 2026-09-05.** C6 reset resume re-proven on 0.4.43 (two cycles, +17 s and +18 s to IP_READY, same P4 session, capture during recovery, pending upload drained by itself); the recovery reserve is not re-armed after 3.5 h of uptime (largest DMA block 15 KB) and is flagged as a #162 watch item, not redesigned. The host preview renderer builds again (its `upload_queue_enqueue` stub had drifted) and its renders of the 0.4.43 ROLL and STORAGE screens were checked. `infra/README.md` gains a pre-deploy checklist and states plainly that `deploy.ps1 backup` is PostgreSQL only and that no object-storage backup runs on the Windows host until `backup.sh` is scheduled. Operator decision recorded: the Windows 98 icons stay for private builds (#134). Remaining gates are operator gates: destructive delete tests on an expendable card, the panel glance, mobile PWA, production deployment and its E2E. `firmware/HARDWARE_VALIDATION.md`.

- **Firmware 0.4.43: the queue tells the truth on the body, forgets what a single delete removed, resumes by itself after an outage, and stops writing log text into KDP replies.** Six defects from the software-completion audit. (1) Single delete (KDP `MEDIA_DELETE` and the gallery dialog) now calls `upload_queue_forget()` before unlinking, the order Delete All already used; a deleted capture with a live job used to re-read a missing asset to the retry cap and park FAILED until reboot. (2) The ROLL screen counts the index (`gallery_media_count()`) instead of the capped list, shows WAITING as window plus card (`pending + cardPending`) and says "still counting" until the card has been seen end to end, shows parked jobs as a line of their own, and labels the since-boot upload counter as such; the delete dialogs use the same exact count. (3) A job parked by a run of network failures is a different thing from one the server refused: `rq_park_is_transient()` tells them apart from persisted fields, and the queue revives the transient kind when the link comes back, when the server answers again, on the first complete card pass after boot, and by a ten-minute probe while the link is up with everything parked - measured: 105 captures parked by an evening with the API down drained to 0 by themselves after the flash, 3 to 4 uploads per 20 s in waves of 32, no button pressed. Parks the server refused still wait for `UPLOAD_QUEUE_RETRY`. (4) `ROLL_STATUS.serverReachable` was the radio state; it now also requires the last HTTP exchange to have been answered, and `serverState` (offline / unknown / reachable / unreachable) says whether the server has been asked since boot; the ROLL screen shows SERVER NOT ANSWERING when Wi-Fi is up and the API is not. (5) `META.JSON` is written to `META.TMP` and renamed, so the commit marker exists whole or not at all; `META.TMP` joins the capture file list. (6) The console was also on USB-Serial-JTAG, the endpoint KDP owns, so every `ESP_LOG` line landed between and inside framed replies - the open reply-loss item. `CONFIG_ESP_CONSOLE_SECONDARY_NONE` puts the console on UART0 only: the host decoder counted 92 discarded bytes on a single 0.4.42 call and **0 discarded bytes and 0 resyncs over 39 calls** on 0.4.43. Also: the shutter banner shows the human reason ("Needs 4096 KB, 812 KB free") instead of the contract code; the Free space row appends LOW under 512 MB (about 650 captures); the Format card row is dimmed and no longer opens a confirm dialog for a feature that does not exist; the gallery task stack goes 4 KB to 5 KB after measuring 1,248 B minimum free during the drain (4,564 B free at boot on 5 KB; internal RAM 93 KB free, reserve held). roll-queue host suite 1,407 -> 1,425. `firmware/HARDWARE_VALIDATION.md`.

- **KINO Roll web: a capture is shown by camera, not by position, and a guest who installs the app lands somewhere.** Frame labels, the frame strip and the playhead now read `frameIndex` (the 1-based camera number) so a sparse capture from cameras 1, 3 and 4 says CAM 1 / CAM 3 / CAM 4 and matches the `cam-NN` download names; the feed's four bars are one per camera slot with the missing one dark, and `capture.status` is finally read: `partial` shows "3 OF 4", `failed` shows FAILED and mounts no player. A landing page at `/` takes a six-character Roll code (normalised to the slug alphabet) and remembers the last Roll; the PWA `start_url` points at it instead of a 404. An error boundary wraps the routes, feed pages drop a malformed capture instead of blanking the gallery, every image has an "Image unavailable" fallback with a one-shot retry on the detail page, the grid no longer preloads full-resolution originals before the bake lands, `WigglePlayer` takes the capture's `direction` (the saved file and the live player could run opposite ways) and renders one frame as a still instead of throwing, the display page passes fps/loop/direction like the other callers, a load failure shows "Could not reach the roll" with Retry and an offline banner follows `navigator.onLine`, and an archived Roll says so. roll-web tests 94 -> 127.

- **KINO Roll backend: statuses that settle, jobs that end, and a sweeper for the row that pinned a capture in `processing`.** A render enqueued lazily from the guest or host routes now recomputes the capture status (the trap `uploads.ts` warned about and both sites fell into); a worker job whose capture row is gone is dropped with one warning instead of failing to record its own failure five times (the dev log showed exactly this for `render-social-formats`); the gallery still is the lower-median stored frame by camera number, so a sparse capture no longer switches viewpoint; a five-minute sweeper re-submits `queued` processing rows with no live BullMQ job (audit API-14, the "stuck in processing" cause when a submit failed after the row committed); device status and current-roll reads carry a 120/min limit; the guest Roll read gets the same ten-misses-then-lock treatment as device join, so a 29.7-bit slug is not an open oracle; incomplete multipart uploads are expired by MinIO's own stale-upload sweep (24 h / 6 h, pinned in both compose files - the pinned release rejects an abort-only lifecycle rule, verified). worker tests 90 -> 102; api 289 tests, of which five in `uploads.test.ts` fail only while a dev worker is consuming the shared queue.

- **V2 hardware accessibility audit: FSIN is not on the Sense board, PWDN and RESET are strapped through 10 k and reachable only at fine pitch or through an FPC adapter.** Documentation only; nothing driven, written or soldered. From the official Seeed v1.0 expansion schematic: the 24-pin camera connector carries RESET on pin 6 (10 k to 3.3 V) and PWDN on pin 8 (10 k to GND), neither crossing the board-to-board connector, which is the physical reason for `pin_pwdn = -1` / `pin_reset = -1`; no FSIN or STROBE net exists, and pin 1 is the only floating pad a module could carry FSIN on. The OV3660 datasheet gives FSIN register bits (0x3835, 0x3836, 0x3016[2], 0x3019[2]) and no timing semantics; its v1.2 revision removed the frame-synchronisation feature claim. Hardware standby via PWDN is documented to reset counters and keep registers; 3.3 V drive on the 2.8 V-domain pads is inside the VDD-IO + 1 V absolute maximum. Classification PATH F (module-side FSIN routing unknown); next gate ACCESS-1, a 24-pin FPC pass-through adapter on one camera to measure pins 1, 6, 8 idle. `firmware/SYNC_HARDWARE_ACCESS.md` (#165).

- **Firmware 0.4.42: Delete All removes exactly what the index holds, and tells the upload queue first.** The readdir wipe (`wipe_pass`, its sizing count and `WIPE_YIELDED`) is gone; `wipe_step()` walks the index list in RAM - the names the Photos row counts and `MEDIA_LIST` pages - 32 per turn under one card acquire, yielding to a shutter between items. Each item calls `upload_queue_forget()` before `storage_capture_delete()`: `rq_forget_action()` is a pure rule (absent: nothing; the job in flight: drop after its step returns, do not persist; otherwise drop now) and every branch re-arms the card-changed cursor, so a deleted capture can no longer retry a missing asset to the cap and park FAILED. When the last indexed item is gone the empty list is published, `INDEX.TXT` is written at once, and a count pass looks for folders the index never held; any remaining owe a rebuild and a second Delete All. Local card only; nothing on the remote Roll is touched. A META.JSON stat gate on the walk was tried and withdrawn: measured on the 1,435-folder card it held the card 3.5 minutes after a rebuilding boot (a stat is a linear FAT lookup, paid per folder per pass); without it the same sequence holds about one minute, as 0.4.41 did. Host: roll-queue 1,393 -> 1,407, gallery-index 173 -> 188, fourteen suites green. Bench: captures 3,567 and 3,867 ms, `MEDIA_LIST` 2.7-4.3 s, index total 1436 -> 1437 as the write landed. **DELETE ALL CODE READY - DESTRUCTIVE TEST PENDING**: the 1,434-photograph card was not wiped and no Delete All was issued. `firmware/HARDWARE_VALIDATION.md`.

- **Firmware 0.4.41: MEDIA_LIST and the Photos row answer from the persisted order index, not from walking the card.** `MEDIA_LIST` took 81.7 s on the bench card because `media_scan` opened META.JSON in every one of 1,325 directories to sort by time, and every client timed out - one of the ways a card of photographs reached a UI as zero. It now reads `INDEX.TXT` in one fread and pages it with `gidx_page()`, a pure and host-tested function that counts every valid line and copies only the requested window, so `total` is the exact number of photographs on the card and memory is bounded by the page. Measured on 1,434 photographs: **4.3 s** with the queue idle, 13.7 s with the upload queue holding the card, 1.26 s for the last page, `total=1434` counted rather than the 1325 directory entries it used to report, summaries `complete/4`, pagination correct end to end. The Photos row reads `gallery_media_count()` - index state already in RAM - so the draw path touches no card and the 0.4.39 regression cannot recur from there. The index cap goes 240 -> 4096 with its arrays in PSRAM, and an index that is honest but shorter than the card (written by a lower-capped firmware) now triggers a rebuild; without that the card read `total=240` after the upgrade. The exhaustive walk is kept as the recovery path for a missing or unreadable index, and a header that disagrees with its body is reported as `indexStale` and rebuilt rather than trusted. Grouped capture during the proof: 4,901 ms, no regression. gallery-index host suite 78 -> 173 checks. LOCAL MEDIA COUNT PASS - the operator confirmed the Storage screen reads **1434** with Delete All enabled; MEDIA_LIST PASS; Delete All not reached and nothing destructive run. `firmware/HARDWARE_VALIDATION.md`.

- **Firmware 0.4.40: the photograph count must never make the camera wait, and the finder-idle stale frame is gone.** Two things. (1) 0.4.39's `storage_media_count_cached()` recomputed from the storage screen's DRAW path, and every shutter invalidates it, so on a 1,325-directory card the count walked the card on every redraw while holding the card lock: grouped captures went from 4 s to **75 s** and the KDP link answered in 9 s. It now takes the lock with a 0 ms timeout and returns the previous answer rather than waiting, abandons the walk the moment `storage_yield_requested()` says a capture wants the card, and recounts at most once every five seconds. Measured after: 26 captures in two minutes, about 4.6 s each. (2) The finder-idle half of the 0.4.38 fresh-frame proof, which was owed: with the body off the SHOOT screen and the pump verified stopped, `frameBeforeEdge` is **0 of 423** where 0.4.31 measured **385 of 393 (98%)**, and the node's `fb_get` sits at exactly one frame period (112 ms) where it used to return in 56 ms - the predicate firing, at the cost predicted. 100 of 100 accepted, 99 four-frame sets, zero shortRead, no CRC, no node reset, P4 internal 86 KB free / 65 KB minimum. **FRESH FRAME PREDICATE PASS.** And one thing nobody was looking for: reflashing the P4 cuts node power, so all four nodes restarted their sensors within milliseconds of each other, and the frame-start spread across a set collapsed to a **median 4.9 ms** - stable over 100 shutters, same camera ordering every time, shrinking 0.9 ppm which agrees with the 1.6 ppm measured independently. Every earlier spread figure in this project (42 ms, 85.9 ms) was measured with sensors that had started at different times, so those numbers describe unaligned start rather than the architecture. It is an accidental demonstration of the V2 phase-alignment concept and not a solution: this is frame start, not exposure, 4.9 ms is still twice `gradeSkew()`'s usable threshold, and node boot timing is the crudest alignment mechanism available. V2-C remains unrun. `firmware/HARDWARE_VALIDATION.md`.

- **Firmware 0.4.38: a photograph is armed after the command that asked for it.** camnode only; no KDP, settings or protocol change. The node already released and re-took any frame armed before the last encoding change; that predicate's reference instant now also includes the capture command, so freshness stops depending on whether a UI happens to be draining the node's one-deep preview queue. The sensor streams unconditionally either way - what differed was that with the finder idle `camsensor_discard_queued()` drops the finished frame and the next one to complete was already in flight, armed before the command by up to a frame period; one discard is one frame short of a guarantee. No new state was introduced: the requirement now lives in the one place it can be stated. Bench, 100 grouped captures with the finder live against the 0.4.37 run of the same size: **100 of 100 accepted with 100 four-frame sets** against 99, **zero** frames carrying an error against one, `frameBeforeEdge` **0 of 396** against 0 of 397, and per-set time *down* from 3,994 to 3,257 ms median - the predicate never fires in this condition, which is the intended result. No node reset, node heap unchanged at 7,242 KB, P4 internal 88 KB free with a 28 KB minimum and the reserve 2/2. The finder-idle half of the proof - where 0.4.37 measured 385 of 393 frames starting before their own shutter edge - needs the body off the SHOOT screen, because `ui.c` gates the pump on `s_screen == SCR_SHOOT` and nothing over KDP changes it; that run is still owed. `firmware/HARDWARE_VALIDATION.md`.

- **V2-A: the sensor restart primitive is NOT PROVEN, and the audit found a better mechanism the project did not know about.** No code changed and nothing was written to any sensor. Against the OV3660 datasheet (PRELIMINARY SPECIFICATION v1.3), `0x3008` is confirmed as SYSTEM CTROL0 with bit 7 software reset and bit 6 software power down, default **0x02** - so standby entry and exit are `0x42` and `0x02`, not `0x40` and `0x00`. Software standby is documented to be cheap and to keep configuration ("all register content is maintained"), but it is **not** documented to reset the frame timing counters, and the datasheet attributes counter reset only to *hardware* standby ("the internal device clock is halted and all internal counters are reset"). Re-phasing is the entire purpose of the primitive, so its central property is unsupported and the register was left alone. The same audit corrects a figure this study had been quoting: a software reset needs **~2 ms** settling per the datasheet, and the >200 ms is the esp32-camera driver's own two 100 ms delays plus a full default-register reload. **The finding that matters: the OV3660 has a dedicated frame-sync input.** Pin E3 `FSIN`, an input out of reset (`0x3016` default 0x22 leaves the FSIN-output bit clear), enabled by `0x3835[0]` with polarity in `0x3835[1]`, and drivable as an output via `0x3016[2]` plus `0x3836`'s output width - so master/slave frame sync between sensors is a documented configuration. That is purpose-built for what V2 needs, it keeps configuration, and it takes the SCCB write out of the critical path: the write becomes setup and the already-proven `SYNC_OUT` edge (1000/1000 accepted) carries the timing. The blocking question is now hardware and not firmware - whether FSIN is routed to the camera module's FPC, which the standard 24-pin DVP set does not normally carry and neither this repository's DVP record nor Seeed's published material resolves. Also measured rather than estimated: SCCB runs at **100 kHz** (`CONFIG_SCCB_CLK_FREQ=100000` in the node's resolved sdkconfig, range allows 400 kHz), a 16-bit-addressed four-byte register write is about **380 us**, and each node drives its own I2C controller so four writes are genuinely parallel rather than serialised through the P4. V2-B not run, as it is gated on V2-A. `firmware/SYNC_FEASIBILITY.md`.

- **The sensors were never drifting: they are rate-locked to 1.6 ppm, and that changes the V2 plan.** No code changed. The 69 us frame-period spread this study has carried since 0.4.30 - and the three-minute phase-decay conclusion built on it - was measured in the finder-live condition, where the returned frame is chosen by preview-pump timing rather than by the sensor's phase. Measuring the sensors against each other instead, as the difference of their `syncToFrameUs` against the one shared edge, over the 0.4.31 finder-idle run of 99 shutters and 17.6 minutes: **relative rate 0.4 to 1.6 ppm**, split halves agreeing within 0.4 ppm, an outlier-proof median-of-pairwise-slopes estimator reproducing least squares to two decimals, and a worst residual of 243 us against a 112,387 us period. A one-time phase alignment therefore holds 2 ms for about 21 minutes and 10 ms for about 1.7 hours, not three minutes. So **phase alignment is a one-shot problem, not a control problem** - which is also why every rejected idea here (phase-aware scheduling, static delays, VTS chasing, a shutter-sampled loop) had the wrong shape: they tracked something that does not move. The V2 recommendation is no longer a shared XCLK: it is to align once on the `SYNC_OUT` edge that is already wired and already proven at 1000/1000, by arming all four nodes and letting each node's existing sync ISR trigger the sensor restart, which replaces four independent UART commands (212 us median, 412 us worst) with an ISR and an SCCB write. Shared XCLK becomes necessary only if relative rate rises materially when warm or if the restart proves non-deterministic; on the XIAO Sense it would also mean contending with a module-internal XCLK net, since the 16 MHz clock is LEDC-generated on GPIO10 inside the module and no XCLK input is exposed. Recorded with the OV3660 controls that actually exist in the pinned driver - `SYSTEM_CTROL0` 0x3008 bit 7 software reset, `set_reg`/`get_reg` exposed so a node needs no fork, and **no hardware reset or power-down line at all** (`pin_reset = -1`, `pin_pwdn = -1`) - and with the standby bit marked as inference pending the datasheet. Also documented: **freshness currently depends on the viewfinder**, because one `discard_queued()` is one frame short of a guarantee and the preview pump supplies the missing drain, so 98% of finder-idle photographs predate their own shutter edge; the fix belongs in the node's capture predicate, not in a new product state. `firmware/SYNC_FEASIBILITY.md`, `firmware/CAM3_SWAP_MATRIX.md`. Nothing in the sync path ships.

- **CAM3 measured rather than suspected, and it is not a streaming outlier.** 10 minutes of live viewfinder with no captures: about 8,000 preview frames per camera and **zero** shortRead, oversize, noLink, decode, timeout, retry and CRC on all four, cam3 within 1.5% of cam1 on frame count. Then 100 grouped captures: 100 of 100 accepted, 99 sets of four, and cam3 the worst channel but only mildly - 4 shortReads and 8 timeouts against 0-1 and 2-4 - with no CRC error anywhere, no node reset, and every failure recovered by a retry. The lifetime figure that started the hunt (297 against 53-94) is contaminated by a bench error of ours and is withdrawn: opening the four node USB consoles to watch for a dropout reset three nodes, because the XIAO's native USB CDC re-straps on open, and the resulting link storm is in that count. Root cause **not isolated** - separating the P4 UART channel from the XIAO, the module, the ribbon and the connector needs physical swaps one variable at a time, and `firmware/CAM3_SWAP_MATRIX.md` gives the procedure with one command per configuration. `firmware/HARDWARE_VALIDATION.md`.

- **The viewfinder is the synchronization variable, measured at 100 shutters a side.** No code changed: 0.4.37's sync path is 0.4.31's, on camnode 0.4.31, and this is the run that makes the earlier two comparable. With the finder live, **every one of 397 usable frames started after the shutter's own edge** and the frame-start spread across a set is 42.0 ms median / 102.8 ms p95; with it idle, the command lands in 2.1 ms instead of 17.1 ms but **385 of 393 frames started before the edge** - the driver hands back the buffer it already had, so the photograph predates the shutter - and the spread doubles to 85.9 ms. 0.4.37 finder-live reproduces 0.4.30 finder-live (42.0 against 46.6 ms median, 0% stale in both, 17.1 against 18.6 ms to the command), so the session's queue work did not perturb the sync path and the 0.4.31 figures were a different experiment rather than a regression. Dispatch spread is 212 us median and 412 us max against a 100-400 us target, which is 0.2-0.4% of the 112.4 ms frame period, while sensor phase is 37-91% of it: the dominant term was never the command side. Verdict **SYNC BASELINE MEASURED**, and the `gradeSkew()` exposure bands stay unreachable on free-running OV3660s. `firmware/SYNC_FEASIBILITY.md` now records the architecture as SOFTWARE_DISPATCH_ONLY with the three terms sized, and the design-only conclusion on registers: the four sensors' frame periods differ by 69 us, so any one-time alignment decays by a full period in about three minutes, `VTS` phase-chasing should not be built (its error signal arrives only when the user presses the shutter), and the step that attacks the dominant term is one oscillator feeding four XCLK pins - a V2 hardware decision. Nothing in the sync path ships. `firmware/HARDWARE_VALIDATION.md`.

- **Firmware 0.4.36: the card is the queue, and reconciliation can see all of it.** Three defects, one of which was hiding the other two. (1) `queue_scan()` bounded its work at `SCAN_MAX_DIRS` and restarted from `opendir()` on every pass, so the bound was a *prefix*: with 788 capture directories on the bench card the newest 276 were never examined, by any pass or any boot, and a capture that missed the 32-entry RAM window at the shutter was never uploaded (#167). The pass is now a window with a cursor - `rq_scan_t` in `roll_queue.c`, host-tested - that advances by what it examined and resets when a pass reaches the end, so a cycle of passes covers the card exactly once at a bounded cost per pass; `UPLOAD_QUEUE_STATUS` gains `cardPending` (eligible captures the window has not taken yet, from the last complete cycle) and `scanComplete`, because before this there was no way to tell "the queue is empty" from "the queue cannot see the work". (2) Reaching those directories is what exposed the real reason four-camera captures never uploaded: `upload_store_meta_roll_id()` read `META.JSON` into a private 2048-byte buffer, a four-camera META is 2301 B and a three-camera one is 1916 B, so `fread` filled the buffer, cJSON refused the truncated text, the roll id came back empty, and `rq_reconcile_action()` read that as "META names no Roll" and retired the photograph - a decision that writes nothing to the card and therefore repeats on every boot for ever (#168). One 4 KB bound for both META readers now, and "could not be read" is its own answer (`upload_meta_roll_t`, `RQ_REC_UNREADABLE`) that retires nothing; the regression test carries the card's own 2301-byte document and asserts that the same bytes cut at 2048 are UNREADABLE and never NONE. Live captures were never affected - the shutter path takes the Roll from the capture report, not from META - which is why this only ever bit a capture taken offline or one the window could not admit. (3) A COMPLETE job left the RAM list only after its record was persisted, and that write is refused whenever a capture holds the card, so the job stayed in the list as COMPLETE for ever: `pick_job()` skips it, retry cannot revive it, and the status counted it as `pending` (#166). It is dropped when it settles, at the cost of at most one redundant `complete` after a reboot, which the server is idempotent about. A fourth thing the bench found, and it is the one that matters most: a completed cycle used to be a permanent claim. `cycle_complete` was set by the first pass that reached the end of the card and never cleared, so `rq_scan_more()` answered false and nothing scheduled another cycle - 42 captures taken faster than the queue could drain them left 13 with work on their records while the queue reported `pending=0 cardPending=0 scanComplete=true`. The flag is now about the LAST cycle, every enqueue marks a cycle owed (`rq_scan_card_changed()`, a flag and not an invented count), and the rescan request is taken under the lock so an enqueue during a pass cannot be dropped. The invariant is the one the status line implies: while any capture has work left, either it is in the RAM window or a cycle is owed. The scan also has no silent exits left: every directory it declines is counted and named in one log line - queued, repaired, unadmitted, unreadable, retired (and how many of those were taken off any Roll), unwritten. Bench, KD4-D121BC with 788 capture directories on the card: the region past the old bound reported **166 captures retired for no Roll provenance** on 0.4.35 and **0 on 0.4.36**, and those 166 then uploaded automatically with **0 failures and no manual enqueue** - 32 admitted per pass, the window draining and refilling from the cursor, `cardPending` counting down to 0 and `scanComplete` true at the end. Backend +156 rows for 166 completions, the difference being captures an earlier attempt had already registered and the server being idempotent on `captureUuid`. The capture that started it, `cf9df964`, ended with a row on Roll `roll__Mg6PTKzfodtJ7zxCjBoNA` carrying four originals and a thumb. Internal SRAM 90 KB free, 49 KB minimum, recovery reserve 2/2 throughout, so #162 stays closed. Host suites: roll-queue 1,381 checks, upload-store 158, fourteen suites green. `firmware/HARDWARE_VALIDATION.md`. Nodes, C6, capture, the UART protocol, SYNC and FLASH_EN untouched. Device side only, same profile.

- **Firmware 0.4.31: the sync edge is proven, and the phase model it was meant to enable is not worth building.** Three changes, all measurement. `SYNC_BENCH` (0x46, allocated since the protocol was written and never implemented) fires pulses with no camera attached and holds every node to one accepted edge per pulse, polling all four after each one so a bad pulse is named by index; the node's sync input gains a 10 ms dead time with the **raw** edge count kept beside the accepted one, so one pulse-event is one generation while any ringing stays visible; and sync attribution now expects the number of pulses actually fired since that camera last answered rather than a bare +1, which is what four of the baseline's nine refused frames really were - a camera that sat out one shutter still saw its pulse. Bench: 1000 pulses, **1000/1000 accepted on all four nodes with raw also exactly 1000, zero rejected, timestamps monotonic**, so the fan-out is electrically clean when idle; and 100 grouped shutters with **zero stale-generation in 397 frames** against six before. Per-node frame periods measured 70 us apart (112,353-112,422 us). The replay that had to justify a phase-aware scheduler refused it: predicting phase from the previous shutter gives a median spread of 56.4 ms against 46.6 ms measured, because a camera's phase moves 17-19 ms between shutters and up to half a period; the perfect-knowledge bound is one halving, 46.6 to 20.8 ms median, and needs the phase of the shutter being taken. So no scheduler was written. Priming, measured as 50 interleaved pairs, changes nothing (58,812 against 58,842 us median). What does change it is the live viewfinder, which was also an uncontrolled variable in the 0.4.30 baseline: with the finder running the command lands 18.5 ms after the pulse and every frame is fresh, without it the returned frame usually started before the command and no set has all four cameras on the same boundary. Targets are now written down in `firmware/SYNC_FEASIBILITY.md`, including that `gradeSkew()`'s V1 exposure bands are unreachable on free-running OV3660s. Device side only, same profile.

- **Firmware 0.4.30: the camera nodes see the shared sync edge, so four-camera frame timing can be measured at all.** The P4 has pulsed `SYNC_OUT` (GPIO32, JP1 pin 19) at every grouped shutter since the capture pipeline existed and no node ever read it, which is why `HARDWARE_VALIDATION.md` has carried that pin as UNVALIDATED and the three `kino.capture` skews as null. The node now arms a rising-edge interrupt on `SYNC_IN` (GPIO2, XIAO pad D1) that does two things and nothing else: timestamp with `esp_timer_get_time()` and increment a counter. The capture reply carries `syncSeq`, `syncEdgeUs`, `syncToCmdUs` and `syncToFrameUs = frameStartUs - syncEdgeUs`, all in that node's own clock, and `HELLO`/`STATUS` carry `syncSeq` and `syncInput`; the P4 attributes a reply to the shutter's own pulse by generation rather than by timestamp proximity (`pure_sync_classify`: the node's counter must advance by exactly one since its previous reply and the edge must precede the command within two seconds) and records `syncClass`, `syncSeq`, `syncToFrameUs` and `frameBeforeEdge` per frame in `META.JSON` and the capture reply. `syncToFrameUs` is the one figure comparable between nodes, because each node measures its own frame against the same physical edge with no shared clock; it is frame start against that edge and it is not exposure timing, so the three skews stay null with their reason. Nothing in the sensor path changed: the OV3660 still free-runs, the edge still triggers nothing, and this is the instrument rather than the mechanism. Measurement only, no tuning: KD4-D121BC, four nodes on 0.4.30 and the P4 on 0.4.30, one 28 AWG branch from JP1 pin 19 to pad D1 on each node. 100 idle grouped shutters plus 20 in each of four connected states: frame-start spread against the common edge median 46.6 ms and max 109.7 ms idle, 16-19 ms median back to back, bounded by the frame period this run measured for the first time (112.4 ms at UXGA on a 16 MHz XCLK, +-0.3 ms); command dispatch 161 us median and 443 us worst; 0 stale frames in 397; every four-camera set ordered as a rotation of cam1-cam2-cam3-cam4, so the dominant term is sensor free-running phase and not dispatch. Offline hold and automatic Roll send re-verified with the measurement running. `firmware/HARDWARE_VALIDATION.md`. Contract: `node_link.h`'s CAPTURE reply, `firmware-contract` D24. Device side only, same profile.

- **Firmware 0.4.29: the Roll queue uploads the cameras a capture holds, not a count.** The bench (2026-09-03, CAM2 powered off, `CAP_000263`) stored a truthful three-frame set - `status complete`, `frameCount 3`, frames cam1/cam3/cam4 as C1/C3/C4 - and the queue then asked for C1, C2, C3: the job carried only `frame_count`, `rq_next_step()` handed out 1..N and `roll_api.c` turned each into `C%d.JPG`, so C2 failed "the asset is not on the card" twelve times, the job parked, and C3/C4 never travelled. Only a *trailing* missing camera uploaded, by coincidence. The job now carries the camera list (`frameSlots` in `UPLOAD.JSON`, positional beside `frameDone`), taken from the capture report at the shutter and from META.JSON's `frames` at reconciliation and `UPLOAD_ENQUEUE`; the step's `frame_index` is the camera slot, which is the file name and the wire `frameIndex`; `frame_count` is the list's length and decides nothing. Records from before have no `frameSlots`: the reconciler adopts the list from META (an old `frameDone[i]` meant camera i+1 and is read that way, so a landed frame stays landed and nothing is skipped), rewrites the record, and a parked job keeps waiting for the user's retry with the reason updated. META that names no usable list (count mismatch, a camera twice or outside the body, a file that is not its camera's, a named file missing from the card, malformed) parks the job with the reason rather than guessing; the C<n>.JPG scan is gone. main.c's duplicate capture-done enqueue is removed (upload_queue.c's listener has the shutter's Roll and cameras). Host tests: the sparse matrix (full, each camera missing, two missing, one camera), resume by camera across a reboot, retry without a second registration, legacy adoption, record and META validation - roll-queue 1,236 checks, upload-store 141. Contract: `docs/roll/ROLL_DEVICE_CONTRACT.md` step 3 and `firmware-contract` D23. Bench: KD4-D121BC boot-117 - CAP_000263 revived by `UPLOAD_QUEUE_RETRY` to COMPLETE under the same capture id with originals 1/3/4 (no asset 2), all 23 previously parked jobs uploaded; five full sets 4/4 with 20/20 frames SD = object = DB; CAM2 off -> C1/C3/C4 uploaded and nothing else; CAM3 off -> C1/C2/C4; restore 3/3 sets 4/4; internal minimum 46 KB, reserve 2/2 (`firmware/HARDWARE_VALIDATION.md`). Device side only, same profile.

- **Firmware 0.4.27: internal RAM headroom is real, not reserved.** 0.4.25's early reserve boot-looped KD4-D121BC (2026-09-02, console capture): holding 2 x 15,872 B across ESP-Hosted's first init starved the TX aggregate buffer the bus allocates lazily on its first frame (`eh_host_bus_sdio.c:786`), and on the next boot the `upqueue` task's 8 KB stack, created when the largest free internal block was 2.6 KB, landed in the 8 KB TCM and its first NVS write tripped `cache_utils.c:127` (a TCM stack is internal but not DRAM). The measurement underneath: internal SRAM 9 KB free at boot, 2 KB minimum. A reserve only decides who starves. `net_hosted_reserve_early()` is removed; the reserve is taken after bring-up as on 0.4.6, sized 15,872 B as on 0.4.24, and `recoveryReady` still reports whether it is held. The headroom comes from keeping internal SRAM for what must be internal - ESP-Hosted's SDIO buffers, the four UART rings, task stacks - and moving task-only statics to PSRAM with `EXT_RAM_BSS_ATTR` (`CONFIG_SPIRAM_ALLOW_BSS_SEG_EXTERNAL_MEMORY=y`): cam_link's channel table (36 KB), the klog ring (24 KB), the upload job table (9.7 KB); none is touched from an ISR or with the flash cache off. Bench: no panic, reserve 2/2 with 32 KB still free, internal free 70 KB (minimum 59 KB), three credentialed C6 recovery cycles with the session unchanged and IP_READY back in 8-19 s, camera-link latency unchanged at 4-5 ms, ROLL-C3 PASS (`firmware/HARDWARE_VALIDATION.md`). Nodes, C6, grouped capture, UART protocol, Roll queue and the reserve size untouched. Device side only, same profile.

- **Firmware 0.4.25: the C6 recovery reserve is taken before the heap fragments.** 0.4.24's right-sized reserve (15,872 B, the component's own SW_AGGR probe) only helps when it can be held, and it was taken after bring-up - after the camera links, capture workers, display and UI had allocated. On 0.4.6 that still fitted 2/2; on 0.4.20-0.4.24 the largest free internal-DMA block at that point was 15,872 B or less, the reserve held 1/2 or 0/2, and `C6_RESET_BENCH` panicked the P4 in ESP-Hosted's re-init probe (#162, coredumps 2026-09-02, twice). `net_hosted_reserve_early()` now takes it at the top of `app_main`, right after boot logging, from the first-boot heap; the post-bring-up take is a no-op once held. `recoveryReady` in `GET_RUNTIME_STATS` says whether it worked. Bench result in `firmware/HARDWARE_VALIDATION.md`. Device side only, same profile.

- **Firmware 0.4.24: provisioning from Studio actually provisions.** Re-provisioning KD4-D121BC after an NVS wipe (2026-09-02, the intended path: register with the provisioning token, `SET_CONFIG roll.credentials`, `NETWORK_SET`) found two breaks on that path. (1) The credential never reached the HTTP client: `SET_CONFIG` merged `roll.credentials.{deviceId,deviceToken}` into the config document and nothing read it there - `roll_http` takes its bearer token from `roll_state`'s NVS record, which only the dev-only self-registration ever wrote - so `ROLL_STATUS` stayed `no-credential` with `hasDeviceToken: true` beside it. Now `SET_CONFIG` hands the pair to `roll_state_set_credential()` and blanks `deviceToken` in the stored document (write-only by contract, and a 4000-byte NVS envelope is no place for a secret); `GET_CONFIG`'s `hasDeviceToken` reports `roll_state_has_credential()`; a token an earlier firmware already saved into config is imported once at boot and blanked. The parser (`meta_take_roll_credential`) is host-tested with cJSON. (2) `NETWORK_SET` stored the network and never joined it: `NO_CREDENTIALS` was terminal until a reboot. A saved auto-join network is now joined at once when the radio is idle. Device side only, same settings profile.

- **A four-camera capture no longer loses its largest frame to a retry stall.** The four-camera transport bench (2026-09-02, #132) found 6 of 20 grouped sets short a frame: under four concurrent transfers the RX FIFO drops the tail of a READ chunk near the 8192-byte boundary (the historical offset-8192 loss, no hardware flow control), and each loss waited out the full 1000 ms per-chunk timeout before a retry that then succeeded - enough 1000 ms stalls on cam2's ~458 KB frame to blow the 8000 ms whole-frame budget at 98%. `CHUNK_READ_TIMEOUT_MS` 1000 -> 400 (a healthy chunk arrives in ~120 ms, so a lost tail costs 400 ms to retry) and `XFER_BUDGET_MS` 8000 -> 12000 (room for the largest frame under four-way load), with a static assert tying them together. Nodes untouched; retry, CRC, mutexes, stacks and scheduler unchanged. Rerun: 20/20 complete, 0 lost frames. The offset-8192 tail loss is reproduced under load, not fixed - only made cheap to recover. (`firmware/HARDWARE_VALIDATION.md`, "One shutter, four frames".)

- **The bench capture command reaches all four camera channels.** `CAMERA_TEST` was the last cam1-only door: `run_capture()` now takes the channel and uses the per-channel link calls end to end, the stored frame is `C<n>.JPG` in its own TC folder, the reply names the cam it measured, and `CAMERA_LINK_STATS(_RESET)` lose the Milestone 1B gate and answer for the asked channel. Diagnostic surface only; the product capture path is untouched. With it, the basic four-camera hardware proof ran on the bench 2026-09-02: all four nodes individually and together, three matching checksums per frame, zero link errors (`firmware/HARDWARE_VALIDATION.md`, "Four nodes on four wires", #159).

- **Firmware 0.4.9: the settings do what they say.** Seven settings the camera stored, Studio drew a control for and the firmware then ignored (found 2026-08-30 while landing 0.4.8, issues #144 and #145). `shoot.previewQuality` never reached `viewfinder.c` and now picks the preview request, so low and high differ in frame bytes. `shoot.displayAfterShotS = -1` means "hold until dismissed"; `ui.c` multiplied it into a negative deadline, so HOLD behaved exactly like OFF, and the result screen now stays until a touch or a press. `body.sounds.warning` had no tone behind it and now plays one on a failed capture and on a full card. Favouriting worked over KDP and was stamped into `META.JSON`, but the body's own photo screen offered only DELETE and ROLL — it has FAVOURITE now, gallery tiles mark it, and `MEDIA_INFO` reads back the flag a thumb flipped. SETTINGS → DISPLAY gained rows for after-shot (0 / 1 / 2 / 3 S / HOLD) and camera-bank idle timeout, both of which were in config with no way to reach them from the body. `body.name` is new: a string of 0..24 characters, default `""`, shown on the About screen, written with `SET_CONFIG` like every other body field, and edited on Studio's Device page beside the serial — `config.device` is still the serial and still the identifier. And `GET_CAPABILITIES` gains `brightnessControl`, `false` on D4-V1, because `body.brightness` cannot ever work here: the Guition carrier drives the backlight from a plain GPIO and lit or dark are the only states (contract D11). Studio greys the slider on an explicit `false` and leaves it live on an absent flag, because firmware older than 0.4.9 never answered the question and a missing flag is not a no.

  **Per-camera exposure and gain reach a sensor for the first time.** QUAD slots have carried `exposureBias` and `gain` since the config schema existed and every look has a capture block, and none of it went anywhere: the node link had commands for quality and resolution only, and `capture.c` sent neither slot nor look values. `NL_CMD_SENSOR` (`0x15`) is the node-link command that closes it — `{aeLevel, gainCeiling, denoise, sharpness, quality?}` out, `{ok, applied:{…}}` back, with the node clamping to the driver's ranges and reporting what it actually set. It is node link, not KDP: no KDP command, payload or capability changed, and a host written against 0.4.8 needs nothing. Before each capture the P4 builds one request per camera in a fixed order — mode defaults, then the active look's capture block, then the QUAD slot's `exposureBias` and `gain`, the per-camera control winning over the shared one. `exposureBias` is an EV in -2..+2 and `set_ae_level()` takes an integer -2..2, so the EV rounds to the nearest step and -1.5 EV and -1.4 EV reach the same sensor; `gain` maps `auto` to AGC untouched, `low` to a 4x gain ceiling and `high` to 32x. Both mappings are pure C and host-tested. `META.JSON` records the echoed values per frame under `sensor`, so a photograph says what the sensor was told rather than what the config held at some later moment; `quality` there is the sensor's 5..63 scale, not the 60..95 wire percentage, the D12 conversion happening before the link.

  A sensor apply that NACKs or times out is logged and the trigger goes out anyway, with `sensor` omitted for that frame — a missed photograph is the wrong price for a settings write. So an absent `sensor` block has two causes, older firmware or a refused apply, and neither means the frame is bad. What still does not happen: the rest of a look — contrast, saturation, temperature and the other grading fields — stays Studio's at import, no grading runs on the camera, and `colorMode: 'mono'` is still not applied on the body. The slot stores mono, `META.JSON` carries it, and the conversion happens at import; that one is a product decision still open, not an oversight. Contract D19 records the command, the merge order, the two mappings, the failure rule, `brightnessControl` and `body.name`. The node image is built from the same `firmware/VERSION`, so a camera node reports 0.4.9 too.

  The P4 gets its own partition table. `CONFIG_PARTITION_TABLE_SINGLE_APP_LARGE` gave a 24 KB NVS, one 1500 KB `factory` slot and no OTA, and left about 12 MB of the 16 MB part unaddressed; at 0.4.8 the radio image measured 1451 KB in that slot, 3 percent free, and the next radio feature did not fit. `firmware/p4/partitions.csv` replaces it with two 3 MB OTA slots sized for the radio image plus real growth, a 256 KB NVS, `otadata`, `phy_init`, a 64 KB coredump partition, and 9.6 MB left unallocated for a future on-device data partition. `ota_0` starts at 0x10000, where `factory` did, so an image built before this change still flashes and boots there. The panic handler now writes to the coredump partition (`CONFIG_ESP_COREDUMP_ENABLE_TO_FLASH`), which costs 21 KB on both images: default 864 KB (0xd8110), radio 1472 KB (0x1702b0) with the new table. The CI size guards are re-derived from the 3072 KB slot, 1200 KB and 1600 KB. Flashing the new table erases NVS - config, hwv rows, saved Wi-Fi networks and the Roll device credential - so no unit carries it yet; `firmware/README.md` has the sequence and what restores what.


- **Firmware 0.4.8: the camera holds looks and sounds.** Ten commands answered `UNSUPPORTED_COMMAND` while Studio's Looks, Wiggle, Quad and Device pages, its backup and restore, and the reference mock all spoke them, and the camera's own LOOK tab offered COLOUR / B&W out of config. `GET_RECIPES`, `SET_RECIPE`, `UPLOAD_RECIPE` and `DELETE_RECIPE` answer now, and so do `GET_SOUNDS`, `SOUND_BEGIN`, `SOUND_CHUNK`, `SOUND_END`, `SOUND_READ` and `SOUND_DELETE`, with the request and response shapes `MockKinoDevice.ts` has always given — a host written against the mock needed no change. Eleven factory looks are compiled into the image from `firmware/p4/main/factory_recipes.json`, which is the same eleven documents as `FACTORY_RECIPES` in `packages/test-fixtures`; two copies of one document is how a Party Neg on the bench stops being the Party Neg in the app, so `factoryRecipeParity.test.ts` compares them id by id and editing one alone is a red test rather than a picture nobody can explain. Custom looks live on the card as `/sdcard/KINO/RECIPES/<id>.json` and custom clips as `/sdcard/KINO/SOUNDS/<id>.wav`; each upload is written to a temporary file and renamed at the end, so a power cut mid-transfer leaves nothing half-written.

  `SET_RECIPE` takes an optional `cam` — absent writes `wiggle.recipeId` as before, `cam1`..`cam4` write one quad slot, `all` writes all four and wiggle — and refuses an id that is on neither the factory nor the custom list, because a config naming a look the device does not have renders as the wrong picture at import rather than as an error. `GET_CAPABILITIES` grows `recipes` and turns `customSounds` true; both come off the same predicates the dispatcher gates on, so the flag and the handler cannot disagree. `recipes` is optional in `Capabilities`: firmware older than 0.4.8 omits it, and an absent flag means the commands NACK, not that the answer is unknown. `SOUND_END` adds one refusal the mock does not have — `BAD_FORMAT` for anything that is not 16 kHz mono 16-bit PCM — because the playback path has one sample rate and no converter, and a clip that fails there would otherwise fail at a shutter press instead of at upload. Limits are the mock's: eight clips, 128 KB each, 8192-byte chunks.

  What the camera still does not do is grade. No look's capture block reaches a sensor register; a look is stored, selected, carried in `META.JSON` and applied at import, exactly as before. Contract D17 drops from 27 unimplemented commands to 17, D18 records the `cam` field, `BAD_FORMAT`, the parity rule and the card paths, and Studio's conformance suite is 33 cases with `GET_RECIPES` passing and a new read-only `GET_SOUNDS` beside it.

  Found on the bench while proving it: **every KDP reply over about 4 KB had been silently lost since the first firmware.** `usb_link_write()` handed a whole frame to `usb_serial_jtag_write_bytes()`, which copies the request into the driver's 4096-byte TX ring as one item; an item larger than the ring can never fit, the call returned 0 at once, and the loop read that as "host gone" and dropped the frame. Everything under 4 KB worked, which is why `GET_HW_VALIDATION` at 3.8 KB answered and `GET_LOGS` never did - the trap in the bench notes that read "4 KiB frames time out". Writes go to the driver in 1024-byte slices now; `GET_LOGS` answers 14 KB on the wire and `GET_RECIPES` was the command that finally made it visible. The console log still shares that USB port, which is what a host decoder's `discardedBytes` are.

- **Firmware 0.4.7 / hardware 0.1.3: the shutter has a pin.** `BOARD_BTN_SHUTTER` has been `BOARD_BTN_NONE` since the first image and every capture has been fired from the touch screen, which works at a bench and is not a camera. It takes `GPIO28`, JP1 pin 21 — the pin ECN-0002 gave to `FLASH_EN`. The other two candidates were rejected on the board, not on preference: `GPIO35` (pin 15) is the ESP32-P4 serial-bootloader strap, so a switch to ground on it is a chip that comes up in the ROM downloader whenever the button is held through a reset; `GPIO31` (pin 10) carries `CAM_PWR_EN`, and giving it away would cost idle power-down for the camera bank and the only route the P4 has to power-cycle a node that has stopped answering. `GPIO28` is not a strapping pin — 34 to 38 are — and the line it replaces has never been asserted: no flash board exists, `capture.c` only ever held the pin low, and `HWV_FLASH_EN_GPIO28` never left `UNVALIDATED`.

  So the built-in constant-current direct-flash assembly leaves D4-V1. A separate external flash module will be used later; it has no P4 pin in this revision and its interface is not chosen. `flashControl` stays `true` in `GET_CAPABILITIES` because the command surface exists, `flashHardware` stays `false` because no flash board is fitted, and no KDP surface changed. `BOARD_FLASH_EN` is `BOARD_GPIO_NONE`, the `_Static_assert` set follows, `HWV_FLASH_EN_GPIO28` stays in the registry as an append-only row that can no longer flip, and `buttons.c` earns `HWV_BTN_SHUTTER` on the first debounced press.

  The wiring is one part: a 6 × 6 × 4.3 mm tactile switch between JP1 pin 21 and GND on JP1 pin 6 or 16, with no external resistor and no capacitor. The P4's internal pull-up holds the pin high, the input is active low, and firmware does the 25 ms debounce and the 600 ms long press. On a meter against pin 6 the pin reads 3.3 V idle and 0 V held. Twelve header GPIOs still carry eleven signals: the four TX/RX pairs, `SYNC_OUT`, `CAM_PWR_EN` and `BTN_SHUTTER`. `BTN_FN` and the mode slide still have no pin and will not get one in this revision. ECN-0003.

- **Firmware 0.4.6: the node sweep no longer wears the shutter's lock.** Gate F left one refusal standing: `CAMERA_CAPTURE` answered `BUSY "A capture is already running"` with nothing running whenever it landed in the background node sweep, which took `capture_lock()` for its whole HELLO round (14 of 20 idle requests before the 300 ms absent-channel probe, 1 of 20 after, 5 of 41 under load). The wire never needed that lock - `cam_link.c` already holds one mutex per channel for exactly one request/response - what was missing was priority. `cam_sched.[ch]` is the policy, host-tested (1,198 checks, cases A-L): REAL CAPTURE > BACKGROUND PROBE. A probe begins only when no capture is admitted and its channel is free, else it is deferred and the sweep comes back; a capture is admitted whenever no capture is active - maintenance never makes it BUSY - and if one probe is on a wire at that instant it waits for that single bounded transaction (300 ms absent, milliseconds present, one 3000 ms transaction when a present node goes silent), then starts. Channels are four independent objects; a capture defers all four. `capture_lock()`/`unlock()` bind `capture_active`; `capture_fire()` waits on a semaphore the probe's end gives; the sweep asks `capture_probe_begin()` per channel and holds nothing between channels. Additive telemetry: `CAMERA_CAPTURE` `bench.probeWaitMs`, `GET_RUNTIME_STATS` `probesRun` / `probesDeferred` / `captureProbeWaits`. Bench result in `firmware/HARDWARE_VALIDATION.md` ("Shutter availability").

- **Firmware 0.4.6: the radio recovers from a C6 reset without a P4 reboot - ROLL-C test 3 passes.** Bench 2026-08-30 on 0.4.5 (`C6_RESET_BENCH`): the C6 rebooted cleanly, the P4 kept running, and the host side kept the state of a coprocessor that no longer existed; nothing re-established anything and only a P4 reboot brought the radio back. `radio_recovery.c` is the fix, a host-tested state machine (113 checks, cases A-M of the bench brief) the supervisor in `net_hosted.c` drives: detect (ESP-Hosted `TRANSPORT_FAILURE`, `sdio_link_up` false, or two unanswered liveness RPCs) → quiesce (stop wanting an association, tell the netif it is disconnected *and stopped*, ignore events, send no RPC into the dead coprocessor) → `esp_hosted_deinit()` → one enable-line pulse with bring-up's 20 ms → free the recovery reserve and `esp_hosted_init()` + `connect_to_slave()` → wait for `rx_ready`, then `rx_ready && tx_ready` (the transport proof, not init's return code) → the version RPC over the restored transport, same policy, incompatible parks → the stale remote Wi-Fi stack deinitialised over the live transport, then init/STA/start → stored credentials through `net_link` → an address the existing lease rules accept → `recoveries` +1 in `NETWORK_STATUS` and `upload_queue_network_restored()`, which makes every `RETRY_WAIT` job due now with its attempts kept and wakes the worker. Every wait bounded; failures back off 2 s doubling to 60 s and park after 6 with the reason on the record; each attempt is a generation and stale observations are ignored; a parked machine restarts only on a bench pulse.

  Six images to get there, each one measured: the deinit hung/panicked when an RPC had been sent into the dead slave first; in-place re-enumeration read every frame as ~979 KB because the component's byte counters reset only in its own deinit/init; the re-init then asserted on its 15,872 B SW_AGGR buffer because the P4's PSRAM heap carries no `MALLOC_CAP_DMA` and internal RAM was down to a 7 KiB largest block - so `kdp_server`'s two 16 KiB frame buffers moved to PSRAM and a **recovery reserve** of two 16 KiB internal DMA blocks is held from bring-up and freed the moment before re-init; finally lwip asserted `netif already added` until teardown also ran the netif's stop action. `GET_RUNTIME_STATS` gains `internalFreeKB`, `internalMinFreeKB`, `largestInternalDmaKB`. On the board: five consecutive recoveries without a reboot in 9.8-21.8 s (one 122.9 s, spent in the component's own deinit RPC timeouts), and ROLL-C test 3 passing end to end - `firmware/HARDWARE_VALIDATION.md`.

- **Firmware 0.4.6, Gate F: photography measured against the radio, and the two ways the upload queue could park a good photograph.** Bench 2026-08-30, single CAM1 (OV3660, 1600x1200), LAN backend, four images; the record is `firmware/HARDWARE_VALIDATION.md` ("Gate F - photography wins"). What changed in the firmware:
  - `capture_report_t` and the `CAMERA_CAPTURE` reply carry a `bench` object (additive): `sdWaitMs` (time from shutter to holding the card), `radioState`/`radioDetail`, `uploadActive`/`uploadPending`, `internalFreeKB`/`largestDmaKB` at the shutter, the storage lock's cumulative yield and timeout counts, the capture worker's stack low-water mark, per-frame `chunkRetries[]` and `crcMatch[]`. One snapshot per capture, no new log lines.
  - The node sweep in `main.c` holds `capture_lock` while it HELLOs every channel; the default 3000 ms per absent channel cost ~9 s of lock every ~19 s on a one-camera body and refused 14 of 20 `CAMERA_CAPTURE` requests at idle. An absent channel is now probed for 300 ms (`OFFLINE_PROBE_MS`, `camlink_hello_ch_timeout()`); a present node answers in milliseconds. After: 1 refusal in 20 at idle, 5 in 41 under Gate F load - the residual sweep window, still open, recorded.
  - An upload step that could not take the card because a capture holds it is a yield, not a failure: `RQ_DISP_YIELD` (appended to `rq_disposition_t`), `rq_classify_step()`, `roll_step_result_t.card_yielded`, `roll_http_file_size_ex()` / `roll_http_sha256_file_ex()` / `read_card_file_ex()`. It costs no attempt and waits `CARD_WAIT_MS`. Before, twelve refusals inside two shutters parked the photograph `FAILED` with "the asset is not on the card" (CAP_000231, stat refused) or "META.JSON unreadable on the card" (CAP_000253, register step) while the file sat intact on the card; and a parked job whose record could not be written was re-stepped past its budget (attempts 20 against 12). A settled job now stays settled. Host tests: roll-queue 1,138 checks (+325).
  - Known and left: `UPLOAD_QUEUE_RETRY` revives the parked jobs the RAM list holds (`PARKED_IN_RAM_MAX` 4) and the rescan brings the next parked records back as parked, so a card with many parked captures needs repeated presses; the `failed` figure read 35 after a press that uploaded four, then caught up over the next passes. Both are queue bookkeeping, not photography, and are recorded in the validation file.

- **Firmware 0.4.5: a bench-only way to reset the C6 and nothing else.** ROLL-C test 3 asks what the upload queue does when the radio chip reboots under it while the P4 keeps running, and no product path resets the C6 after boot. `C6_RESET_BENCH` (`0x4d`, in the diagnostics block, private) pulses the C6 enable line once with bring-up's own timing and reports the link down; it is compiled only with `-DKINO_C6_RESET_BENCH=1` on a radio build, and every other build answers `UNSUPPORTED_COMMAND` and moves no pin. The decision lives in `bench_c6.c`, host-tested (13 checks: one pulse when built in, nothing otherwise, never a P4 restart). The bench client now keeps the raw bytes of any reply that does not parse. What the C6 reset does to a pending upload is recorded in `firmware/HARDWARE_VALIDATION.md`, not claimed here.

- **Firmware 0.4.4: the first photographs reach a Roll on a real backend, and the two defects that stopped them.** The LAN bench (2026-08-30, D4 on Wi-Fi at 10.20.80.181, the repository's Fastify API on the dev PC at 10.20.99.57:3000 with Postgres, Redis, MinIO and the worker) put a capture through the whole chain for the first time: document POST 201, thumbnail asset first, original second, `/complete` 200, worker `extract-metadata` and `generate-gallery-still` done 130 ms and 280 ms later, one Postgres row on the Roll, and the object in MinIO byte-identical to `C1.JPG` on the card (SHA-256 `66493736…d4353b`, 72,108 B, CRC32 `64bb3d9a` on the card, in the object and in the API's `assets.sha256`). Two application defects had to go first.

  **A photograph did not know its Roll.** `meta.c` wrote `rollId: null` into every META.JSON, the capture path never told the upload queue about a finished capture (only UPLOAD_ENQUEUE did, and it used whatever Roll was current), and boot reconciliation stamped the current Roll onto every committed capture that had no record. On the bench card that was 102 captures with `rollId null` and 34 UPLOAD.JSON records all naming a Roll their photographs were never taken on. Now `capture_report_t` carries the backend `rollId` snapshotted at the shutter, META writes it (string, or `null` when the camera was not on a Roll), a capture-done listener enqueues with that id, `upload_queue_enqueue()` reads the Roll from META and refuses a capture that has none (`ESP_ERR_INVALID_STATE`; UPLOAD_ENQUEUE answers `INVALID_STATE`), and reconciliation compares the record's Roll with META's: a disagreement, or a record for a photograph whose META names no Roll, is **retired** (parked `FAILED`, `last_error` "no Roll provenance: capture …, record …") rather than uploaded. The 34 stale records on the bench card were parked that way; their photographs are untouched. `rq_reconcile_action()` gained `RQ_REC_RETIRE`; MEDIA_READ may return `UPLOAD.JSON` so a bench can back a queue up before it is touched.

  **A job resumed after a reboot waited on the previous boot's clock.** `next_attempt_ms` is a deadline on the boot's monotonic clock and the record keeps it; after a reset the resumed `RETRY_WAIT` job sat until the new uptime passed the old number - 27 minutes on the bench (CAP_000186, boot-412 to boot-413) while the API had been back for three. `rq_job_boot_resume()` voids the deadline on the RESUME path of `upload_store_inspect()`; attempts are kept. Host suites: 2,501 checks (roll-queue 806, meta 125, store 100, net-report 98, kdp_core 75, and the rest unchanged).

- **Firmware 0.4.3: a read-only audit of every firmware file, and the fixes that did not need a bench.** Seven auditors read the P4 app, the camera node, the C6 image, the UVC preview tool, the shared components, the host tests and the firmware documents (2026-08-29, issue #137). What follows is the part of the ~100 findings that is a correctness or boundary defect and can be verified by reading and by the host suite; the task watchdog, the partition table, the file splits, the KDP RX reader task and the C6 SDIO re-establishment loop each need bench time and are tracked separately.

  Two were critical. **DELETE did not delete.** `storage_capture_delete()` unlinked five names and `rmdir()`ed; every capture also writes `THUMB.JPG`, so the directory was never empty, the picture reappeared in the gallery with no metadata, and every aborted capture became a permanent "kept for inspection" orphan. The six names live in one table now, `STORAGE_CAPTURE_FILES`, read by the delete path and by the `MEDIA_*` allow-list so they cannot drift again. **Three of the six `MEDIA_*` handlers checked for `/` but not `..`**, while the other three checked both; `MEDIA_DELETE {"id":".."}` walked one level up and removed `/sdcard/KINO/C1..C4.JPG` and `META.JSON`. All six go through `pure_is_capture_dirname()`, the rule the network path already used.

  The rest, briefly. The TLS gate read a cached "clock untrusted" reason instead of the clock, so a clock set from Studio never released it and uploads idled for the whole boot; it asks `clock_trustworthy_for_tls()` now, and the hold release is one guarded operation instead of a read-then-write that could resurrect `IP_READY` after a disconnect. `net_link`'s state was written from four tasks and read from three with no lock; it has one, compiled out on the host so the tests still build. A dropped AP made the event task call `esp_wifi_connect()` from inside its own disconnect handler with no delay — a full-band scan loop; it is an `esp_timer` backoff, 1 s doubling to 30 s. `cam_probe` guarded twelve seconds of HELLOs with a try-lock sample and called it exclusion; it holds `capture_lock()` for the sweep. `ESP_ERROR_CHECK(camlink_init())` was the one abort in a boot written to degrade, and it fired before the USB recovery channel existed; it logs. The gallery read `META.JSON` into 1 KB when a two-frame document is ~1076 bytes, so every multi-camera capture would have rendered as a UUID with zero frames; 4 KB, with a host test that measures a four-frame document. The log-drain task blocked on the USB FIFO while holding the TX mutex; events use a 250 ms bounded write and count a drop rather than wedge the server. The soak test passed `jpegQuality` to the sensor unconverted, the D12 inversion again. The node's frame buffer was sized once at UXGA while QXGA was selectable, so a bright 2048×1536 JPEG over 384 000 bytes overflowed it; it is sized for QXGA. A capture could spend two 4 s `fb_get` timeouts on the node's only task against an 8 s host budget; the discard is gated on a queued frame and the host waits 12 s. `taskmon` published a slot before filling it and read the table unlocked, the use-after-free it was written to prevent; it publishes last and reads under the lock, and its registry was exactly full at 20. A stored `apiBase` over 47 characters was truncated into a different, still-valid URL and used with the bearer token; over-long values fall back to the compiled default. Physical buttons drove the renderer from the buttons task — dormant only because no pin is assigned; they post to a queue the UI task drains. Plus: NVS return codes on the capture counter, a short `fwrite` that left a truncated frame in a committed capture, a viewfinder hold shorter than the pump it waited for, control bytes from a server error body that could make `UPLOAD.JSON` unreadable forever, thirty-two parked uploads blocking every new one, a per-pixel divide in the viewfinder blit, and a click sound re-synthesised with two thousand `expf` calls and 33 KB of internal heap on every tap.

  Documents were wrong about the board. `firmware-contract/README.md` D13 said `FLASH_EN` had no GPIO (it is GPIO28, JP1 21) and gave `SYNC_OUT` two different pins in one section (GPIO32, JP1 19); `power.c` and `hardware_validation.h` still described the pre-ECN-0002 map, telling a reader the camera-rail switch was inert while the idle path drives it. `firmware/README.md` said nothing was bench-validated under a ledger with six validated rows. The three C6 documents disagreed on whether anything had run on a board; `HARDWARE_VALIDATION.md` owns that now and the prose points at it. The two recorded SHA-256s for the same `kino-c6.bin` still disagree, and that is written down as a disagreement for the next build to settle rather than a pick. `firmware/*/sdkconfig.debug` — referenced by name, untracked by omission — is committed. Host tests: `kdp_core` 46 → 75 checks with four new decoder cases (exact `KDP_MAX_PAYLOAD`, a decoder smaller than the frame, discard accounting, magic inside a payload); new cases for `net_link_clear_clock_hold`, `rq_sanitise_detail`, an escape-heavy `last_error` under the 768-byte record cap, and the META.JSON size bound.

- **Firmware 0.4.2: the capture path had four concurrency defects an audit could see and a bench could not yet.** `storage_file_crc32()` hashed through one `static` 4 KB buffer while four capture workers and the bench read files back at the same time, so one task's `fread` could land between another's `fread` and its CRC update — an intermittent "stored X, node said Y" on a card that kept every byte, or a false pass. The buffer is per call now. Product captures and the bench captures in `kdp_server` (`CAMERA_TEST`, soak, `STORAGE_SELF_TEST`, `STORAGE_BENCH`) held two different locks that never looked at each other; a node holds exactly one frame and a new capture command replaces it, so a bench capture on cam1 during a shutter capture turned the worker's next chunk read into `BAD_ID`. One lock, `capture_lock()` in `capture.c`, and a binary semaphore rather than a mutex because the soak job releases from a task that did not take it. The card lock moved from `capture_task()` into `capture_fire()` for the same reason the viewfinder hold did: `CAMERA_CAPTURE` calls `capture_fire()` directly and was writing four frames with nothing keeping the upload worker or a `MEDIA_READ` off the SDMMC bus; the six `MEDIA_*` handlers and the gallery tile decode take the card as the UI user now and answer `BUSY` rather than contend. The viewfinder's pump counter was a `volatile int` incremented by four unpinned tasks on a dual-core part — `volatile` stops the compiler caching it and nothing else — so a lost update could let a capture start while a preview transfer was still on the wire, or leave every shutter press waiting out a 1.5 s hold. It is `atomic_int`.

  Two more from the same audit. **The camera node now discards the queued frame before a photograph.** With `fb_count=1` the driver captures one frame the moment the buffer is returned and then stalls, so every capture after the first was handed a frame exposed just after the previous release — M1 measured 1.8 s, 3.4 s and 27.0 s before the shutter. `camsensor_discard_queued()` empties the buffer first, which bounds the frame start to one frame period after the command (~112 ms at UXGA); preview sizes skip it so the viewfinder keeps its rate, and the reply carries the cost as `discardMs`. This is a correctness fix, not synchronisation: the four sensors still free-run, and `SYNC_FEASIBILITY.md` still scopes that work. And **both images now build at `-O2`**: every one of the 1005 objects in the P4 image was compiling at IDF's default `-Og`, CRC loops and JPEG staging included. `sdkconfig.debug` is the overlay that puts `-Og` back for a debugger; a `sdkconfig` generated before this change keeps `-Og` until regenerated, and `firmware/README.md` says so. The first `-O2` build failed on `-Werror=stringop-truncation` and `-Werror=format-truncation`, which only fire with optimisation on: every `strncpy(dst, src, sizeof dst - 1)` in `p4/main` is `strlcpy` now (26 sites, five of them in the soak job with no `- 1` at all, which is the unterminated copy the warning exists for), and the Wi-Fi credential slot key grew from 8 to 12 bytes so `"n%u"` cannot truncate. The `-- USING O3` line in the configure output is ESP-IDF's own message, not a project flag; every object was `-Og` while it printed.

- The JP1 header map was wrong for the second time, and this time the board settled it. The previous entry replaced the recorded pinout with the manufacturer table for the `JC-ESP32P4-M3-DEV` — a different carrier that shares the ESP32-P4 module and nothing else, our board being the `JC4880P443C-I-W`. Under that map `CAM1` opened UART1 on GPIO1/GPIO2, which reach no connector here, so the camera link could not work and its failure looked exactly like a wiring fault: a node that was known good in isolation answered nothing across 113 frames, a diagnostic node image logging every byte on its link UART saw none, and a TX-to-RX loopback across JP1 7–9 with the node removed entirely received zero bytes while a continuity check on the same jumper read a clean logic 1.

  So the header was measured instead of transcribed. The P4 drove all 47 of its usable GPIOs in turn — everything except the console pair and the mounted card's six — each announcing its own index in binary as a six-pulse sync followed by six bit windows, one pulse for a 0 and three for a 1, while a camera node watched a single wire and reported what it counted. JP1 pin 13 answered as GPIO49; JP1 pin 7 answered as GPIO52, predicted from the drawing before the run. The map is back to GPIO52/51/50/49/35/34/32/28 on the left and GPIO33/31/30/29 on the right, which is what the repository recorded originally and could not justify at the time.

  Twelve header GPIOs carry eleven signals, so `FLASH_EN` (GPIO28) and `CAM_PWR_EN` (GPIO31) have pins again and the I²C expander the previous entry proposed for M2 is unnecessary — which matters for M1 §37, where cutting the camera bank is the whole test. `GPIO35` on pin 15 is the one spare, and it stays unconnected: it is the ESP32-P4's serial-bootloader strap, so a pin tied low there is a board that comes up in the ROM downloader instead of the application, which at a bench reads as dead firmware rather than as a wiring mistake. `GPIO34` carries `CAM3_TX` and is a strapping pin too, safe only because we drive it and a node's UART RX is high impedance; a camera's TX must never land on either. Both rules are now compile-time assertions.

  The rule that came out of it, and the reason this is the last time: do not edit these numbers from a datasheet, a photo of another board, or a vendor page. Measure the pin. A manufacturer document is evidence about *a* board; only the bench is evidence about *this* board. ECN-0002.
- **The D4 header map was fiction, and the camera UARTs were on pins that do not exist.** The repository recorded the Guition carrier's 2x13 expansion header as carrying GPIO52/51/50/49/35/34/32/28 down the left and GPIO33/31/30/29 down the right, and put the four camera UARTs on 52/51, 50/49, 34/33 and 30/29. The rows came in on 2026-08-21 as "hardware data" with no manufacturer source, and because rows 1-3 and the C6 block were right the table passed review. The JC-ESP32P4-M3-DEV pinout exposes exactly eleven P4 GPIOs on JP1 — 1, 2, 3, 4, 5, 20, 32, 33, 45, 46, 47 — and not one of the eight recorded camera pins is on any connector. A node wired to the old instructions could not have answered, and the failure would have read as a bad ground.

  GPIO3 and GPIO5 are the touch and panel resets, both validated on hardware, so nine header pins are free for ten wanted signals. The map is now CAM1 GPIO1/2 (JP1 pins 7/9), CAM2 GPIO47/46 (10/12), CAM3 GPIO32/33 (19/21), CAM4 GPIO45/4 (14/13), SYNC_OUT GPIO20 (17); FLASH_EN and CAM_PWR_EN have no header pin and are `BOARD_GPIO_NONE`, which the firmware honours by not driving them rather than by driving pin -1. None of the nine is a strapping pin (34-38), USB (24-27), SD (39-44), the C6 SDIO bus (14-19, EN 54), I2C, I2S or LCD. `board_d4v1.h` is the one source of truth: `_Static_assert`s prove uniqueness, header membership and column parity at compile time, a host test (`test-pins`, 293 checks) repeats them and dumps the map, and a vitest in `@kino/hardware-profiles` parses the header and fails if `d4-v1.json` drifts from it. The profile's gpio keys are `CAM1_TX`…`CAM4_RX` now, each with its JP1 pin. Every camera row in the validation ledger stays UNVALIDATED; the one row that had been marked validated, `CAM_PWR_EN GPIO31`, is void, because GPIO31 goes nowhere. Hardware design package 0.1.1, ECN-0001. Do not wire a XIAO from any document that names GPIO52/51.

- **The C6 routing is established, and finding it turned up a defect that was waiting for the bench.** The mapping came from outside the repository — Guition documentation for this carrier — and was reconciled against two sources inside the toolchain before any pin was written down. `espressif/esp_hosted` 3.0.6 pins all six SDIO signals to exactly GPIO14-19 for a P4 host with a C6 coprocessor, with `min == max` so it is one value rather than a range, and `ESP_HOSTED_HOST_RESET_GPIO` defaults to 54 for every P4 target rather than one board. Independent support that this carrier follows the reference pinout: the microSD block is exactly the P4's slot-0 IOMUX pad set from the SoC headers, and that set was already validated here by a real 29820 MB mount. CLK and CMD cross over between the two chips, which looks like a transcription error and is not.

  The defect was not a pin. Separate pins are not separate driver resources: the P4 has one SDMMC controller with two slots, `SDMMC_HOST_DEFAULT()` selects slot 1, and `storage.c` never assigned `.slot` — so the card has been sitting on the slot ESP-Hosted needs for the radio. It worked, because slot 1 is matrix-routable and the pins were passed explicitly, which is exactly why it would otherwise have been found at a bench with a half-built radio. The card is now on slot 0, where those six pins are the IOMUX pads the board wires; slot 1 has no IOMUX path at all, so the old configuration routed the card's own dedicated pads through the GPIO matrix to reach the wrong slot. Espressif ship this arrangement as an example. `GPIO54` is named `BOARD_C6_EN` and not `BOARD_C6_RESET`, because it is the C6's `CHIP_PU` — an enable — and a macro called `C6_RESET_HIGH()` asserts the opposite of what it reads like. Its polarity on this carrier is still unconfirmed, so it is one constant for the bench to flip.

- **The C6 image is Espressif's ESP-Hosted coprocessor now, not a bespoke Wi-Fi application.** The earlier conclusion that the official slave "does not compose as a component on IDF 5.5.1" was measured against esp_hosted **0.0.10**, a stale package; 3.0.6 resolves and carries the coprocessor firmware in the same component. So the hand-written radio app was wrong, and `radio.c`, `transport.c` and `board_c6.h` are deleted rather than kept because they existed — the component owns the radio, the RPC server and the SDIO slave, and its Kconfig declares *and enforces* the slave pins, so a duplicate set nothing included was only somewhere to drift. Pinned to an exact 3.0.6, because host and coprocessor negotiate an RPC wire version and a mismatch on this board presents as a transport fault, which is the failure most likely to be misdiagnosed as bad Wi-Fi. 1 105 872 bytes, and two clean builds of one commit match. The module's flash size is unknown and the image does not fit the 2 MB table, so that is a gate at the top of its README rather than a footnote.

- **Capture and upload share a real lock instead of a boolean.** The previous state was a file-static mutex in `capture.c` that nothing else could see, plus a flag the upload worker polled — a hint, not exclusion, so a reader that opened a handle between the check and the capture still took a descriptor. `storage.h` now owns the card with three users in priority order, a capture is never refused it, and the worker gives it back early when `storage_yield_requested()` goes true, which bounds a capture's wait by one chunk rather than one whole file. The audit that came out of this corrected the stated reason for the descriptor budget: **no path in this firmware holds two handles at once**, capture included, because the frame is closed before its CRC read-back opens it. The real reason a capture holds the card is the SDMMC bus and the frame spread, and three comments that said otherwise are fixed.

- **Two test suites were flaky for findable reasons, and the Twin build was red.** Three sites indexed a `Record<FirmwareProfileId, …>` with a plain `string`, which is a `TS7053` that breaks `tsc -b` and therefore `npm run build` in CI; one narrowing accessor fixes all three and keeps the hazard the casts would have hidden, since the lookup can genuinely miss. The worker's queue test waited for the `failed` row while asserting on the `abandoned` row written after it. The KDP relay test picked a port from a fixed 40-wide window with no bind retry, so a lingering socket surfaced as an unrelated timeout; it asks for port 0 now, which needed the relay to report the port it actually bound rather than the one it was asked for. Worth recording: local npm 9.6.4 exits **0** when a workspace script exits 7, while node 22's npm 10.9.8 — the one CI installs — returns 7. A green local `npm test` proves less than it looks like.

- The camera had two wall clocks and only one of them was ever set. `clock_set()` moved a private epoch base inside `clock.c`; `settimeofday()` was never called, so `klog`'s `t`, FAT's file dates and everything else reading `gettimeofday()` went on counting from power-on while `capturedAt` reported the host's date. The bring-up log caught it in the plainest form available: the entry announcing "clock set to 2026-08-27T19:38:44+02:00 by host" carried `t=526536`, about nine minutes past 1970. It also explains the 1970 dates that looked like a corrupt persisted clock and were not — the RTC keeps running across a soft reset, so those numbers were the board's accumulated powered time.

  KINO wall time is now the ESP-IDF system clock. `clock_set()` calls `settimeofday()` and persists, `clock_now_ms()` reads the system clock instead of keeping a second epoch, and `klog` asks `clock_now_ms()` rather than libc, so there is one definition of what time it is. `clockSource` and the UTC offset stay KINO metadata, because the system clock cannot hold either. `esp_timer_get_time()` is untouched and remains the monotonic clock for `klog.us` and every duration — a wall-clock correction must be free to jump and a measured duration must be free of it. `LogEntry.t` keeps its unit and meaning.

  Restoring gained the rule it never had: a persisted time is adopted only if it is a real time (2020..2100) and only if it is **later** than what the system clock already reads. The RTC survives a reset, so after a host sync the running clock is fresher than the NVS snapshot, and adopting the snapshot would date the next capture before one already on the card. `pure_clock_restore_action()` holds that decision so it is host-tested rather than reasoned about: 19 new checks, and a mutation to the old always-adopt behaviour fails exactly the two that describe the regression.
- `GET_RUNTIME_STATS` was reporting freed memory as a stack measurement. The icon builder deletes itself when it finishes and the task registry kept its handle, so every snapshot called `uxTaskGetStackHighWaterMark()` on a TCB that had been freed. On the first P4 bring-up it read 0, then 1460, then 1380 across three calls seconds apart — drifting, because the heap underneath was being reused, and 0 reads as a task that all but overflowed. A self-deleting task now hands the registry a last reading while its own stack still exists, and the row is marked `exited` so a boot-time number is not taken for live telemetry. Registration became idempotent by name and refuses to resurrect an exited slot, which is the half that actually made it work: the icon builder outranks the task that creates it, so it finishes and reports before the `xTaskCreate` site has registered it at all, and the registration arriving afterwards carried the dead handle. Measured after the fix: 2292 bytes, identical across four calls over 35 s.
- `cam_probe` had 356 bytes left of its 4096-byte stack, measured on the first P4 bring-up with no nodes fitted, while it was doing nothing but timing out on four empty channels. The branch it takes when a node *answers* is the expensive one — a `camlink_info_t` on this stack, filled through `camlink_get_info_ch()`, then a `klog` line through varargs — and it runs the instant CAM1 first replies. The overflow would have landed exactly on the node-greeting checkpoint and read as a link or node fault. 8192 now, and the same measurement reports 4452 free.

- Two clean builds of one commit produced different firmware, so a hash written into a bench record could not identify the source that produced it. `CONFIG_APP_REPRODUCIBLE_BUILD` was unset, which leaves ESP-IDF stamping the build clock into `esp_app_desc` and deriving the ELF SHA-256 beside it: two `fullclean` builds of identical `camnode` source differed in 67 bytes, beginning with three digits of the build time at 0x74 and the whole ELF SHA-256 field at 0xB0. The option is set for all three apps now, and `camnode` built twice from two different directories gives the same `kino-camnode.bin`. Nothing in this firmware calls `esp_app_get_description()`, so no timestamp is given up to do it. M1 identifies the flashed image by hash, and until now that record was decoration.

- The camera bench tool reaches its own log. Pressing BOOT during the three seconds after power-up — with the LED lit to mark the window — skips UVC, keeps the USB-Serial-JTAG port, redirects the log onto it and reports per-frame JPEG sizes with a SOI/EOI check done on the device, before USB touches the data. It could not be a single read at startup, which is what it was first: BOOT is GPIO0, so holding it while the board powers up puts the ROM into download mode and the app never runs. First use corrected three things guessed at earlier — the sensor implements every setter this firmware calls rather than lacking denoise, real JPEGs are 7.7-30.4 KB rather than the 160 KB the transfer buffer was sized for, and the sensor sustains at least 16 fps, so the 6 fps over USB is neither the clock nor the sensor. Bulk transfer was then tried against that and delivered 6.1 fps against 6.0, so both obvious levers are tested and 6 fps is what this component gives; it is kept for its packet retransmission rather than for a frame rate it did not buy.
- The six KDP questions the firmware contract listed as "firmware team decision required" are decided, because a bench is the wrong place to meet a design question. Sequence IDs wrap to 1 and never to 0 — 0 is the events' sentinel, and a uint32 left to overflow would start minting requests that read as events; `nextSeq()` in `packet.ts` and `kdp_next_seq()` in `protocol.h` are the same rule on both ends, and the P4’s link to the camera nodes now uses it. HELLO is confirmed **not** to be a precondition and firmware may not make it one: the client, the reference device and the P4 dispatcher already agree, and a stricter device would reject hosts that are within contract. `GET_MODES` and `CAMERA_ARM` get real response types — `armWindowMs` is required, since with no CAMERA_DISARM it is the only thing telling a host when the sensors fall back to `ready`. `STATUS` (0x81) and `FW_PROGRESS` (0x82) are reserved and must not be emitted by a 0.x device rather than being given a guessed payload for a flow that does not exist. Media resume stays offset-restart with the frame CRC as the per-chunk check and no completion hash. `JOB_PROGRESS` for a job this session never started is sanctioned for the reconnect case that produces it, and inventing job IDs is not. None of it bumps the protocol: KDP stays 1.
- The guest Roll is rebuilt as the album that belongs with the camera. Chrome is a moulded warm-grey plate — the ABS of a camera accessory, edged by two 1 px inset lines rather than a gradient — and the photography runs full-bleed on black between the plates, one capture per row on a phone. The frame counter sits in a dark window let into the plate the way it does on a camera's top deck, the three destinations are chunky keys in a routed well where the selected one reads as pressed in, and clock marks group the stream by minute instead of stamping a relative time under every tile. The four-frame mark is the wiggle's playhead: one bar per camera, and the lit bar is the frame on screen, so it says a capture has four frames, shows which one you are seeing, and is the indicator when motion is off. The photo page is back, photograph, contiguous four-frame strip, camera metadata, and one Save. Type is BIZ UDPGothic with tabular figures — 56 kB of Latin subsets; the Japanese subset is 1.3 MB and is deliberately not shipped. The host dashboard keeps its light treatment, separated by `data-surface` on `<body>`.
- Saving hands the file to the system share sheet where the browser supports it, so a photograph reaches Photos instead of Files: a plain download link does not reach the camera roll on iOS. Downloads remain the fallback, and the sheet now names what each row is for — original, wiggle as MP4, and the three crops — with sharing a link alongside them.

- Asking for better JPEG quality made the picture worse. `jpegQuality` is documented `60..95` across the contract and Studio's sliders, where higher is better; `esp32-camera` takes `5..63`, where **lower** is better, and the node clamps to it. Nothing converted between them, so 85 arrived as a request for 63 — the worst JPEG the sensor can make — and 95 did the same. Both numbers sit inside the other scale's range, so neither end could detect it. One conversion now, in `capture_quality_to_sensor()`.
- Only CAM1 was ever greeted. The probe task said hello to one channel, which was right while one node was all the harness had and stopped being right the moment a capture asked which cameras to fire: a channel nobody has greeted reports offline, so a body with four nodes wired would have taken a one-frame photograph and called three cameras missing. All four are greeted in turn now — in turn rather than at once, so a bench unit with one node fitted does not collect four simultaneous timeouts every two seconds.
- Every gallery listing reported every capture as a wiggle taken at the epoch. `media_summary()` read `kind` and `ts` from `META.JSON`, which has never contained either — it is a `kino.capture` document and always was, carrying `mode` and `capturedAt`. The fallbacks looked exactly like deliberate defaults. The wire names are unchanged; only the keys read from the file are.
- The conformance suite failed honest behaviour. `MEDIA_INFO` demanded exactly four files, which was true while every capture came from a body with four cameras wired and is not true of a partial capture — a state the capture pipeline reports on purpose. It now checks that the file count agrees with the capture's own `frameCount`, which catches a real defect a fixed 4 could not, and treats the deliberately-omitted `sha256` as absent rather than failed. `MEDIA_READ` gained a check that offsets actually seek: reading the same bytes from 0 and from 128 looks perfectly healthy until a client assembles a file out of pages and gets page one repeated.
- Touch stopped losing presses, and neither cause was in the touch driver. Sounds played straight from the UI task: `play_click()` opens the codec, queues the samples, waits for the DMA to clock them out and closes again — the best part of 200 ms during which the GT911 went unread, so presses were missed and releases arrived late or not at all. Sounds now go through a queue to their own task and the UI never waits on one; the queue drops rather than blocks, because a backlog of ticks finishing after the finger has gone is worse than a missing one. The second cause was `s_down` being cleared on a **single** failed read, on a bus the GT911 shares with the ES8311 — a codec transaction could return no points while the finger was still down, so presses let go of themselves mid-tap, worst of all exactly while a sound played. A release now needs three consecutive empty polls, 45 ms against the ~80 ms a real tap takes to lift. The point is also published as one 32-bit word rather than two `uint16_t`s, because the UI could read the new x with the old y and register a press on a tile the finger never crossed.
- A touch that wakes a sleeping screen wakes it and does nothing else. The backlight goes off but the UI keeps running and keeps hit-testing, so reaching into a bag for a camera whose display had timed out fired whatever tile the thumb landed on. The whole press is swallowed until the finger lifts.
- Every 3D model rendered mirrored left-to-right. The fix that turned the objects to face the camera negated x as well as z, on the theory that a lone z flip would mirror the image; a lone z flip is the standard right-to-left-handed conversion and mirrors nothing, while negating both is a half turn about Y, which really does. It survived for as long as every object was symmetric — the film canister's leader and the crate's letter A, both on the wrong side, are what exposed it.
- The 3D depth buffer collapsed. Depth was scaled by a fixed constant, which for a model at about 1.9 radii put every near surface at roughly −283000 before clamping, so everything in front landed on 0 — and since the test rejects equal depths, whichever part was built first won: the camera body hid all four of its lens barrels and the gallery cards hid the photographs printed on them. Near and far planes are now mapped onto the full 16-bit range per model, which is what lets the buffer separate two surfaces a fraction of a millimetre apart.
- The camera XCLK drops from 20 MHz to 16 MHz, which is a capture-integrity fix and not a tuning preference. At 20 MHz the OV3660 corrupted 77 of 160 frames (48%), each carrying a chroma-damaged band in a fixed ~12-pixel zone around x=498 — JPEG MCU column 31. At 16 MHz the same measurement over 200 frames gave 1 (0.5%). Corrupted JPEGs at 20 MHz are a known esp32-camera problem and the guidance is to avoid multiples of 10 MHz on this part. `camnode` reads the same constant from `board_xiao_s3.h`, so the product was set to corrupt roughly a third of its captures over the same DVP bus — silently, into a roll nobody is watching a preview of. The cost is capture throughput, which single stills barely spend; a corrupt still costs the photograph. The 0.5% residue is upstream, and the next lever is the OV3660 PCLK register fix rather than another clock change.
- Sensor tuning is applied rather than left to whatever the driver last wrote: gamma, black and white pixel correction, lens correction, AWB with its gain stage, DSP-assisted exposure and a capped gain. Untuned frames were flat, grey and speckled, which is what makes a good module look faulty. Saturation goes to **-2**, Espressif’s own value for this part, whose example notes these sensors ship over-saturated — lifting it instead amplified a slight green bias until grey shirts read olive (G +1.9% untuned, +12.0% lifted, +5.2% at -2). Every setter is null-checked: the driver only fills in the ones a sensor model implements, and calling a missing one panics before USB comes up, which presents as a board that enumerates as a serial port and never appears as a camera.
- Wiggle playback and the D4 frame strip no longer depend on `downloadsEnabled`. A host turning saving off used to freeze every photograph in the Roll and hide the four frames behind it; that flag now gates only what leaves the phone.
- Guest touch targets meet 44 px, including the back link on the photo page, which was a 167×14 px hit area and the only way back. The photo page has an `h1` naming the photograph — its only heading had been `h2 "D4 frames"`.
- Roll's shared control styling no longer out-specifies the surface it lands on. `button:not(.kino-button):not(.roll-tab):not(.action-link):not(.frame-thumb)` scored (0,4,1) and silently forced 30 px controls and a light gradient onto anything new; bare-button styling is now scoped to the host surface, with `.kino-button` still covering the design-system button everywhere.

- The on-device UI is rebuilt around the camera rather than around a launcher. The 2x3 menu stays and becomes the home screen: **MODE, LOOK, GALLERY, ROLL, SETTINGS, POWER**, each a large Windows 98 shell icon with a word under it. There is no SHOOT tile, because shooting is a hardware action - the physical shutter opens the viewfinder from the menu and takes the photograph from the viewfinder, and it fires from any screen. FLASH and STATUS are gone from the top level: flash is a cycling control on the viewfinder, one press per change, and the diagnostics STATUS carried moved into Settings. There is no global status strip either; a permanent SD/WIFI/ROLL bar across every screen is what a miniature PC looks like, so battery lives in the viewfinder and storage speaks up only when it is nearly gone.
- The viewfinder is four previews, Back, Flash and Battery, and nothing else. Frame rates, card capacity, link diagnostics, the mode readout and the on-screen shutter are all gone - they were engineering information competing with the photograph. Four 4:3 panes on a 5:3 panel leave a column of dead space down each side whatever you do, so the three controls live in those columns and the panes take the full height: 308x231 each, against 272x204 for the strips-above-and-below version, which is 49% more picture.
- Every control now writes the real configuration. MODE and FLASH used to mutate a local variable and nothing else, so the screens did not change what the camera did and the viewfinder - which reads the config - visibly disagreed with the screen you had just used. MODE offers WIGGLE and QUAD, which is what `SET_MODE` accepts; the BURST and SINGLE the old screen advertised were never implemented anywhere in the firmware. LOOK sets `colorMode` across the four quad slots, which is a real contract value that persists and is stamped into META.JSON, and says plainly that looks are applied on import rather than pretending the preview will change. Anything the hardware cannot do renders disabled with the reason - brightness ("The backlight on this body is on or off"), Wi-Fi and auto-upload ("Not fitted"), shut down ("Hold the power slide").
- Gallery is a photo browser instead of a dead end. Thumbnails are focusable and open a single photograph decoded at 520x390 through `thumb_load` rather than a 208 px tile blown up, and a capture can be deleted behind a confirmation. Captions are one short line - the mode, or "2 of 4" in red for a partial - with no filename, size or path.
- The menu icons are Windows 98 shell icons at their native 48 or 32 px, scaled by an exact integer factor with nearest neighbour and given a much lighter display emulation than the XP set had. That last part matters: these are pixel art with hand-placed dither, and the sharp-bilinear pass built for smooth-scaled artwork averaged the dither into mush. `scripts/bake-w98-icons.mjs` decodes the ICO/DIB directly - taking the deepest frame at the largest size, because these files carry a 16-colour and a 256-colour copy of the same drawing and the first directory entry is the flat one.
- Every setting a screen writes goes through `meta_patch_path`, which builds the nested patch `config_merge` expects. It lives in `meta.c` and is host-tested against the real cJSON because the first version dropped the first path segment - `body.sounds.ui` built `{"sounds":{"ui":...}}`, which merges into the config root perfectly happily and writes a key nothing reads. The screen would have looked right and the toggle would have moved.
- **Studio's Bring-Up page is the field body's checklist, not the battery build's.** `KINO_FIELD_BODY` 0.1.4 is a USB-C powered fixture with no pack bay (ECN-0004), no built-in flash (ECN-0003), no Hall switch and no function button, and the 78-item sheet still opened with "LiPo inspected" and closed its electrical run with "Flash added last". Twenty-five items are retired - the LiPo/BMS/SW6106/fuse checks, the flash driver and chamber checks, the whole BATTERY-PATH section, battery runtime, the charge-temperature check, low-battery-during-flash - and seventeen are new: a USB-C POWER section (which port is USB-Serial-JTAG, bus voltage at JP1 under a four-camera capture, brownout margin, laptop port vs PD brick vs power bank, hot-replug), the GPIO35 strap check, the glued face shell, the sliding cover's all-four-dark test, the shutter's lead channels, peak bus current and a power-bank runtime for V2. 70 checks. Surviving items keep their ids so a saved record keeps its ticks; the retired ids are listed in `bringup.ts` so an old record's ticks can still be read. "SD writes and speaker verified at low volume" becomes "SD writes and body sounds verified" - the sounds come from the Guition carrier's ES8311 + NS4150, not from a part we fit. `FLASH_EN` stays in the wiring record reading unassigned, as before; the never-wired `Function button` row goes.
- **Studio's Bench measurements table only asks for parts that are in the camera.** `measurementTasks()` iterated every `components` entry, and the profile keeps the battery build's BOM history there - battery, fuse, BMS, SW6106 module, camera switches, speaker, caps, slide switch, harnesses - none of which has an instance in the field body, so fourteen rows demanded calipers on parts nobody could pick up. It now iterates only components with a placed instance. The body row keyed on `confidence !== MEASURED` and still asked for "skeleton ribs" and `enclosure-shell/enclosure-chassis`, components that no longer exist; the body is OFFICIAL_CAD from `hardware/cad/KINO_FIELD_BODY` (the six `field-*` shells), so the row appears only for a provisional envelope and names the field shells. Stage A no longer watches an SW6106 that is not fitted, the acceptance item for issue #4 drops "flash", and the record-in text names the Twin's actual control (PARTS -> MEASURE ACTUAL PART). Tests cover the instance filter with a synthetic unplaced part.
- **Studio's Skew Bench speaks the shipped SYNC_BENCH contract.** Firmware answers SYNC_BENCH with one blocking RESPONSE of per-camera edge counts (no timing) after up to pulses×gapMs; the reference device on `d4-sim-full` answers a job start. Studio sent `startJob` unconditionally, timed out at 3 s and threw JOB_NOT_ACCEPTED against every real camera. It now sends `{pulses, gapMs, poll}` as a plain request with a deadline sized to the run, adopts a job when the reply names one (`adoptJob`, previously unused), and otherwise renders an EDGE INTEGRITY report — accepted/raw/rejected edges, expected, short-by, monotonic and clean per camera, one TRIGGER WIRE CLEAN / DIRTY verdict — stating that exposure skew is not measured by this firmware. Run sizes are 25/100/200 pulses at 100 ms, the firmware's 1..200 bound. Overview's SENSOR SYNC tile quotes the edge verdict without inventing a skew figure.
- **Studio gates flash on the emitter, not the flash window.** D4-V1 reports `flashControl: true` (the firmware keeps a window) with `flashHardware: false` (ECN-0003, no LED). Calibration FLASH and Developer FLASH TIMING are Unsupported with the reason, the power-load ladder drops its flash rungs, Overview's FLASH row reads NOT FITTED, and Shoot FLASH POLICY, Wiggle FLASH IN WIGGLE and Quad FLASH / FIRE-SKIP keep their controls with one visible note that they set the firmware's window only and the external top light is not camera-controlled.
- **Studio's Shoot page renders GET_MODES.** The session fetches GET_MODES (tolerated when absent) into the device store; the mode cards render the camera's `available` / `unavailableReason` — a mode the camera cannot shoot now is a disabled card with its reason — instead of a hard-coded pair, and SET_MODE re-reads it.
- **REGISTER KINO writes `network.apiBase`.** The firmware's own uploader reads `network.apiBase` (`https://host[:port]`), not `roll.credentials.serverUrl`; registering against a server left the camera posting to the compiled default. Studio now writes the server origin there alongside the credentials, and the Roll page shows the camera's stored upload target.
- **Device page PHYSICAL CONTROLS say what D4-V1 has.** FUNCTION BUTTON and SLIDE SWITCH have no GPIO on this body (ECN-0003) and the firmware reads neither key; both selects are disabled with that note, and wiggle VIEWFINDER IN WIGGLE / SAVE ORIGINALS and quad slot FLASH / NOTE carry a one-line "not read by firmware" hint from one shared helper.
- Studio: LOAD DEMO PACKAGE on Updates is developer-mode only and hidden unless the session is simulated — it was reachable in production in front of a real camera. Dead exports removed: `MASKED_PASSWORD`, `cloneRecipe`, `getRollLinks`, `clearCreatedRolls`, `resetUpdateState`, `setRollServerUrl`.
- KINO Twin: the FAULTS tab gains a FIRMWARE PROFILE selector, so the Twin can answer as the shipped firmware (0.4.56, one camera wired, refusing what the camera refuses) or as the simulated-future demo device. Switching reboots the device with a new session, and a connected Studio sees the reboot the way it would a real one (HARDWARE ERROR, then connect again).
- KINO Twin: SCREEN VIEW (`#screen`, a header button, or SCREEN VIEW on the SCREEN tab) shows just the camera's display, full window, with nothing else mounted — no 3D scene, panels or overlay, so no view reset or pick can get between you and the firmware. Touch, SHUTTER and FN work; FIT / 1X / 2X scaling (integer scales draw pixel-exact); Esc leaves. The simulator keeps running underneath, so Studio's link, the card and the config survive entering and leaving it.
- KINO Twin: the firmware screen hot-reloads. `npm run twin:ui:bake` rewrites the module in place; SCREEN VIEW's WATCH notices the new bytes and restarts only the screen on the new build (RELOAD does it on demand), and the dev server no longer reloads the whole page when a bake lands. The bar shows the module fingerprint and when it was loaded.

### Removed

- The provisional enclosure in the Twin: `enclosure-shell` (two clear acrylic panels) and `enclosure-chassis` (the skeleton frame), their builders in `@kino/three-assets`, and the five pack-chain nets that ran to parts the field body does not place. Studio's MEASURE checklist row "Final acrylic thickness" became "Face shell thickness".
- KINO Twin's FIRMWARE, TIMING and FLASH tabs. FIRMWARE picked a firmware generation for the sketch; with the real `ui.c` on the screen the Twin boots as today's camera, and `device.setFirmwareProfile()` or a Studio OTA install still switch it for tests. TIMING and FLASH charted per-camera exposure numbers the code itself labelled SIMULATED and invented; next to a real firmware screen they read as measurements. `tagLabel` moved to `panels/provenance.ts` for POWER.
- Studio's in-tab demo device. KINO Twin is the simulator: it runs the same reference device, over the same protocol and the same connect path, and has its own fault panel — Studio was shipping a second, weaker copy of it. Gone with it are the `OPEN DEMO DEVICE` and `DEMO` buttons, both demo menu items, and Studio's simulator-faults panel. The reference `MockKinoDevice` stays as test infrastructure and no longer ships in the product: the Studio bundle drops from 457 kB to 366 kB (144 kB to 115 kB gzipped) because the simulator is no longer reachable from app code. Two consequences were fixed rather than inherited — the device-only Roll path now recognises any simulated session, so a Twin session with no Roll server keeps the QR flow instead of silently losing it, and the connect screen says how to reach a Twin when none is answering, including the relay route for the split-port dev servers where the same-origin bridge cannot work.

### Added

- **The camera can hold a Roll, and every photograph it takes is durably queued for it.** What it still cannot do is send one. When this was first written the reason was that the repository recorded no P4-side pin for the C6 at all; that has since been resolved and the remaining reason is that nothing has been run on a board. `firmware/C6_HARDWARE_MAP.md` carries the routing and its evidence chain, `firmware/C6_BRINGUP.md` the order to bring it up in.
- Everything that does not need the radio was built and is host-tested — 1369 checks, up from 514. The upload queue is the part worth having: `UPLOAD.JSON` lives inside each capture's own folder, written to a temp name and renamed, the same metadata-last discipline that makes `META.JSON` the commit marker. One file per capture rather than one queue file, so a corrupt record costs one photograph instead of the queue, a job cannot outlive the capture it describes, and reconciliation is a directory scan rather than a cross-check between two files that can disagree. A job carries no image bytes — it names a UUID and every step re-reads the card, so there is nothing a reboot can lose, and re-reading is also exactly what a 422 `CHECKSUM_MISMATCH` asks for. Progress is a set of completion flags and never a byte offset: an offset would be a second source of truth about the server's state and would be wrong precisely when it mattered. The test that matters asserts the acceptance criterion directly — 50 captures queued with no network, carried across a reboot, then drained, each registering exactly once.
- The contract's two statements about 422 had to be reconciled rather than picked between. Its queue section groups 422 with the drop statuses, "do not retry the same bytes"; its error table says "re-read the file from SD and re-upload". Both hold: the prohibition is on the *same bytes*, and a fresh read of the card is the one thing that can fix a checksum mismatch. So 422 is a bounded re-read that parks rather than loops, and it deliberately does not touch the network attempt counter — a bad file must not slow every later upload for that job. 401 and 403 halt the whole queue instead of parking one job, because they fail every job identically and parking them one at a time walks the entire backlog into FAILED for something the user can fix in a minute.
- **`NETWORK: NOT FITTED` was wrong twice over and is gone.** The C6 *is* on the module; what is missing is the P4's route to it. A user reading "not fitted" goes looking for a component to add. The Connection screen now reports the two facts separately — `Radio: ESP32-C6`, `Link: Not routed` — the same split `flashControl` and `flashHardware` already use, and every value comes from `net_link` so the screen becomes correct on its own when the transport lands. Association is never reported as connected: `NET_WIFI_ASSOCIATED` shows "Getting address", because a camera that says connected while DNS would fail has lied to the user and to its own upload queue, and `net_link_can_upload()` accepts `IP_READY` alone for the same reason. There is no on-screen keyboard, on purpose — a passphrase entered on a 480×800 panel with no physical keys is worse than the USB path, and building a bad one to claim independence from Studio would be the wrong trade.
- The ROLL screen shows a Roll, its join QR, and what is waiting. It works with no radio, which is the part worth explaining: `PublishedRollJoinRequest` is documented as a server-published assignment written to the camera over `ROLL_JOIN`, carrying an already-resolved `rollId`, `slug` and `guestUrl`. Studio is the party with an internet connection, so it creates or looks up the Roll against the real API and hands the camera the answer over USB-C. The camera persists that across reboot, renders the QR, and queues every capture against it — so a guest at a party can scan the camera's screen and be on the Roll while the radio question is still open, and when the transport does come up the backlog drains into a Roll that already exists and already has guests on it. `ROLL_CREATE` and the slug-only form of `ROLL_JOIN` are HTTP calls the camera cannot make and refuse with `NETWORK_UNAVAILABLE` naming the actual radio state; there is no CREATE button on the screen, because a control that cannot work is the same defect as a shutter that logged instead of capturing.
- Wi-Fi passphrases and the device token live in NVS namespaces of their own — `kino_wifi`, `kino_roll`, `kino_rollsec` — none of which is the `kino` namespace `config_store.c` owns and `GET_CONFIG` returns verbatim. That is a structural guarantee rather than a filter: there is no code path from a config reply to a passphrase, so the leak cannot be reintroduced by someone adding a field, whereas a redaction list would have to be maintained forever and would fail the first time it was forgotten. `NETWORK_LIST` reports the mask and `hasPassword`, never the passphrase, and the only way a secret leaves either store is a callback that hands it to its consumer and `memset`s the buffer before returning, so exactly one frame ever holds it. The `kdt_` token form, `Bearer`, `password=`, `token=` and `passphrase=` are stripped from every error string that can reach a KDP reply, `GET_LOGS` or a crash dump. Not protected, and recorded rather than implied away: `CONFIG_NVS_ENCRYPTION` is off, so a passphrase is plaintext in flash to anyone who can read the chip — the threat handled here is accidental disclosure through the protocol, the logs and the backups, which is the one that happens by itself.
- `FW_QUERY` answers, and `TargetId` gained `c6`. The D4 carries six firmware images and the version model could name five, so a C6 running a stale hosted image — a second thing that can be out of date in the field — could not be diagnosed. Every other command in the `FW_*` group still fails closed: there is one `factory` partition and no OTA pair, and a query is not an update path. Two conventions the response establishes: an empty `version` means the device could not read one and is not version zero, and an unreachable target is `idle` rather than `error`, because nothing failed and nothing was attempted.
- Uploads yield to photography rather than sharing with it. The worker runs at priority 2, below the UI at 4 and the capture workers at 5, and stops touching the card entirely while a capture is in flight. That is not only about CPU: the FAT volume is mounted with `max_files = 4` and a capture already holds a frame handle plus a read-back handle for its CRC check, so an upload reader competing for a handle would fail outright, and one reading the card during a four-camera transfer competes for the SDMMC bus the capture's timing budget depends on. Yielding costs an upload a few seconds; not yielding costs frames. Gate F has to measure it and has not.
- CI gained the C6 image, the two new host-test suites, and a reproducibility job. `CONFIG_APP_REPRODUCIBLE_BUILD` had been set for all three apps and the property was checked by hand once, but nothing enforced it afterwards — an unpinned dependency or a timestamp reaching the image breaks it silently, and the first place that hurts is an update whose hash does not match what was published. Two clean builds of the P4 and the C6 are now compared on every run.
- `GET_CAPABILITIES` still reports `network: false`, `roll: false` and `rollUpload: false`, and that is deliberate with the whole `0xa0..0xaa` surface implemented. Studio's `supports()` gate is fail-closed, so setting them true would make Studio render the Roll and Network pages and issue commands that must then refuse — a broken panel instead of an absent one. What the handlers buy in the meantime is not the flag: it is that a host gets a specific `NETWORK_UNAVAILABLE` naming the radio state instead of an `UNSUPPORTED_COMMAND` that cannot tell an unimplemented command from an unrouted chip. The new `radioFitted` and `radioRouted` carry the distinction until then.

- **The camera takes photographs.** One press captured nothing: `SHOOT` logged "no capture pipeline in this build" and that was the whole of it. `capture.c` now fires every online camera, writes `C1..C4.JPG` and a `META.JSON` into one folder under one UUID, and verifies each frame twice — the transfer against the node's own CRC-32, then the stored file read back off the card, because a link that delivered the bytes intact and a card that kept them are different claims. The frames are fetched concurrently, one worker per camera: the node link runs at about 92 KB/s and a UXGA JPEG is 200-400 KB, so four cameras in turn would be twelve seconds and the last frame would be a different moment, a different pose and possibly a different room. Done at once the capture costs about what one frame costs. A camera that fails costs you that camera and nothing else — the capture commits `partial` with a per-camera reason, because a dead CAM3 must not lose CAM1, CAM2 and CAM4. `META.JSON` is a `kino.capture` v1 document, the schema Studio already validates, so a card read on a laptop needs no reverse engineering. Wired to the on-screen key, the physical button and `CAMERA_CAPTURE` alike, and it emits `EVT_CAPTURE`.
- The shared trigger wire is driven — `GPIO32` pulses 200 µs at the start of every capture — and it coordinates nothing yet, which is recorded rather than glossed. The camera nodes still expose when their command arrives over their own UART, so the frames are separated by however long it takes to wake four workers and put four commands on four UARTs; that figure is reported as `dispatchSpreadUs`, under its own name. All three skews in `kino.capture.timing` stay `null` with a reason. `CAMERA_CAPTURE` with `action: "timing-test"` is refused rather than answered: a rolling shutter that free-runs has no fixed relationship between when a command arrived and when light was integrated, and microseconds invented here would be filed as evidence. Driving the pin now means the node-side arm is later a node change alone.
- `MEDIA_READ` and `MEDIA_THUMB` return bytes, so pixels can leave the camera at all — `MEDIA_LIST` had been offering names and sizes for files no host could fetch, which is why `gallery` was false. Raw bytes with the binary flag, not base64: a 300 KB JPEG through base64 costs a third again in transfer for nothing. `MEDIA_THUMB` pages exactly like `MEDIA_READ` on both the device and the reference mock, because a thumbnail is small but not bounded and a noisy frame can encode past `MAX_PAYLOAD` — a reply that silently did not fit would reach a client as a truncated JPEG rather than as an error.
- Every capture writes a `THUMB.JPG` beside its frames, made once from the frame already in memory rather than on demand from the card. The whole path is hardware — the JPEG codec decodes, the PPA scales, the codec encodes again — which is single-digit milliseconds against roughly a second in software, and that is the difference between a shutter that feels immediate and one that does not. The PPA scales in sixteenths, so a 1600-wide frame reduces by 3/16 to 300×225 rather than to a round 320: rounding up would overflow the box, and resampling afterwards would spend a second of CPU making a 320-pixel picture out of a 300-pixel one.
- The gallery screen shows the pictures instead of the card's statistics. Six 4:3 tiles a page, decoded on a task of their own — a page is six card reads and six JPEG decodes, and doing that inside a draw call would stall the screen for most of a second every time someone scrolled, while the screen is what tells them the camera has not hung. Tiles are 4:3 because every sensor on this camera is, and a tile of any other shape puts a black border down the side of every photograph it will ever hold. Chevrons sit in the margins where the page-turn taps are, drawn only in the direction that has a page. An empty grid says which nothing it is — no card, a card that would not mount, or a card with no captures — because a broken card and an empty one look identical otherwise. This is the only consumer standing next to the card, which makes it the only check that a capture is an image rather than a folder of the right size.
- A clock, and an honest account of what it is worth. The body has no RTC, no battery-backed clock and no network, and `kino.capture` requires `capturedAt`. `HELLO` gained two optional additive fields so a host can set the time, and every timestamp travels with a `clockSource` of `host`, `persisted` or `unset` — because a camera that has never been told the time must not invent a plausible date. A plausible wrong timestamp survives import, sorts into the wrong place in someone's roll and is never questioned again; an obviously wrong one is caught the first time anyone looks. A persisted time assumes no time passed while the camera was off, which is certainly wrong and deliberately so: it is the only assumption that cannot date a capture earlier than one taken before the power cycle. Host times outside 2020-2100 are refused, which is what a seconds-for-milliseconds mix-up looks like.
- The camera's screen is a four-lens viewfinder, which is what a four-lens body's screen is for — the six-tile launcher moved behind a MENU key. Two things had to change before it could exist at all. camnode accepted only `1600x1200` and `2048x1536`, both full-size stills at 7.7–30.4 KB, which is about 330 ms of link time each and would have made a four-up view a slideshow at under 1 fps; it now also accepts `640x480`, `320x240` and `160x120`, deliberately **not** on the KDP `Resolution` type, because a capture must never be stored at one. And `cam_link.c` drove a single UART with every piece of its state — mutex, decoder, counters, sequence — as a module global; it is now four independent channels, because the P4 has five high-power UARTs and each camera has its own, so four transfers overlap instead of queueing. That is the whole difference between a viewfinder and a slideshow, and it shows on the bench: with nothing wired, all four channels report their timeout within one millisecond of each other rather than eight seconds apart. Decoding is the P4's hardware JPEG engine — four software JPEG streams would cost more CPU than the link costs bandwidth. Panes with no node say `NO LINK` rather than showing a black rectangle that could mean any of several things, and the viewfinder only asks for frames while it is the screen being looked at.
- Timeout budgets are now per-caller rather than per-command. A stored capture may fairly wait eight seconds for a slow node — it is the photograph. A viewfinder pane may not: freezing the picture someone is framing with for eight seconds is worse than admitting it has nothing, and the next frame is 200 ms away regardless. Preview capture waits 900 ms and preview read 600 ms.
- Repeated link timeouts are counted instead of printed. An absent node is a permanent condition and the viewfinder asks each camera for a frame several times a second, so an unwired channel produced roughly three log lines a second forever — filling the ring and burying everything that mattered. The first failure keeps its full per-request detail, because that is the one that diagnoses the fault; repeats are summarised as a run length on the next line after thirty seconds, and a node that comes back logs its recovery. Measured with four dead UARTs: eight lines a minute instead of a hundred and eighty, with `run 64` still saying how fast they are failing.
- Physical controls, with no pins invented for them. `docs/HARDWARE.md` says outright that button and mode-slide pins are unassigned, so `BOARD_BTN_SHUTTER` and `BOARD_BTN_FN` are `BOARD_BTN_NONE` and skipped at init — a floating input read as a button fires the shutter by itself in a bag, which costs a roll and a battery, and that is a far worse failure than a control that does nothing. Assigning a real pin is a one-line change. The physical shutter and the on-screen key run the same `fire_shutter()`, because two ways to fire one shutter must not become two implementations of it. Unlike a touch, a button press is not swallowed when it wakes a dark screen: someone pressing a physical shutter means to take a picture whether or not they can see the panel.
- The camera has its own screen. A 480×800 ST7701S over MIPI-DSI, driven as a landscape 800×480 canvas through the P4's Pixel-Processing Accelerator: the CPU draws into linear memory and the PPA rotates and alpha-blends whole frames in hardware. Doing the quarter turn in the drawing primitives instead — writing each logical row down a column of the panel buffer — costs a 960-byte stride per pixel in PSRAM, a cache miss per pixel, and a renderer that cannot animate; the boot dissolve went from roughly 5–8 fps to a measured 26 frames in 452 ms, which is panel rate. Two framebuffers, because `draw_bitmap` recognises its own buffer and switches the scan source rather than copying, so presenting a frame is a cache write-back and a pointer swap instead of a 768 kB copy into memory the display is reading. Animation is driven by the clock rather than a frame counter, so a slow frame costs smoothness and never timing. Six destinations behind a boot iris that opens onto the wordmark; presses activate on release, so a finger that lands on the wrong tile can be slid off it.
- On-device power management, implementing the `BodyConfig` contract rather than inventing a scheme: `sleepS` blanks the backlight and `camIdleTimeoutS` drops the camera bank on `CAM_PWR_EN`, so four idle XIAOs stop costing a 3000 mAh cell anything. Thresholds are re-read every pass, so a Studio change takes effect immediately instead of after a reboot. Touch feeds the idle timer and KDP traffic deliberately does not — Studio polling status every second would otherwise keep the panel lit in a bag.
- `GET_POWER_STATUS`, `GET_CONFIG`, `SET_CONFIG`, `SAVE_CONFIG`, `RESET_CONFIG`, `GET_MODES`, `SET_MODE`, and the read side of the gallery: `MEDIA_LIST`, `MEDIA_INFO`, `MEDIA_DELETE`, `MEDIA_FAVORITE`. Studio and Twin call all of the first five and the firmware implemented none of them. Settings are held as a JSON document rather than a C struct and written by deep merge, for two reasons that are the same reason: a struct would drop a field this firmware does not understand yet, so Studio would appear to save a setting and then lose it, and a replacing write would clear every sibling of the field being changed. Verified against the hardware over the real link, including a setting surviving an actual power cycle. The card is the only gallery index there is — no database is kept, because a card pulled and edited on a laptop is a normal thing to do to a camera and an index would then be a lie that outlives reboots.
- `firmware/p4/host_preview`, which compiles the firmware's own renderer natively and writes every screen to a file. Looking at the UI used to mean a build, a flash and a serial capture — about two minutes to see one pixel change, at a resolution low enough to hide the thing being judged — and every visual bug so far survived precisely because looking was expensive. It found three in its first run. `ui.c` is included as a translation unit rather than linked, so the static drawing functions are reachable without putting a single test hook in the firmware.
- `firmware/uvc-preview`, a bench tool that makes a XIAO ESP32-S3 Sense present itself as an ordinary USB webcam, so a camera module can be judged the moment it arrives — no harness, no P4, no Wi-Fi, no host tooling, and any camera app is the viewer. `camnode` could not do this job: it serves the node link on UART1 and captures one JPEG on request, and a still frame is the wrong instrument for soft focus, an OV3660 colour cast, mismatched field of view, or frames the sensor fails to produce under load. It logs the sensor PID at boot and the frame rate and empty-buffer count when the stream closes, so a module gets a number rather than an impression, and the LED carries the boot verdict because TinyUSB owns the USB PHY and there is no console on the port you just plugged in — which is also why reflashing needs BOOT and RESET. Pins come from camnode’s `board_xiao_s3.h`; the tool declares none of its own. Studio’s bring-up worksheet gains the matching CAMERA MODULE INCOMING CHECK, first because the modules arrive before the harness does, and it asks for agreement across the set rather than four modules that each work alone.
- Studio's bring-up worksheet runs past the electrical build, because V1 is a camera someone carries rather than a rig on a desk. Four sections join the three from the hardware spec: the printed body, which is structure and not packaging — it holds the lens baseline, PLA and PETG creep, and pitch is therefore measured as printed and measured again after a month in a bag; the closed-body power and thermal numbers, which an open bench cannot produce and which size V2's regulators, bulk capacitance and venting; the effect itself, the one test no simulator stands in for and the only one that can invalidate the product rather than the build; and field reliability, since a hang at capture #40 during an event loses the roll and nobody reruns the party. Checks that produce a number now have somewhere to put it, and the values travel with the exported record — a tick that records no measurement wastes the run it came from. One RUN button captures the perishable device numbers in a single file: link stats for every camera, the log ring and runtime stats, none of which survive a power cycle.
- Bring-up failures carry their measurement, so the first bench run on real hardware is also the diagnostic one. A node-link timeout logs elapsed against budget plus the bytes, frames, duplicates and CRC errors that arrived during that one request — silence, a stalled transfer and a reply to a request the P4 had already given up on used to produce the same line. Capture failures name their numbers too: where in the JPEG the transfer died and how far that was as a percentage, how much memory the staging buffer wanted against what was free, the implausible size, both checksums on a mismatch. `CAMERA_LINK_STATS` gains `latencyMaxMs`, because a timeout budget has to be sized off the worst round trip and the most recent one tells you nothing. Every budget in `cam_link.c` was chosen before any hardware existed, and `BENCH_M1B.md` now says to pull the numbers before power-cycling, which is when they are lost.
- Studio, Twin and Roll show the KINO wordmarks instead of spelling the brand in letterspaced text: Studio in the menu bar and connect card, Twin in the header and welcome overlay, Roll in the guest header, PIN gate and both host dashboards. The brand rasters are opaque and no app surface matches their baked-in field, so each app ships a trimmed transparent derivation keyed from ink coverage; on dark chrome that reproduces the official `*-on-dark` artwork, badge knockout included, rather than inverting a raster in CSS. The shipped marks stay `LicenseRef-KINO-Reserved` and `npm run license:check` fails if the MIT grant over `apps/**` ever swallows them.
- Roll guests get the audited product surface: MY PICKS keeps hearted photos in a tab of their own, live arrivals collect behind a "N new" pill instead of reflowing a feed someone is reading, `/r/:slug/display` drives a TV or projector with an optional scan-to-join QR, and one-tap SAVE PHOTO / SAVE WIGGLE sit beside 9:16, 4:5 and 1:1 social crops rendered on first request.
- Enhancement modes are real and still off by default: SUBTLE is a frozen preset and CUSTOM is configurable, both running on a local in-process provider that is deterministic and shares one grain field across a capture's frames, with the provider, operations and strength recorded on every enhanced asset. An operation outside the wiggle-safe list is refused, never quietly dropped.
- KINO Twin can be served to a Studio in another browser, container or machine over a WebSocket relay (`npm run twin:relay`): the same wire vocabulary and connection state machine as the same-origin channel, with only the carrier swapped.
- Twin exports fabrication files — front-panel DXF with real lens cutout positions, per-instance transform CSV, and a STEP solid of the assembly envelope — all resolving through the same geometry the viewport draws, so a measured override changes the file.
- Twin components can be backed by converted CAD meshes: a registered mesh replaces the parametric proxy, fitted to the profile's recorded dimensions so clearance and the BOM stay authoritative, with the proxy as the fallback when no asset is present or a load fails.
- Twin records component material and mass where a real source exists, and offers an experimental 4×16340 bench pack as a selectable alternate power profile that is never the default, never saved into a layout, and always labelled.
- The Twin acceptance walk runs headless in CI: connect, capture, fault injection, record/replay and a measured override are asserted on every pull request, while the WebGL "does it look right" checks stay a human gate.
- `npm run release` produces a release bundle from a clean checkout — Studio, any built firmware images, a machine-readable manifest with per-target digests, and checksums over everything — and marks a bundle built from a dirty tree as unreproducible. `docs/RELEASE_TRUST.md` records the signing and firmware-rollback contract before either is implemented.
- Studio's Overview reports Wi-Fi and Roll state beside the power rows, the Gallery can open a folder of already-transferred frames without a camera attached, camera cards show the armed state, and the Developer page gains an SD write bench and a power-load bench that plots measured draw against the model's prediction.
- Render consistency and per-capture playback (audit #59): the wiggle alignment geometry moved into `@kino/media` and the worker's baked WebP/MP4 now apply capture-time calibration offsets (rotate + common overlap crop at source resolution) when the capture's provenance carries them; derived assets record `calibrationVersion`, `aligned`, `crop`, and `look` in their producer identity, and `metadata.json` echoes provenance and the applied calibration. New `captures.playback` column (migration `0009_capture_playback`) with a host `PATCH /api/host/captures/:captureId/playback` route that persists fps/loop/direction in the KDP vocabulary, re-renders the wiggle derivatives, and drives both the guest player and the baked files. A truthful capture-time `calibrationVersion` and offsets must come from the device; firmware does not record them yet.
- KINO Studio with simulated and Web Serial device connections.
- KINO Device Protocol framing, CRC, commands, transports, jobs, and timing vocabulary.
- Portable `kino.*` schemas with versioned migrations and unknown-field preservation.
- Reference D4 device, media store, factory recipes, and injectable failure scenarios.
- Roll API foundation with authentication, roll lifecycle, resumable uploads, object storage, and live events.
- Installable Roll guest PWA with a virtualized live feed, mode-aware capture detail, sharing, download controls, and anonymous reactions.
- Private Roll host dashboard with QR sharing, live moderation, Roll settings, guest counts, and durable ZIP export polling.
- Studio can register a connected KINO with the Roll server, pass its write-only device credential to the camera, and assign server-created Rolls without making offline camera work depend on the backend.
- Studio can check the Roll firmware catalog, see why a release is incompatible, download verified P4 and camera-node images, and retain a loaded local package when the server is offline; maintainers can publish verified packages with the V1 CLI.
- Shared KINO design tokens and accessible utility primitives now keep Studio and the responsive Roll guest/host surfaces in one visual family.
- Studio and Roll status changes, queue progress, sharing and exports now announce correctly; keyboard tab navigation, mobile PIN hints, focusable feed scrolling and automated contrast guards close the pre-hardware accessibility audit.
- Background processing for capture derivatives, playable roll recaps, durable ZIP exports, and recoverable seven-day trash retention.
- Maintained hardware, architecture, development, firmware-contract, troubleshooting, contribution, security, and release documentation.
- Split light and dark KINO D4 brand marks plus a real Studio demo capture.
- Path-scoped MIT software and CERN-OHL-S-2.0 hardware licensing with SPDX/REUSE metadata and reserved brand assets.
- Machine-readable software, protocol, schema, database, and hardware version control enforced by CI.
- D4 physical revision records, artifact versions, and numbered engineering change notices.
- Public KINO D4 project board with roadmap-backed issues, target views, and a repository helper for status updates.
- KINO Twin foundations: versioned D4 hardware profiles, deterministic simulation and replay, BroadcastChannel KDP transport, parametric assembly, engineering viewport controls, inspector, and wiring view.
- Studio connection to KINO Twin through the same KDP client path used by serial hardware, including cross-tab handshake and reboot coverage.
- Twin transport leases recover from crashed Studio tabs, and Studio detects device restarts even when the underlying link remains open.
- Large Studio galleries can extend their index in bounded 5,000-row windows without eagerly loading every thumbnail.
- KINO D4 firmware (ESP-IDF): P4 main controller and XIAO camera-node applications with a host-tested portable KDP C core, checksummed single-camera capture to SD, bench diagnostics (storage self-test, link stats, soak job, per-unit hardware-validation registry), structured device logs, honest runtime stats, and a six-check self test — CI-built, awaiting physical bench validation.
- Twin now has a top-level recovery screen and stricter assembly/runtime invariants, with replay, power, flash-risk, wiring, and scene-store edge coverage.
- KINO Twin optical overlays show per-camera axes and frusta, neighboring and four-camera overlap, adjustable subject proxies, and live pitch/distance readouts; unmeasured D4 optics remain explicitly marked `MEASURE REQUIRED`, while candidate lens angles are labelled design scenarios.
- Production and isolated staging stacks now run behind Caddy with automatic TLS, unbuffered live events, private PostgreSQL/Redis/MinIO services, gated migrations, and API-streamed private media.
- Redis-backed request budgets now protect device uploads, guest reads, PIN attempts, device registration, device Roll joining, and anonymous Roll creation.
- API database statements and object-storage requests now time out instead of holding the shared connection pool behind a hung dependency.
- A camera-simulating uploader now verifies resumable fixture uploads, lost-ack and duplicate retries, background derivatives, guest visibility, host closure, and controlled concurrent load before physical cameras are available.
- Production operations now include atomic off-host database/object backups, bounded daily/weekly retention, an isolated digest-verifying restore drill, authenticated Prometheus application metrics, rotating JSON logs, and private MinIO/node-exporter signals.
- KINO Twin can measure two picked assembly points and continuously report assembled-pose component collisions, sub-millimetre hard clearances, cable clearances, and blocked USB or microSD service keepouts.
- KINO Twin now runs the shared KDP simulator in-tab, exposes staged power/test-capture controls, and visualizes boot, exposure, UART, SD, upload, firmware, power, thermal, and Studio connection activity.
- KINO Twin exposes protocol-honest fault injection plus power, synchronization, and rolling-shutter flash analysis panels with explicit simulated and estimated provenance.
- KINO Twin stores versioned as-measured component overrides separately from canonical profiles and provides an enclosure-lock measurement checklist that refreshes geometry and collision findings immediately.
- KINO Twin records and verifies raw-KDP simulation sessions, exports versioned layouts and engineering reports, captures viewport PNGs, and ships a same-origin Studio/Twin preview harness with a 17-point acceptance runbook.
- Twin now guides a first-time user: a powered-off start card names the one action that matters, boot progress renders as a live ladder in the viewport, power and shutter are primary header actions with stated disabled reasons, right-panel tabs carry plain names with one-line descriptions, panels say why they are empty while the simulator is off, the parts list explains itself, and the reference grid has a toggle.
- The Twin device no longer fires ambient demo captures: every capture in the 3D view was commanded by the operator or Studio. A bare reference fixture keeps its lively ambient behavior for the test suites.
- The Twin display renders the live simulated device UI — boot stages, firmware version, battery voltage, SD/Wi-Fi/roll status, capture and firmware-update progress, per-camera faults, and a SIMULATED-labelled CAM2 preview field — on the 3D glass and in a DISPLAY inspector tab whose shutter drives the same raw-KDP capture path Studio uses.

- Master Twin+Studio audit against the D4 hardware/software specification: compliance matrix and thirteen maintained documents under `docs/audit/`, provisional P4 header assignments and XIAO DVP pin map in the hardware profile, and OV3660/OV5640_AF sensor profiles recorded as capability-driven data.

- Capability-driven autofocus architecture: `CAMERA_FOCUS` (trigger/lock/set/mode/store-fixed) behind the new `autofocus`/`focusLock`/`manualFocus` capabilities, PARTY AUTO / PARTY FIXED / MANUAL focus modes with the Wiggle focus→lock→arm→capture flow, per-camera focus state in camera info and the Twin snapshot, four AF faults (fail, VCM stuck, timeout, hunting), a capability-gated FOCUS panel in Studio, and AF state on the Twin's device display. OV3660 firmware simply lacks the surface; nothing assumes OV5640 globally.
- Flash timing is honest about exposure: the Twin flash timeline lights each camera's own SIMULATED exposure window instead of the whole frame, `flashBandRisk` moved into the protocol package and accepts per-camera windows, and Studio gains a FLASH TIMING bench that measures VSYNC phases and reports pulse coverage with measured and assumed inputs labelled separately.
- The power model tells one story: the engine's state of charge follows the device's own draining battery, a charger-connected scenario feeds 0.6 A through GET_POWER_STATUS and the Twin model alike, SW6106 light-load shutdown and a one-camera capture brownout are injectable, the 5 V rail droops gradually past 3 A instead of holding a cliff, and bus demand past the converter's 18 W class carries its own warning.
- Idempotent protocol reads retry once on timeout with a fresh sequence number; mutations stay one-shot. The capability gate fails closed when the capability query never answered, keeps the deliberate everything-on fallback for pre-negotiation firmware that NACKs it, and the client drops CRC-valid frames whose framing version it does not speak.
- The reference device can now injure the transport like real hardware: duplicated response frames, a dropped byte in transit, a mid-frame link death, and a wrong-baud garble that never frames. The Twin BroadcastChannel path splits frames like the serial mock so Studio's decoder reassembles on both. One shared contract test drives the identical command sequence over the serial mock and the Twin channel.
- Restoring a backup made on a different camera now warns with both serials before anything is written — per-camera calibration is measured on one physical unit.

- Capture and derivative provenance: everything a firmware build sends beyond the typed capture surface now lands in a `provenance` column with the device's serial and hardware as they were at the shutter press, and every derived asset records its producer — job, encoder, and the settings that decided the bytes — with a produced-at timestamp. Retuning a render constant is now visible in the data.
- The AI enhancement gate exists before any AI backend: OFF by default, provider-independent interface (local / self-hosted / external), and an external provider is refused without explicit consent — the skip reason names which gate held.

- Database migration 0008: additive `provenance` on captures and `producer`/`produced_at` on assets.
- Studio closes its audit gaps: calibration exports a full per-unit report and imports per-camera offsets only (order and spacing stay physically verified), firmware downgrades warn loudly with both versions before the same explicit confirm, the health overview shows the device-reported 5 V rail or says NOT REPORTED, and per-camera temperature sits on the camera cards.
- Twin gains a PINS tab — the first consumer of the profile's 2×13 header table, provisional GPIO assignments, and XIAO DVP map — and the hardware-profile schema carries optional mass/material metadata and per-instance optical-center offsets that the optics overlay applies (zero until the bench measures real centers).
- Twin gains a ROLL tab: a development bridge that registers as a device, creates or joins a real Roll, shows the working `JOIN THIS ROLL` QR on the virtual D4 display, and uploads committed virtual captures over the public device wire contract — thumb first, idempotent by capture UUID + asset role, with backoff retry across a server outage and an honest single-frame ingest on the current-firmware profile.
- A party simulator (`npm run party:sim`) drives bursty capture load, concurrent SSE guest sessions, and an outage drill against a running Roll API, reporting capture-to-guest arrival percentiles and duplicate counts; `docs/roll/` documents the device upload contract for the future Wi-Fi firmware milestone, the Twin integration, the capture schema map, the realtime architecture, the acceptance walks, and the guest product audit.
- The Roll guest product closes its audited gaps: a MY PICKS tab mirrors the guest's hearts locally per browser (host moderation drops stale picks), live arrivals buffer behind an "N new" pill instead of shifting a scrolled feed, `/r/:slug/display` puts the roll on a TV or projector full-bleed with an optional `?qr=1` join QR and a screen wake lock, and capture detail gains labelled SAVE PHOTO / SAVE WIGGLE plus 9:16, 4:5 and 1:1 social crops — the MP4 and the crops render on first request through `POST /api/rolls/:slug/captures/:captureId/renders` and the new `render-social-formats` worker job, all behind the host's downloads switch.

- `infra/deploy.ps1` operates the Roll production stack on a Windows server: environment-file creation with consistently generated secrets, pre-flight validation, build-and-wait-for-healthy startup, fast-forward update, byte-faithful database backups, and a volume-preserving stop. `npm run dev:all` starts the whole development stack — infra services, migration, api, worker, roll-web, twin, studio — in one terminal with prefixed output.
- An automated Studio↔Twin end-to-end test drives the real BroadcastChannel transport through connect, the full read surface, bench diagnostics, a capture landing in the gallery with genuine JPEG bytes, Roll creation, and upload enqueue — the whole connection is a CI assertion, and a second test drives Studio's real session path against the honest Milestone-1B firmware profile.
- `npm run version:check` now diffs every KDP command and event name/value between the normative TypeScript enum and the firmware header (both directions), and asserts the Twin's firmware-profile map covers `firmware/VERSION`; the firmware CI job also triggers on contract, protocol, and build-tooling changes, and the camnode's camera component is pinned with its lockfile committed.
- `npm run release` builds a release bundle from a clean checkout — Studio, any built firmware images, a machine-readable `kino.release` manifest with per-target digests and hardware compatibility, and `SHA256SUMS` over everything — after running the version, license, lint and build gates, and it marks a bundle built from a dirty tree as not reproducible from the commit it names. The trust contract that has to exist before signing (who signs, where verification keys live, the refusal order before flashing, key rotation and loss recovery) and the firmware rollback state machine are written in `docs/RELEASE_TRUST.md`, with `FW_ROLLBACK` reserved in both protocol enums so no UI is built against an invented shape.

### Fixed

- The firmware build daemon stamped every package `compatibleHardware: ["V1"]`, a string no device reports - the P4 firmware answers `kino-v1` and the Twin `D4-V1` - so a daemon build was BLOCKED at Studio's compatibility gate on any camera. It lists both.
- `firmware/p4/host_preview/Makefile` began with a UTF-8 byte-order mark, which GNU make reads as a missing separator; `make -C firmware/p4/host_preview` failed before it parsed. The BOM is gone.

- The published worker image installs the distribution's ffmpeg and drops the npm-fetched build, so the GPL obligation that MP4 rendering carries attaches to a binary whose corresponding source is published — recorded in `THIRD_PARTY_NOTICES.md` along with the development fallback.
- The Twin's flash now reaches the photograph: a flash exposure lights the virtual sensors' render from the module's front (inverse-square falloff with subject distance, sensor layer only, so the 3D viewport stays unlit), previews never flash, and the flashUnavailable fault suppresses it. Before, FLASH ON changed only the log line and metadata — a dim-party capture rendered identically dark either way.
- A capture in flight now survives a host link drop: the reference device's exposure → transfer → SD-commit chain no longer dies when the KDP client disconnects right after the CAMERA_CAPTURE ack — only power-off or reboot cancels it, matching the physical rule that unplugging the cable must not lose the photograph.
- The reference device actually verifies firmware images: `FW_END` hashes the received bytes against the declared sha256 and rejects a corrupted image instead of answering `verified: true` unconditionally.
- Studio backups no longer contain the camera's Roll identity block, strip it from older backup files on restore, and now record camera firmware versions, protocol, and the config schema version.
- A Twin session is labelled KINO TWIN in the Studio toolbar and sidebar instead of appearing as USB hardware, the Overview FLASH lamp is device-capability-driven instead of hardcoded green, and the conformance runner classifies unsupported commands correctly.

- Twin typography sits on one four-step token scale with nothing below 10 px, and every panel padding lands on the even spacing grid — the tabs, chips, tables and checklists read as one instrument again.
- The Twin display screen stays crisp when viewed through the clear rear acrylic instead of being washed out by the panel's tint.
- Twin assembly renders the D4 correctly: the display sits landscape inside the body with its glass facing the rear, its connector keepouts hug the board face instead of floating in space, the camera-node service keepout covers rear USB-C access instead of the lens, and the enclosure skeleton is an open edge frame so internal components are visible through the clear panels.
- Sensor-missing camera nodes remain available for firmware repair while sensor-dependent calibration reports the specific fault.
- Repeated Twin power-off calls no longer emit duplicate shutdown activity, and scenario toggles notify observers once.
- Studio and Twin production bundles are split into measured chunks instead of shipping a single oversized application bundle.
- Updated `sharp` to 0.35.3 in image-producing workspaces to remove the current high-severity libvips advisory.
- Production device registration is now first-write-wins, preventing a known serial from rotating and stealing an already deployed device credential; repeated unknown Roll join codes trigger an hour-long per-device lockout.

- Twin's left parts tree ellipsizes component names with the full name as tooltip and stops breaking instance ids mid-word; the scene workflow gained CLEAR STAGE, Delete-key removal, an OPERATE view pose, a far plane that no longer clips distant subjects, and the two overlap readouts carry distinct names.
- `GET_LOGS` can always answer: a full 200-entry log ring serialized past the 16 KB frame cap and the firmware silently sent nothing, so every log fetch timed out forever — firmware and the reference device now return the newest entries that fit, the TypeScript encoder refuses oversized frames loudly, and the contract documents the cap.
- Refusals no longer look like success in Studio: a failed firmware update renders its error (the store field was never displayed), FACTORY RESET and RESET CALIBRATION surface a NACK instead of silence, and a refused factory reset disarms the reboot-expected window instead of priming the next disconnect to be misread as a reboot.
- Studio degrades honestly on Milestone-1B firmware: Gallery, Wiggle and Quad leave the navigation when the camera reports them unsupported, Shoot and Calibration explain what the firmware lacks instead of a blank page or error-collecting buttons, backup/restore says the firmware exposes no settings instead of "still loading", and the toolbar GALLERY button follows the same gate. `SerialTransport` observes write backpressure and gained its first test suite.
- The Twin clearance panel stopped reporting the entire interior as colliding: shell parts carry only a PROVISIONAL whole-body envelope box and are excluded as NOT EVALUATED until measured shell geometry exists; with the noise gone the engine surfaced real conflicts, and the PROVISIONAL layout was re-placed to zero findings — the battery stands vertically on the left, the power module moved to the mid-plane, and all thirty harness routes were re-authored with endpoints pinned to instance positions.
- The Roll stack starts as documented: the api and worker dev entries load `infra/.env` and default `NODE_ENV`, the API gained CORS for localhost origins and a `PORT` override, Studio and `preview:all` proxy `/api`, and the three web apps hold fixed dev ports. Joining a Roll no longer fabricates a guest link on the device's own origin — the server names it; Roll server calls time out instead of hanging; a failed TEST SERVER restores the device-only stub; dead navigation ends were removed (Simulator Faults toggle, recovery instructions inline, Twin re-probe after a failed connect, offline Bring-Up worksheet in developer mode).
- The Studio firmware builder path works end to end: built packages declare the hardware string devices actually report instead of the design revision label (compatibility always read BLOCKED), three hardware-validation items no longer lose their bench evidence on reboot to over-long NVS keys, the build daemon reports missing tooling as missing tooling with the spawn error in the log, restricts its browser surface to localhost origins with a DNS-rebinding guard, and no longer blocks firmware builds on unrelated backend version drift.
- Studio's hardware-compatibility check normalises the four spellings of one board (`V1` from GET_DEVICE_INFO, `kino-v1` from GET_CAPABILITIES, `D4-V1` from hardware/REVISION, catalog `compatibleHardware`) to one canonical id before comparing; a package built for the connected camera was refused on spelling.
- Studio Developer BENCH DIAGNOSTICS took no link claim: SD self test, link stats, CAM1 test capture, HW validation, storage bench and the soak now claim and release the device like PHASE does, each with its own status-bar label, and a soak that fails no longer leaves the button spinning.
- Studio Overview RUN SELF TEST read a stale `testRunning` from its first render through the toolbar's uiBus subscription; it reads the live flag, resets when the camera disconnects, and a 60 s safety timeout clears the busy state with an error instead of spinning for the session.
- Studio FOCUS: switching mode re-reads config so the mode highlight and MANUAL slider follow; HOLD/RELEASE FOCUS is gated on `focusLock` and the manual VCM slider on `manualFocus`.
- Studio Roll and Gallery sent NETWORK_STATUS / ROLL_STATUS ungated while the session poller gated them on `network` / `roll`; the pages use the same gates and hide the row when the firmware lacks the group instead of reporting a link fault.
- Studio VIEWFINDER retried a refused CAMERA_PREVIEW every ~1 s forever; a firmware that NACKs it stops the loop and shows Unsupported ("This firmware has no host viewfinder").
- Studio: `void refreshAll()` from the toolbar and the page-level `refreshDeviceInfo()` calls could reject with an unhandled `KinoTimeoutError: No response to GET_DEVICE_INFO`; both route through the poller's `pollFailed` freshness handling and SYNC reports the miss in the status bar.
- Studio: a link that stays open while the camera stops answering (a hung board on USB CDC, a Twin that let go of the relay) no longer sits on KINO CONNECTED with every read timing out. Three unanswered polls in a row raise the close the transport never sends, and the strip reads HARDWARE ERROR with the reason.
- KINO Twin: the device server's client lease is counted in heartbeats, not milliseconds, so a Studio in a background tab (throttled timers, but every ping answered) is no longer dropped; and when a lease does end the client is told with a close, instead of being forgotten silently.

### Known incomplete work

- **The D4 menu icons are Microsoft's and KINO holds no licence to them.** `REUSE.toml` declares all of `firmware/**` MIT, which would have relabelled the artwork as ours to give away; `firmware/p4/main/icons_w98.h` is now mapped to `LicenseRef-Microsoft-Proprietary` instead, and `license:check` fails if either that mapping, the header's own SPDX line or the `THIRD_PARTY_NOTICES.md` entry goes missing. The annotation records the ownership and grants nothing: a firmware binary built from this tree embeds the artwork, publishing that binary is redistribution, and scaling by an integer factor does not make it a new work. Issue #134 has to be answered — a licence, or original artwork in the same idiom — before a release carries these files.
- The D4 firmware in `firmware/` is CI-built but bench validation on physical hardware is pending; final hardware measurements are not recorded yet.
- Firmware signing and rollback are not implemented.
- Mobile-device browser acceptance (iOS Safari, Android Chrome) is device-gated and unrecorded.

## Changelog rules

- Record user-visible changes, protocol compatibility changes, schema migrations, database migrations, hardware revisions, and security fixes.
- Keep refactors out unless they change a public contract or remove a supported path.
- Move Unreleased entries into a dated version section only when that version is published.
- Link the release section to its Git tag once tags exist.
