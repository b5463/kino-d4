{
  "name": "@kino/simulator-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "//sideEffects": "Pure classes, functions and constants — no top-level work. Studio imports only computePower from this package; without this flag Rollup keeps the whole index, dragging TwinSimulator and MockKinoDevice into the shipped bundle.",
  "sideEffects": false,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": { "test": "vitest run", "lint": "tsc --noEmit" },
  "dependencies": {
    "@kino/kdp": "*",
    "@kino/test-fixtures": "*",
    "@kino/hardware-profiles": "*",
    "@kino/schemas": "*",
    "zod": "^3.24.0"
  },
  "devDependencies": { "typescript": "~5.8.3", "vitest": "^3.1.3" }
}
