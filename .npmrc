# `engines` in package.json is advisory unless this is set: without it npm
# prints a warning and installs anyway, so a Node 20 checkout gets a tree that
# builds here and fails in CI. Node 22 is what CI installs and what README and
# CONTRIBUTING tell a contributor to use, so make the version a refusal rather
# than a warning.
engine-strict=true
